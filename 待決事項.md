# 待決事項

本檔記錄**真正無法從既有規格推導、且會影響核心玩法或產品方向**的未決問題。

收錄判準見 `AGENTS.md`：能從 `規格企劃.md` 既有原則合理推導的 UI、資料結構、API、MCP、Server 行為，由實作者直接補齊，不進本檔。只有當 A/B 選擇會明顯改變跑團方式、影響 DM / Player 核心權利、改變規則玩法，或造成難以逆轉的產品方向時才收錄。

每一項要寫清楚：問題、已確立不再重開的前提、選項與代價、與既有規格的衝突、決定時機。

已拍板的項目從本檔移除，決定寫回對應的 SSOT 文件；歷史從 git log 追。

最後更新：2026-09-03

---

## OQ-001 — 外部 AI 進桌的傳輸層

**狀態**：未決
**提出**：2026-09-03
**決定時機**：**P3 — Exploration + Roll + AI 開工前**。M01 / M02 / P2 期間不需要答案，**也不得因為本項而提前設計 P3 的 schema / API / tool 契約**。

### 已確立的前提（不再重開）

使用者已明確表達目標：**由本機的 AI session 進入網站後可以實際去動作。**

因此以下不是選項：

- 網站後端自行呼叫模型、自備 API key 或代付費（`規格企劃.md`〇 硬原則 11）。
- AI 只能唯讀或只能給建議，不能真正執行 GameAction。
- AI 走一套跟真人不同的遊戲邏輯（`規格企劃.md` 十四）。

未決的只有**傳輸層**：本機 AI session 要走什麼路徑搆到站台，以及**誰是發起方**。

### 問題

`規格企劃.md` 全篇寫「AI 透過 MCP / Site Tools 進桌」，但沒有回答連線怎麼建立。現行寫法隱含站台要有一個 AI 搆得到的 endpoint，對一個朋友間自架的私人站台來說，這代表：對外公開可達的服務、Join Token / scoped Seat 認證基礎建設，以及隨之而來的曝險。這件事沒被討論過，不是已經決定過的事。

### 選項

| 選項 | 做法 | 代價 |
|---|---|---|
| **A — 站台暴露 MCP endpoint** | 站台提供 MCP server，使用者在自己的 Claude / ChatGPT / Codex session 裡連上，以 scoped Seat / Join Token 認證 | 站台要對外可達或至少同網段；要自建 token 簽發、撤銷、scope 過濾；符合現行規格方向 |
| **B — 本機瀏覽器橋接（Pedelec 模式）** | Web App 透過 SDK → Chrome Extension → Native Messaging → 本機桌面 runtime → 本機 agent CLI；agent 需要動狀態時 call back 進前端定義的 tool | 站台完全不必對外開任何東西；但每位參與者要裝 Chrome 擴充功能 + 桌面程式 + 自備 agent CLI 認證；**發起方變成網站**，與規格衝突（見下） |
| **C — 自建同模式** | 採 B 的架構但不相依 Pedelec | 不受單一上游專案存亡影響；但橋接層要自己寫、自己維護 |

三個選項都保住硬原則 11：模型永遠跑在使用者自己機器、用他自己的認證，後端一行模型呼叫都沒有。

B / C 有一個 A 沒有的附帶好處：AI session 綁在某個瀏覽器分頁上，那個分頁本來就只有該 Seat 的權限，**「秘密靠 Server 過濾」幾乎免費**——前端 tool 只要是既有 seat-scoped server API 的薄包裝，Secret DC / DM Notes / Hidden Monster / 他人 private knowledge 自然送不到 AI Player 手上，不必另外設計一套 scoped MCP token。

### 與既有規格的衝突

`規格企劃.md`〈AI Parse 由外部 AI 執行〉一節寫：

> 同一原則適用於全站：AI DM、AI Player、Character Builder 協助、`resolve_action()` 的敘事，全部由外部 AI Session 發起。**網站永遠是被呼叫的一方。**

選項 B / C 把方向反過來：Web App 建立 session 並送出 prompt，agent 回頭 call 前端 tool。**若選 B 或 C，這句規格必須同步修改**，不得默默推翻。選項 A 沒有這個衝突。

### 參考資料

**Pedelec**——把「Web 前端呼叫本機 AI coding agent」做成現成橋接的開源專案，是選項 B 的具體實例、也是選項 C 的參考架構。

- Repo：`https://github.com/kaoruisaac/pedelec`
- 文件站：`https://kaoruisaac.github.io/pedelec`
- 產品站：`https://pedelec.cc/zh-tw`（注意：Anthropic remote session 的 egress proxy 擋這個 domain，只能從 repo 讀）
- npm SDK：`@kaoruisaac/pedelec`
- 授權 / 版本：MIT、v0.2.12（2026-09-03 檢視）、單一作者、repo 約 5.9 MB

架構：`Web App / SDK → chrome.runtime.connect（Extension，origin approval gate）→ Chrome Native Messaging → pedelec-native-host → Tauri 桌面 CoreRuntime → provider CLI`。支援的 provider：`codex | antigravity | opencode | cursor | claude | ollama`。

SDK 介面（`sdk/src/index.ts`）：`createSession({ provider, effortLevel, skills })`、`session.sendText()`、`onChat / onTool / onStatus / onError / onEnded`、`defineTool()` 帶完整 `argsSchema`、`uploadAsset / readAsset`、`checkAvailability() / getApprovalStatus()`。

值得單獨一看的檔案，**不論最後選哪個選項都可以拿來用**：`skills/pedelec-integration/references/tool-design.md`。那是一份 tool 契約檢查表——每個 tool 要寫清楚讀哪些狀態、改哪些狀態、回什麼成功證據、結構化失敗長什麼樣、冪等性與重複風險、確認／權限／版本要求，並明文禁止 `click_selector` / `execute_javascript` 這類泛用契約。將來設計 Site Tools 時可直接當 review 清單。

2026-09-03 的檢視只讀了 README、`skills/pedelec-integration/` 參考文件與 SDK 公開介面；`desktop/` 與 `extension/` 的實作未細看，也未實際執行。

### 目前不做

- 不接 SDK、不寫 tool schema、不改任何 P Roadmap。
- 不因本項提前拆 P3 Subphase。
- 決定前 `規格企劃.md` 維持原文；真的選了 B / C 再改上面那句衝突。
