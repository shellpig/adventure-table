# Adventure Table — 技術棧討論

> 本文件是 **技術方案提案／討論稿**，不是產品規格、也不是長期技術 SSOT。
>
> 用途：交給其他 AI、工程師或未來的自己審查目前預計採用的技術棧，確認是否有更合適、更簡單或風險更低的選擇。
>
> **討論完成後，正式拍板的技術決策應寫入 `開發設計方針.md`；本檔屆時刪除或停止維護。** 不要讓 `技術棧討論.md` 與 `開發設計方針.md` 同時成為技術真值來源。
>
> 如果本文件與 `規格企劃.md` 衝突，**以產品規格為準**。技術選型必須服從產品行為，而不是反過來要求產品配合技術。

最後更新：2026-08-29

---

## 一、專案前提

Adventure Table 是一個：

- 輕量、桌上跑團優先的 D&D 5e 2014 VTT。
- Built-in Content 先使用 SRD 5.1。
- Human 與 AI 共用同一套 Game State / Game Actions。
- 外部 AI 透過 MCP / Site Tools 正式加入桌內當 DM 或 Player。
- Server 是唯一真實狀態來源。
- 支援長期 Campaign、Session、Character、Combat、Timeline、Snapshot 等持久狀態。
- 第一版是朋友間私人使用，不以大型 SaaS 或商業平台為目標。
- **網站本身不接 LLM API**：不保存模型 API key、不代付模型費用、不在 backend 自行呼叫 LLM。所有 AI 能力都來自使用者的外部 AI Session，透過 MCP / Site Tools 操作網站。
- **介面以繁體中文為主**；D&D / 系統關鍵字如 Action、Bonus Action、Reaction、Saving Throw、Concentration、Spell Slot、Initiative 等保留英文原文。

因此技術棧優先順序不是「可支援百萬使用者」，而是：

1. **規則層容易寫對、容易測。**
2. **Human UI 與 AI Tool 能共用同一套 backend logic。**
3. **Character / Campaign state 有可靠 persistence。**
4. **送出去的資料在單一位置依身分過濾**——因為 AI Player 會直接讀到 server 送的內容，這不是防人類作弊，是防資料真的進到 AI context。
5. **AI Join Token 可撤銷。**
6. **AI reconnect 後能重新取得真實狀態，不依賴舊 conversation。**
7. **Tactical Map 可以之後加入，但不能污染核心 Rules Engine。**
8. **部署與本機開發要簡單。**
9. **避免第一版過度架構。** 這是朋友私用的工具，不是多租戶 SaaS；遇到「要不要先做起來以防萬一」時，預設答案是不要。

---

## 二、第一輪 Review 後的目前提案

本版已吸收第一輪 Claude Opus review。以下仍是「目前推薦」，不是正式拍板。

| 領域 | 目前推薦 | 狀態 |
|---|---|---|
| Web Frontend | **React + TypeScript + Vite** | 高信心方向；取代原 Next.js 提案 |
| Routing | React Router 或同級 SPA router | 可替換 |
| UI Styling | Tailwind CSS | 建議，可替換 |
| Server State | TanStack Query | 建議，可替換 |
| Client UI State | Zustand | Optional；能不用就不用 |
| Tactical Map | **P5 前延後決定** | 不在 P0 鎖 PixiJS；屆時比較 Konva / PixiJS / Canvas 等 |
| Backend | **Python + FastAPI** | 高信心 |
| Data Validation | **Pydantic** | 高信心 |
| ORM | SQLAlchemy 2.x stable line | 建議 |
| DB Migration | Alembic | 建議 |
| Database | **PostgreSQL current stable line** | 高信心 |
| Realtime | WebSocket + state refetch/reconnect | 建議 |
| AI Integration | Official MCP Python SDK current stable line | 建議；MCP 隔離在 adapter layer |
| API Contract | **OpenAPI → generated TypeScript client/types** | 強制 pipeline，不手寫 mirror DTO |
| Reference Content | **Git files 是 truth，DB 是 derived** | 高信心 |
| Runtime / User Content | **DB 是 truth，可 export 成 file** | 高信心 |
| 資料可見性 | 單一 `project_for(actor)` 過濾點，三個出口共用 | P0 就要做 |
| Auth | Room / DM / Owner 憑證雜湊 + 可撤銷的 scoped AI token | 位置 P0 留好，完整流程 P2/P3 |
| 還原 | **不做 Undo 機制**；Human DM Direct Edit，AI DM 主持時不可還原 | 已定案 |
| Snapshot | 整包序列化 / restore，完整功能 P7 | P0 只要不堵死 |
| Character 資料模型 | Character / CharacterVersion / CharacterState 三層 | P0 就要做 |
| Entity ID | UUID，不用自增流水號 | P0 就要做 |
| Python Tests | pytest | 高信心 |
| Frontend Unit Tests | Vitest | 建議 |
| End-to-End Tests | Playwright | 高信心 |
| Deployment | Docker Compose | 高信心（第一版） |
| Reverse Proxy / Static Hosting | Caddy | 建議 |
| Asset Storage | Local filesystem → future S3-compatible | 建議 |
| i18n | 文案集中單一檔案，default `zh-TW`；不裝 i18n 框架 | 建議 |

### 版本號原則

本文件不把框架 minor / major 版本號當長期 architecture rule。

實作時使用：

> **當下受支援的 current stable / LTS line，除非相容性或部署環境有理由固定其他版本。**

例如 PostgreSQL、Vite、MCP SDK 的版本在真正開工時再核對，不讓討論稿裡的數字永久綁死專案。

---

## 三、目前推薦的整體架構

```text
Browser
│
├─ React / TypeScript / Vite SPA
│  ├─ Character Sheet / Builder
│  ├─ Room / Campaign UI
│  ├─ Exploration / Chat / Combat UI
│  └─ Tactical UI（P5 技術延後選）
│
│ generated OpenAPI client
│ HTTP + WebSocket
▼
FastAPI / Python
│
├─ Authentication / Credential Validation
├─ Actor Context / Scope
├─ Authorization
├─ Application Use Cases
│  ├─ Character / Builder
│  ├─ Rules Engine
│  ├─ Combat Engine
│  ├─ Campaign / Session
│  ├─ GameAction / GameTransaction
│  ├─ Event / Reconnect
│  └─ Adventure Runtime
├─ project_for(actor) 資料過濾
├─ HTTP / WebSocket Adapter
├─ MCP Adapter
└─ Persistence
     │
     ▼
PostgreSQL
```

Reference content：

```text
Git versioned definition files
        │
        ▼
Validation / normalization
        │
        ▼
Idempotent import
        │
        ▼
PostgreSQL derived/indexed copy
```

External AI：

```text
ChatGPT / Claude / other external AI
                │
                │ MCP / Site Tools
                ▼
            MCP Adapter
                │
                ▼
        Same Application Use Cases
```

### 最重要的邊界

```text
Human Web UI ─┐
              ├─→ Application Use Cases → Domain / Rules / State
External AI ──┘
```

Human UI 與 MCP 不可以各自實作一套遊戲規則。

例如：

```text
Human 點 Attack
AI 呼叫 attack()
        ↓
同一個 Attack use case
        ↓
Actor / Scope
        ↓
Authorization
        ↓
Rules Validation
        ↓
GameTransaction
        ↓
Persist State
        ↓
Visibility-safe Result / Event
```

---

## 四、Frontend：React + TypeScript + Vite

### 4.1 React / TypeScript 為什麼保留

Adventure Table 是大量互動 UI：

- Character Sheet
- Character Builder
- Spell selection
- Inventory
- Player Cards
- Chat
- Dice / RollRequest
- Quick Combat
- Tactical Combat
- Map Editor
- DM Tools
- Adventure Import Review

React component model 適合這類重互動且元件重複度高的產品。

TypeScript 有助於：

- API contract 清楚。
- Character / Combat UI 少掉欄位拼錯。
- generated API client 有完整型別。
- AI coding agent 對 TS / React 支援成熟。

### 4.2 為什麼第一輪 review 後改用 Vite

原提案是 Next.js，但目前更推薦 Vite，理由：

- 產品沒有 SEO 需求。
- 高度 client-heavy，主要是登入後 Application。
- 真正 Game Server 已經是 FastAPI。
- 不需要 Server Components / Server Actions 承擔核心價值。
- Vite build 後可直接由 Caddy 提供靜態檔。
- production 少一個 Node server runtime。
- 可以更明確維持「Frontend 是 Client；FastAPI 是唯一 backend」。

Production 可簡化成：

```text
Caddy
├─ static React/Vite build
└─ /api + WebSocket proxy
        ↓
      FastAPI
```

這不是說 Next.js 不可用；只是目前需求下，Vite 的簡單性更有價值。

### 4.3 Frontend 不擁有 Game Truth

前端可以有 draft / local UI state，但以下不能只存在 client：

- HP
- Inventory
- Spell Slots
- Character Build
- Campaign State
- Combat State
- Formal Roll result
- committed Tactical position

Server 才是唯一 truth。

---

## 五、Frontend State

### 5.1 TanStack Query

目前建議管理 Server State：

- Character
- Campaign
- Session
- Combat
- Adventure
- Timeline

負責 fetch / cache / mutation / invalidate / refetch。

WebSocket 收到 state changed event 後，可以 invalidate 或針對安全 DTO 做 cache patch。

### 5.2 Zustand

只在 React local/context 明顯不夠用時使用。

適合：

- selected token
- map zoom / pan
- active Character Sheet tab
- right panel width
- builder current step
- local draft interaction

不適合承載 Server truth。

---

## 六、Backend：Python + FastAPI

### 6.1 Python 的真正優勢

第一輪 review 後，不再把「MCP 生態」當成 Python 獨有優勢；MCP 同樣有一級 TypeScript SDK。

目前保留 Python 的主要理由是：

- D&D 5e domain logic 可讀性與測試性。
- Pydantic 很適合大量 reference data / action / DTO validation。
- Character progression、numeric override、structural validation 等規則形狀很適合明確的 Python domain layer。
- pytest 適合大量 headless rules test。
- PDF / DOCX 等普通 document preprocessing 生態成熟。
- 使用者本身熟悉 Python。

典型 domain logic：

```text
calculate_proficiency_bonus()
validate_multiclass()
calculate_spell_slots()
resolve_attack()
apply_condition()
validate_feat_prerequisite()
resolve_rest()
resolve_reaction()
```

### 6.2 FastAPI 的角色

FastAPI 負責：

- HTTP API
- WebSocket endpoint
- Request / Response validation
- OpenAPI generation
- authentication entry point
- Human UI access
- MCP 與 backend service 的共同宿主／整合點之一

它不是 Rules Engine 本身。

推薦分層：

```text
HTTP / WebSocket / MCP Adapter
             ↓
      Actor Context
             ↓
       Authorization
             ↓
     Application Use Case
             ↓
    Domain / Rules Engine
             ↓
 GameTransaction / Repository
             ↓
         Persistence
```

---

## 七、資料可見性：單一過濾點

產品硬規則（規格企劃基線第 6 條）：

> Secret DC、DM Notes、Hidden Monster、其他角色的 private knowledge，不該看的 actor 根本不能收到。

**這條規則存在的理由是 AI，不是人類玩家。** 人類玩家不會去看封包；但 AI Player 收到什麼就直接讀到什麼，server 送過去的隱藏怪物就是進了它的 context。所以「不要送」是唯一有效的做法，Prompt 叮嚀沒有用。

### 7.1 做法

在**一個地方**依身分決定送什麼：

```text
完整 Domain Entity
        ↓
project_for(actor)
        ↓
可送出的 DTO
```

- DM 拿完整資料。
- Player / AI Player 拿過濾後的資料。

三個出口——HTTP response、WebSocket event、MCP tool result——都走同一個函式。不要各自實作一份。

### 7.2 不要做的事

第一輪 review 提過但**確認不做**，因為對朋友私用是過度設計：

- Policy Engine（OPA / Casbin 等）。權限規則就是 role × action 的小固定表，幾十行普通程式即可。
- 「entity 存在性必須在 SQL query 就排除，避免筆數對不上洩漏」的分層設計。單一過濾點已經涵蓋 AI 讀到資料的情況；人類玩家數筆數不是真實威脅。
- 掃描所有 route 回傳型別的 architecture test。

### 7.3 測試要求

保留最低限度的負向測試，因為這是規格硬規則：

- Player / AI Player 拿不到 DM-only 欄位。
- MCP tool result 與 WebSocket event 也套用同一過濾。

## 八、Authentication / Credential / AI Token

P2 / P3 才會完整出現 Room / Seat / AI join workflow。第一版不做帳號系統、不做 OAuth。

產品需要四種憑證：

```text
Room Password    進房
DM Key           取得 DM role
Owner Key        管理 Room / Campaign / Assets
AI Join Token    AI 加入某個 Seat
```

### 8.1 三條規則

1. **憑證不存明文。** Room Password / DM Key / Owner Key 存雜湊。
2. **AI Join Token 可撤銷。** 隨機字串、只存雜湊，帶 seat / session scope 與 `revoked_at`。Human Take Back Control 後舊 token 立即失效；Session End 撤銷該 session 所有 AI token。
3. **每次 formal action 都檢查撤銷狀態。** MCP 是長連線，把授權結果快取住會造成「Take Back Control 之後 AI 還能繼續操作」。這是這一塊最可能出現的 bug。

`expires_at` 不需要另外設計——session-scoped 撤銷已經涵蓋。

> 實務理由：AI Join Token 一定會被貼進使用者的 AI 對話紀錄裡。所以它必須短命、單一 Seat、可撤銷。

### 8.2 憑證有效 ≠ 動作被允許

Token 驗證通過後仍要檢查 role / seat scope。AI 永遠拿不到 Owner Key。

第一版到此為止，不做 IAM、不做 refresh token、不做權限管理後台。

## 九、還原：不做 Undo 機制

**產品規格已於本輪修改**（見 `規格企劃.md` 第九章）：第一版不做 Undo Transaction / Revert Transaction。

還原的做法是 **Human DM 直接改資料**：

- 血扣錯了就把 HP 加回去。
- 法術位、道具數量、Action / Bonus / Reaction 已使用狀態、Death Save 計數、Initiative、Token position，都是 DM 本來就能改的欄位，改回去即可。
- Player 未 Confirm 的 draft 仍可 Cancel，那不是 Undo。
- **AI DM 主持時沒有還原功能**，任何人都不行。

### 9.1 這對後端的意義

原本「每個 write path 都要可逆、要記錄 before/after、要能生成反向 operation」的要求**取消**。

剩下的要求只有一般水準：

- Domain write 集中在 repository / service，不要散落各處的裸 SQL update。這是為了可讀性與可測性，不是為了 revert。
- DM Direct Edit 本身是一筆正式改動，寫進 Timeline，不偷改歷史。

### 9.2 Snapshot 保留

Snapshot 是整包 dump，不要求寫入路徑可逆，成本低很多，因此保留。

```text
Automatic：Session Start / Scene Change / Combat Start / Combat End / Session End
Manual：DM Save Point
Restore：Active State 回到該時點，後續 history 標為 archived
```

P0 只要確保 Campaign 相關狀態可以被整包序列化與還原，不需要做 Restore UI。

**小錯改資料，大錯回存檔。** 這兩層之間不需要第三種機制。

## 十、Validation：Pydantic

Pydantic 預計作為 Python boundary validation 核心。

例如：

```text
CharacterData
SpellDefinition
ItemDefinition
MonsterDefinition
AttackAction
CastSpellAction
RollRequest
GameTransactionInput
AdventureImportDraft
```

使用場景：

```text
Reference definition files
        ↓
Pydantic validation
        ↓
合法 normalized data
```

以及：

```text
MCP / HTTP input
      ↓
Pydantic
      ↓
Actor / Authorization / Rules
```

型別 validation 不等於 Structural Rule validation。像 Multiclass prerequisite、Subclass timing 等仍屬 domain rule。

---

## 十一、Database：PostgreSQL

Adventure Table 有強 relational structure：

```text
Room
Campaign
Session
Seat
Character
CharacterVersion
Combat
Combatant
GameTransaction
TimelineEvent
RollRequest
Item
Adventure
Scene
NPC
```

再加上 Transaction、Concurrent mutation、Referential integrity、Version history、Snapshot、Restore、Permission、Archive lifecycle，因此 PostgreSQL 比 document DB 更自然。

### 11.1 PostgreSQL + JSONB

不代表所有資料都要正規化成數百 table。

Relational core 傾向：

- Room
- Campaign
- Session
- Character
- Seat
- GameTransaction
- Timeline
- Combat
- credential / token identity

JSONB 適合：

- SRD / custom definition 本體（spell、monster、feature 的機制與敘述）——形狀高度異質，且 Git file 才是真值，DB 只是查詢副本
- Snapshot payload
- Imported provenance metadata
- Rare custom rule extension

**但 Character build 的選擇結果要用 relational 明細表**，不要塞 JSON：角色選了哪些 feature、哪些法術、ASI 怎麼分配、加職順序為何——P1 Builder 要驗證與重算 progression，塞成 JSON 就得在應用層手刻一堆比對邏輯。

一句判準：

> **derived / immutable / 異質 → JSONB；需要 FK、需要驗證、需要跨實體查詢 → relational。**

### 11.2 避免兩個極端

不要：

```text
每個 feature property 都拆成 table
```

也不要：

```text
整個 Campaign 塞一個超大 JSON blob
```

P0 開發設計時需要更精確定 relational / JSONB boundary。

---

## 十二、ORM / Migration

目前建議：

- SQLAlchemy 2.x stable line
- Alembic

原因：

- Python ecosystem 成熟。
- transaction boundary 清楚。
- migration 可追蹤。
- pytest integration 容易。

具體 table schema 到 P0 開發設計時再決定，本討論稿不鎖死。

---

## 十三、Reference Content 與 Runtime Content 的 Source of Truth

第一輪 review 後，這一點改成明確雙線，而不是模糊寫 Hybrid。

### 13.1 靜態 SRD / Custom Definition

```text
Git file = Source of Truth
DB = derived / indexed copy
```

例如：

```text
data/
├─ srd5.1/
│  ├─ races.json
│  ├─ classes.json
│  ├─ subclasses.json
│  ├─ backgrounds.json
│  ├─ feats.json
│  ├─ skills.json
│  ├─ spells.json
│  ├─ equipment.json
│  ├─ monsters.json
│  ├─ conditions.json
│  ├─ languages.json
│  └─ ...
└─ custom/
```

流程：

```text
File definitions
↓
Validate / Normalize
↓
Idempotent Import
↓
DB derived copy
```

要求：

- Import 必須 idempotent。
- Reference row 不靠 production DB 手工修改。
- 修改 reference content 應回到 Git file。
- DB 可建立索引、FK target、查詢最佳化，但不是原始真值。
- provenance / source version 要可追蹤。

### 13.2 使用者／跑團 Runtime Content

以下相反：

```text
DB = Source of Truth
```

例如：

- Quick Enemy
- Runtime NPC
- Runtime Scene
- Runtime Item
- Campaign Fact
- Save as Template
- Character current state
- Campaign current state

需要可攜時：

```text
DB
→ Export
→ Portable file/package
```

### 13.3 P0 SRD 導入

P0 仍應完成主要 SRD 5.1 reference data 導入，而不是只塞少量 fixture。

但：

> **資料已導入 ≠ mechanics 全部在 P0 自動化。**

Fireball definition 可 P0 完整存在；Tactical AoE geometry 到 P5 才做。

---

## 十四、MCP

### 14.1 MCP 是正式 Adapter

```text
Human HTTP/WebSocket ─┐
                     ├─→ Same Application Services
External AI / MCP ───┘
```

MCP 不是第二個 backend。

### 14.2 Python 不是因為 MCP 才選

MCP 有成熟的一級 TypeScript SDK，因此「要做 MCP」本身不是選 Python 的理由。

目前若 backend 採 Python，使用 Official MCP Python SDK current stable line 即可。

### 14.3 MCP types 不污染 Domain

```text
MCP protocol types
      ↓ adapter
Application DTO / Use Case
      ↓
Domain
```

Rules Engine 不依賴 MCP-specific object。

### 14.4 網站沒有 LLM

MCP Adapter 提供 tool / resource / event access；它不代表 backend 有模型。

例如 Adventure Import：

```text
External AI
→ 讀取 source / extracted text
→ 呼叫 importer MCP tools
→ 建 Draft / update Draft / answer warning
→ finalize
```

backend 可以做普通 document preprocessing（例如 PDF / DOCX text extraction），但不自行呼叫 LLM parse。

第一版沒有理由先建立獨立「LLM importer worker」。

---

## 十五、API Contract：OpenAPI → TypeScript 是強制 Pipeline

Python + TypeScript 雙語言的最大長期風險之一是 schema drift。

因此由「建議」升級為實作契約方向：

```text
API DTO / Pydantic
↓
FastAPI OpenAPI
↓
Code Generation
↓
Generated TypeScript client / types
↓
React Frontend
```

**禁止為同一 API DTO 長期手寫另一份 TypeScript mirror interface。**

Domain model 不必等於 API DTO：

```text
Domain Model
→ Visibility-safe API DTO
→ OpenAPI
→ Generated TS
```

改 backend DTO 後重跑 codegen 即可，不需要另外做 CI drift 檢查——一個人開發，忘了重跑當場就會編譯失敗。

精確 codegen 工具到開發設計時再選。

---

## 十六、Realtime / Durable Event / Reconnect

需要 realtime 的場景：

- Chat
- RollRequest
- PendingAction
- Turn changed
- HP changed
- Combat event
- ReactionRequest
- AI / Human controller changed
- Session state changed

WebSocket 是 delivery / invalidation mechanism，不是 truth。

```text
GameAction
↓
Transaction commit
↓
Persistent state / durable event
↓
WebSocket notification
```

掉線後 client / AI 必須能重新 fetch 真實狀態。

### 16.1 Durable event 先用 PostgreSQL

規格要求 AI 可以：

```text
get_pending_events()
wait_for_event(timeout)
```

第一版朋友團規模下，先讓 durable pending event 存 PostgreSQL。

WebSocket / MCP wait 只是 delivery mechanism。

Server restart 不應讓待處理 Reaction / Roll 等重要事件消失。

`wait_for_event(timeout)` 目前傾向**直接 polling**：每 1–2 秒查一次有索引的 pending event 表。朋友團規模下成本可忽略，而 PostgreSQL 的 LISTEN/NOTIFY 在連線池下行為微妙、斷線期間不補發、payload 有大小上限——省下的延遲不值得那些失敗模式。

真的遇到 scale / latency 問題，再評估 Redis / NATS / RabbitMQ。

---

## 十七、i18n / UI Language

產品已定：繁體中文為主要介面，D&D / 系統關鍵字保留英文。

**不要裝 i18n 框架。** 第一版沒有第二語言，i18next / react-intl 這類套件買到的唯一好處是「字串不散落在 component 裡」，而那件事用一個檔案就能做到：

```text
messages.ts   →  扁平的 key → 中文字串
t(key, params) →  一個 helper function
```

約數十行。真的出現第二語言時再換框架。

架構上唯一重要的一條：

> **Backend 回傳錯誤代碼，不回傳中文字串。** 所有顯示文案歸前端所有。

否則語言邊界從第一天就破了，之後要改文案得翻兩個 repo。

## 十八、Tactical Map 技術延後到 P5 前決定

原提案直接選 PixiJS，第一輪 review 後改成 **Deferred Decision**。

原因：

- Tactical Combat 是 P5。
- P0～P4 尚未提供足夠真實互動資料來判斷 renderer / editor library。
- 本產品不是即時動作遊戲；高 FPS sprite throughput 未必是主要需求。
- Map Editor 可能更重視 selection、handles、drag、hit detection、shape editing。

P4 Quick Combat 完成後，再依實際需求比較：

- Konva
- PixiJS
- Native Canvas
- SVG / hybrid
- 其他當時成熟方案

評估項目：

```text
Grid
Token drag/select
Wall / Door editor
Terrain paint
Path preview
AoE preview
Zoom / Pan
Touch
Hit detection
Selection handles
Performance
React integration
```

不論最後選哪個 library：

> Rules Engine 不知道 renderer library 存在；client movement draft 也不是 server true position。

---

## 十九、Testing Strategy

### 19.1 pytest

大量 headless tests：

- proficiency bonus
- skill / save calculation
- multiclass prerequisite
- class progression
- subclass timing
- ASI / Feat
- spell access
- multiclass spell slots
- equipment calculation
- attack resolve
- condition
- death save
- concentration
- reaction
- transaction
- authorization
- visibility projection
- token revocation
- snapshot / rollback compatibility

### 19.2 Reference Data Validation

每次 reference data change 應跑：

```text
schema validation
reference validation
duplicate ID validation
class progression validation
spell source validation
item reference validation
monster action validation
idempotent import validation
```

資料錯誤 fail fast，不靜默忽略。

### 19.3 Frontend

Vitest：component / utility / state presentation。

Playwright：真正 workflow。

P1 重要案例：

```text
Open Character Workshop
↓
Create Human
↓
Fighter 5
↓
Multiclass Wizard
↓
Wizard 5
↓
Review
↓
Confirm
↓
Save
↓
Reload
↓
Character remains identical
```

### 19.4 可見性負向測試

規模不用大，但這幾條要有，因為它們對應產品硬規則：

```text
Player / AI Player 拿不到 DM-only 欄位
MCP tool result 與 WebSocket event 也套用同一過濾
已撤銷的 AI Token 被拒絕
```

---

## 二十、Deployment

目前第一版推薦：

```text
Docker Compose
│
├─ Caddy
│  ├─ static React/Vite build
│  └─ reverse proxy / WebSocket
│
├─ FastAPI + MCP
│
└─ PostgreSQL
```

這比原本 Next.js runtime + FastAPI + PostgreSQL 再少一層 server runtime。

第一版不要主動加入：

- Kubernetes
- Microservices
- Kafka
- Redis Cluster
- Service Mesh
- 大型 API Gateway platform

朋友私用沒有理由先承擔這些成本。

---

## 二十一、Asset Storage

前期：local filesystem。

例如：

```text
assets/
uploads/
maps/
adventure_sources/
```

DB 保存 metadata / ownership / reference。

未來需要再抽換 S3-compatible storage。

Storage abstraction 可以保留，但不要 P0 就造完整 object-storage platform。

---

## 二十二、Repository 結構暫案

```text
adventure-table/
│
├─ apps/
│  ├─ web/                  # React / TypeScript / Vite
│  └─ server/               # FastAPI / Python
│
├─ data/
│  ├─ srd5.1/
│  └─ custom/
│
├─ tests/
│
├─ docker-compose.yml
│
├─ AGENTS.md
├─ PROJECT_BRIEF.md
├─ 規格企劃.md
├─ 技術棧討論.md             # 暫時 review 文件；拍板後內容編入開發設計方針
├─ 實作規格書.md             # Phase 規劃時建立／補充
├─ 開發設計方針.md           # 正式 technical SSOT
└─ 測試指南.md               # 操作驗收
```

是否需要 `packages/`、monorepo tool、generated client 位置等，到 P0 開發設計時決定。

---

## 二十三、P0 的要求：分成「要做」與「只是留後路」

P0：Character Core + SRD / Rules Foundation。
P1：Character Builder Complete。

前一版把十二條要求平鋪成一張清單，讀起來像十二件要蓋的東西，有把 P0 做成純架構工程、遲遲交不出一張角色卡的風險。分成兩類：

### 23.1 P0 就要實作

| 項目 | 說明 |
|---|---|
| **Character 三層資料模型** | `Character`（身分：誰、屬於哪個 Campaign、active/inactive/retired/dead）／ `CharacterVersion`（每次 Level Up 凍結一份，寫入後不再修改）／ `CharacterState`（HP、Temp HP、Slots、Hit Dice、Conditions 等一直在變的東西）。規格要求「保留完整 immutable previous version，不是 diff」「Character Version 永不刪」，做成單張可就地更新的表，P1 等於重做 |
| **Multiclass 能表示** | `character_level` 與 `class_levels` 分離、保存加職順序、Feature 標記來源、Spell 標記 source class / feature、Spell Slot 與 Spell Access 分離。P0 可以只用 fixture 建，但模型要撐得住 `Fighter 5 / Wizard 5` |
| **SRD 5.1 主要 reference data 導入** | Git file 是真值 → 驗證 → idempotent import 進 DB |
| **單一資料過濾點** | 送出去的資料經過 `project_for(actor)`，三個出口共用 |
| **Domain write 集中在 repository / service** | 不散落裸 SQL update。為了可讀可測，不是為了 revert |

### 23.2 只要不堵死，P0 不必實作

| 項目 | P0 要留的後路 |
|---|---|
| Auth / AI Join Token（P2/P3） | backend 有 actor context 的位置，不要把 role 寫死在 route 裡 |
| Snapshot / Restore（P7） | Campaign 相關狀態可以被整包序列化 |
| Export / Import（P7） | **domain entity 用 UUID，不要用自增流水號**。規格要求 Import 做 ID remapping、Import → New copy；跨實例匯入時流水號會撞。P0 加是零成本，事後改是全表遷移 |
| Character Workshop（P2） | **不要把 Character 的 `campaign_id` 設成 NOT NULL**。規格允許「未加入 Room 前也能創角色」，之後才進 Campaign Roster |
| 未來規則版本 | **reference data 帶 `ruleset` 標記**（目前一律 `dnd5e-2014`）。Campaign Rules 已有 Ruleset 欄位；將來若要加別版，事後補是整批遷移 |
| Tactical geometry（P5） | Quick Combat 不依賴任何座標；Combat Engine 不預設 grid 存在 |
| i18n | 文案集中一個檔案，backend 回錯誤代碼 |

### 23.3 P1 才要求

從 UI 正式完成合法 progression，並通過驗證案例：從空白建出 `Human Fighter 5 / Wizard 5（Character Level 10）`，不直接改底層 JSON。

## 二十四、目前刻意不加入的技術

### Redis / NATS / RabbitMQ

目前 PostgreSQL + WebSocket / wait mechanism 足夠。

只有真遇到 pub/sub、cache、latency 或 event throughput 問題才評估。

### Kafka

完全過度。

### Elasticsearch / OpenSearch

初期 PostgreSQL / application search 足夠。

### Vector Database

不要因為「AI」就先架 vector DB。

初期使用：

- structured lookup
- SQL filtering
- text search

需求明確後才評估 pgvector 或獨立 store。

### Microservices

第一版採 modular monolith：

```text
一個 Python backend deployment
內部分清楚 modules / layers
```

不先拆 character-service / combat-service / ai-service。

### Undo / Revert Transaction

還原改用 Human DM Direct Edit，AI DM 主持時不可還原。因此不做 transaction 反向操作、不要求每個 write path 可逆。

### Policy Engine

OPA / Casbin 等。權限規則是 role × action 的小固定表，普通程式即可。

### i18n Framework

第一版沒有第二語言，文案集中一個檔案就夠。

### Backend LLM Worker

**不建立。** 網站不接 LLM API。

普通 PDF / DOCX extraction 如未來有阻塞或資源需求，可以再評估一般 background worker；那與 AI model worker 是兩件事。

---

## 二十五、目前最大技術風險

### 25.1 秘密資料進到 AI context

如果每個 endpoint / MCP tool / WebSocket handler 自己過濾，遲早有一個忘記，而 AI Player 收到什麼就直接讀到什麼。

對策：單一 `project_for(actor)` 過濾點，三個出口共用。

### 25.2 Character 做成單張可就地更新的表

P1 要保留每次 Level Up 的完整舊版本。P0 若只用一張表，P1 所有讀寫角色的程式都要重寫。

對策：P0 就分 `Character` / `CharacterVersion` / `CharacterState`。

### 25.3 AI Token 授權結果被快取

MCP 是長連線。Human Take Back Control 後若不重新檢查撤銷狀態，AI 還能繼續操作角色。

對策：每次 formal action 都查 `revoked_at`。

### 25.4 Rules Engine 資料模型過早寫死

例如假設單一 class、單一 spell slot pool、spell 只有單一來源，P1 Multiclass 立刻重構。

### 25.5 Reference Data 與 Rules Logic 混在一起

不要到處寫：

```python
if class_name == "fighter" and level == 5:
    add_extra_attack()
```

需分清 Content Definition、Structural Rule、Calculation Engine。

### 25.6 Frontend 變第二個 Game State

Client local mutation 不能凌駕 Server truth。

### 25.7 WebSocket 被當 Event Store

Realtime 連線可能掉，重要狀態必須能從 persistence 重建。

### 25.8 AI Adapter 污染 Domain

Rules Engine 不應出現：

```text
if controller == chatgpt
```

Human / AI 差異只在 authentication、adapter、connection、context delivery；Role / gameplay permission 共用。

### 25.9 Adventure Importer 把外部 AI Output 當 Truth

Importer 仍必須走 Draft / Validation / Review / Confirm。

### 25.10 Tactical renderer 過早定案

P5 前資訊不足，不應為 Pixi / Konva 提前扭曲資料與 component 架構。

---

## 二十六、目前信心分級

### 高信心

- React + TypeScript frontend。
- Vite SPA 比 Next.js 更符合目前產品形狀。
- Python + FastAPI backend。
- PostgreSQL。
- Pydantic。
- Modular monolith。
- Human UI / MCP 共用 Application Use Case。
- 單一 `project_for(actor)` 資料過濾點。
- Character / CharacterVersion / CharacterState 三層。
- Entity ID 用 UUID。
- Reference definition：Git file truth / DB derived。
- Runtime content：DB truth。
- OpenAPI → TypeScript codegen。
- 不做 Undo 機制，還原走 DM Direct Edit。
- pytest / Playwright。
- Docker Compose 第一版。

### 中信心／仍可調

- SQLAlchemy / Alembic。
- TanStack Query。
- Tailwind。
- Caddy。
- local asset storage 的抽象方式。
- SRD definition 要正規化到什麼程度（見第二十七章 B）。

### 延後決定

- Tactical Map renderer/editor library。
- Redis / dedicated message broker。
- Vector search。
- Object storage provider。

---

## 二十七、已收斂的決定與剩餘問題

### 27.1 已收斂，不需要再 review

| 題目 | 結論 |
|---|---|
| Next.js vs Vite | Vite。沒有 SEO 需求、全站需登入，Next.js 只是多養一個 Node runtime |
| SRD 資料 file 還是 DB 為真值 | 靜態定義 file 為真值、DB derived；runtime / 使用者建立的內容 DB 為真值 |
| Python vs TypeScript backend | 維持 Python。規則引擎是最難的部分且完全在 backend；代價是 OpenAPI codegen 必須是強制流程 |
| 要不要 Policy Engine | 不要 |
| 資料可見性怎麼做 | 單一 `project_for(actor)` 過濾點；不做查詢層／投影層分層設計 |
| Undo | 不做。改用 Human DM Direct Edit；AI DM 主持時不可還原 |
| JSONB 邊界 | derived / immutable / 異質 → JSONB；需要 FK、驗證、跨實體查詢 → relational。Character build 選擇結果走 relational |
| i18n | 不裝框架，文案集中一檔；backend 回錯誤代碼 |
| Tactical renderer | 延後到 P5 準備時再選 |
| 晚期功能對 P0 的反向要求 | 見第二十三章，已分成「要做」與「留後路」兩類 |

### 27.2 仍值得下一輪挑戰的問題

只剩三個真正還沒有答案的：

**A. `wait_for_event(timeout)` 用什麼實作？**

目前傾向直接 polling（1–2 秒查一次有索引的 pending event 表），理由是朋友團規模下成本可忽略，而 PostgreSQL 的 LISTEN/NOTIFY 在連線池下行為微妙、斷線不補發、payload 有上限。請確認 polling 是否有明顯缺點。

**B. SRD 5.1 的正規化程度該到哪裡？**

Spell / Monster / Feature 的機制敘述有多少要拆成結構化欄位、多少維持文字 + DM 裁定？拆太細 P0 做不完，拆太粗 P4 Quick Combat 自動化會卡住。這是 P0 最可能失控的地方。

**C. 這份文件裡還有沒有殘留的過度設計？**

本專案是朋友私用的工具。若某項要求的理由是「將來如果規模變大」，直接指出並建議刪除。

## 二十八、目前暫定結論

```text
Frontend
React + TypeScript + Vite
React Router（或同級 SPA router）
TanStack Query
Tailwind
文案集中單一 messages 檔，不裝 i18n 框架

Backend
Python + FastAPI + Pydantic
SQLAlchemy + Alembic
Modular Monolith

Database
PostgreSQL current stable line
Entity ID 用 UUID
Character / CharacterVersion / CharacterState 三層
SRD definition 存 JSONB，Character build 選擇走 relational

Architecture
Server = Source of Truth
Human UI 與 MCP 共用同一 Application Use Case
單一 project_for(actor) 資料過濾點
Domain write 集中在 repository / service
Backend 回錯誤代碼，不回顯示文案

AI
External AI only，無 backend LLM API
MCP Adapter，Official MCP Python SDK current stable line
AI Join Token 可撤銷，每次 formal action 檢查

API Contract
FastAPI OpenAPI → generated TypeScript client/types

Reference Data
Git versioned SRD / custom definition files = truth
→ validation → idempotent DB import
Runtime / 使用者建立的內容：PostgreSQL = truth

還原
不做 Undo Transaction
Human DM Direct Edit；AI DM 主持時不可還原
Snapshot 保留（P7 做完整 Restore）

Realtime
WebSocket 作為通知；durable pending event 存 PostgreSQL
wait_for_event 傾向 polling

Tests
pytest / Vitest / Playwright
含資料可見性負向測試

Tactical
Renderer 延後到 P5 準備時再選

Deployment
Docker Compose：Caddy + static Vite build / FastAPI / PostgreSQL
```

### 這一版刻意砍掉的東西

第一輪 review 提出、但確認**對朋友私用的工具是過度設計**，不做：

- Policy Engine（OPA / Casbin）
- 查詢層 vs 投影層的雙層可見性設計
- 掃描所有 route 回傳型別的 architecture test
- Undo / Revert Transaction 機制與「每個 write path 必須可逆」的要求
- OpenAPI codegen 的 CI drift 檢查（codegen 保留，CI 強制檢查不做）
- i18n 框架
- AI Token 的 `expires_at`（session-scoped 撤銷已涵蓋）

判準：**如果某項要求的理由是「將來規模變大會需要」，就不做。**

這份結論仍是討論結果，不是正式 technical SSOT。拍板後編入 `開發設計方針.md`，本檔停止維護。

## 二十九、審查回覆建議格式

下一個 reviewer 請聚焦第二十七章的三個剩餘問題，並回答：

```text
1. 我同意哪些決策
2. 我反對哪些決策，以及具體替代方案
3. 第二十七章的三個問題（polling / SRD 正規化程度 / 殘留過度設計）
4. P0 的「要做」清單有沒有漏
5. P0 的「留後路」清單有沒有多餘的
6. 有沒有任何一項的理由是「將來規模變大」——那就該刪
```

**請不要提出以大型 SaaS 為前提的建議。** 這是朋友私用的工具，使用者人數是個位數。
