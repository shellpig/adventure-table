# Adventure Table — 技術棧討論

> 本文件是 **技術方案提案／討論稿**，不是產品規格硬約束。
>
> 用途：交給其他 AI、工程師或未來的自己審查目前預計採用的技術棧，確認是否有更合適、更簡單或風險更低的選擇。
>
> 如果本文件與 `規格企劃.md` 衝突，**以產品規格為準**。技術選型必須服從產品行為，而不是反過來要求產品配合技術。

最後更新：2026-08-29

---

## 一、專案前提

Adventure Table 是一個：

- 輕量、桌上跑團優先的 D&D 5e 2014 VTT。
- Built-in Content 先使用 SRD 5.1。
- Human 與 AI 共用同一套 Game State / Game Actions。
- 外部 AI 透過 MCP / Site Tools 正式加入桌內當 DM 或 Player。
- Server 是唯一真實狀態來源。
- 支援長期 Campaign、Session、Character、Combat、Timeline、Snapshot 等持久狀態。
- 第一版是朋友間私人使用，不以大型 SaaS 或商業平台為目標。

因此技術棧優先順序不是「可支援百萬使用者」，而是：

1. **規則層容易寫對、容易測。**
2. **Human UI 與 AI Tool 能共用同一套 backend logic。**
3. **Character / Combat / Campaign state 有可靠 transaction 與 persistence。**
4. **AI reconnect 後能重新取得真實狀態，不依賴舊 conversation。**
5. **Tactical Map 可以之後加入，但不能污染核心 Rules Engine。**
6. **部署與本機開發要簡單。**
7. **避免第一版過度架構。**

---

## 二、目前提案總覽

| 領域 | 預計技術 | 狀態 |
|---|---|---|
| Web Frontend | Next.js 16 + React + TypeScript | 建議 |
| UI Styling | Tailwind CSS | 建議 |
| Server State | TanStack Query | 建議，可替換 |
| Client UI State | Zustand | 建議，可替換 |
| Tactical Map | PixiJS 8 | 強烈建議 |
| Backend | Python + FastAPI | 強烈建議 |
| Data Validation | Pydantic | 強烈建議 |
| ORM | SQLAlchemy 2.x | 建議 |
| DB Migration | Alembic | 建議 |
| Database | PostgreSQL 18 | 強烈建議 |
| Realtime | WebSocket | 建議 |
| AI Integration | Official MCP Python SDK v2 | 強烈建議 |
| SRD Content | Version-controlled JSON + validation + DB import | 強烈建議 |
| Python Tests | pytest | 強烈建議 |
| Frontend Unit Tests | Vitest | 建議 |
| End-to-End Tests | Playwright | 強烈建議 |
| Deployment | Docker Compose | 強烈建議（第一版） |
| Reverse Proxy | Caddy | Optional |
| Asset Storage | Local filesystem → future S3-compatible | 建議 |

目前最核心、不希望輕易更換的四個方向：

> **Next.js / TypeScript + FastAPI / Python + PostgreSQL + PixiJS**

但即使這四個也不是產品規格。如果其他方案能明顯降低複雜度、提高可靠性，而且不破壞產品需求，可以更換。

---

# 三、預計整體架構

```text
Browser
│
├─ Next.js / React / TypeScript
│  ├─ Character Sheet / Builder
│  ├─ Room / Campaign UI
│  ├─ Exploration / Chat / Combat UI
│  └─ PixiJS Tactical Map
│
│ HTTP + WebSocket
▼
FastAPI / Python
│
├─ Character / Builder
├─ Rules Engine
├─ Combat Engine
├─ Campaign / Session
├─ GameAction / GameTransaction
├─ Permission / Visibility
├─ Event / Reconnect
├─ Adventure Runtime
└─ MCP Server
│
▼
PostgreSQL
```

最重要的邊界：

```text
Human Web UI ─┐
              ├─→ Application / Domain Services → Rules / State
MCP / AI ─────┘
```

Human UI 與 MCP 不可以各自實作一套遊戲規則。

例如：

```text
Human 點 Attack
AI 呼叫 attack()
        ↓
同一個 Attack use case
        ↓
Permission
        ↓
Rules Validation
        ↓
GameTransaction
        ↓
Persist State
        ↓
Event / Timeline
```

---

# 四、Frontend：Next.js + React + TypeScript

## 4.1 為什麼選 React / TypeScript

Adventure Table 前端不是一般內容網站，而是大量互動 UI：

- Character Sheet
- Character Builder
- Spell selection
- Inventory
- Player Cards
- Chat
- Dice / RollRequest
- Quick Combat
- Tactical Combat
- Map Editor
- DM Tools
- Adventure Import Review

React component model 很適合這類高度互動、重複 UI 元件多的產品。

TypeScript 則有助於：

- 前後端 contract 更明確。
- Combat / Character UI 少掉大量欄位拼錯。
- API schema 可由 OpenAPI 生成。
- AI coding agent 對 TS / React 的支援成熟。

## 4.2 為什麼選 Next.js，而不是純 Vite React

Next.js 的優點：

- Routing / layout / loading / error boundary 有成熟規範。
- TypeScript 整合成熟。
- 之後如有需要，可同一專案處理部分 web-facing server functionality。
- 生態、文件與 AI coding agent 支援非常充足。
- Self-hosting 沒問題。

但這個專案使用 Next.js **不是因為 SEO**。

甚至可以提出一個合理反對意見：

> Adventure Table 其實是 client-heavy application，純 React + Vite 可能更簡單。

這是目前歡迎其他 AI 評估的項目之一。

## 4.3 強制架構原則

**Next.js 不當 Game Server。**

不要把以下核心規則塞進 Next.js Server Actions：

- Attack resolve
- Roll resolve
- Character Build validation
- Combat state
- GameTransaction
- Permission
- Campaign persistence

即使 Next.js technically 可以做 backend，也不應因此形成第二套 backend logic。

---

# 五、Frontend State：TanStack Query + Zustand

## 5.1 TanStack Query 管 Server State

例如：

- Character
- Campaign
- Session
- Combat
- Adventure
- Timeline

這些資料的 truth 在 Server。

TanStack Query 負責：

- Fetch
- Cache
- Mutation
- Invalidate
- Refetch

WebSocket 收到 state changed event 後，可 invalidate / patch query cache。

## 5.2 Zustand 只管 UI State

例如：

- selected token
- map zoom / pan
- active Character Sheet tab
- right panel width
- builder current step
- local selection / draft interaction

以下資料**不能只存在 Zustand**：

- HP
- Inventory
- Spell Slots
- Combat State
- Character Build
- Campaign State

因為這些必須由 Server 當唯一 truth。

## 5.3 可替代性

TanStack Query / Zustand 都是 utility-level choice。

如果實作時發現：

- React built-in state 已足夠。
- 某一 library 帶來多餘複雜度。

可以減少甚至移除，不需回頭修改產品規格。

---

# 六、Backend：Python + FastAPI

## 6.1 為什麼 Python

這個專案的 backend 不是 CRUD 為主，而是大量 domain logic：

```text
calculate_proficiency_bonus()
validate_multiclass()
calculate_spell_slots()
resolve_attack()
apply_condition()
validate_feat_prerequisite()
resolve_rest()
resolve_reaction()
```

Python 很適合寫這種可讀性高、測試導向的規則邏輯。

另一個重要原因是 AI / MCP / importer 生態：

- MCP Python SDK
- Pydantic
- PDF / DOCX parser
- AI orchestration
- structured output
- data pipeline

Adventure Importer 與 AI DM 後續會大量受益。

## 6.2 為什麼 FastAPI

FastAPI 的角色：

- HTTP API
- WebSocket endpoint
- Request / Response validation
- OpenAPI generation
- Auth / permission entry point
- Human UI access
- MCP 與 backend service 的共用宿主之一

它不是 Rules Engine 本身。

推薦分層概念：

```text
API / MCP Adapter
       ↓
Application Use Case
       ↓
Domain / Rules Engine
       ↓
Repository / Persistence
```

這可以避免未來 Rules Engine 綁死 FastAPI。

## 6.3 為什麼不優先選 Node.js Backend

Node / TypeScript backend 當然可行，而且有「全棧同語言」優勢。

目前沒有優先選的原因：

- Rules / AI / importer 比 full-stack language uniformity 更重要。
- Python 比較適合預期的大量 AI / document processing 工作。
- Pydantic model 很適合 GameAction / Character / MCP input。
- 使用者本身熟悉 Python。

這仍然是可以被其他 AI 挑戰的選擇。

如果有人主張 TypeScript backend，應回答：

> 是否真的能降低整體複雜度，而且 MCP / AI / document importer 不會因此變更麻煩？

---

# 七、Validation：Pydantic

Pydantic 預計作為 Python boundary validation 的核心。

例如：

```text
CharacterData
SpellDefinition
ItemDefinition
MonsterDefinition
AttackAction
CastSpellAction
RollRequest
GameTransactionInput
AdventureImportDraft
```

使用場景：

```text
SRD JSON
   ↓
Pydantic validation
   ↓
合法 domain data
```

以及：

```text
MCP / HTTP input
   ↓
Pydantic
   ↓
Permission / Rules Engine
```

AI 傳入亂碼或型別錯誤時，不能進 Rules Engine 才爆炸。

---

# 八、Database：PostgreSQL

## 8.1 為什麼不用 MongoDB 當主要 DB

Adventure Table 有非常強的 relational structure：

```text
Room
Campaign
Session
Seat
Character
CharacterVersion
Combat
Combatant
GameTransaction
TimelineEvent
RollRequest
Item
Adventure
Scene
NPC
```

再加上：

- Transaction
- Concurrent mutation
- Referential integrity
- Version history
- Snapshot
- Restore
- Permission
- Archive lifecycle

因此 PostgreSQL 比 document DB 更自然。

## 8.2 PostgreSQL + JSONB

不代表所有資料都一定正規化成很多 table。

可採混合方式：

Relational core：

- Room
- Campaign
- Session
- Character
- Seat
- GameTransaction
- Timeline
- Combat

JSONB 適合：

- Custom content extensions
- Imported metadata
- AI provenance
- Partially structured feature data
- Rare / custom rule payload

這樣同時取得：

> relational integrity + extension flexibility

## 8.3 PostgreSQL 版本

目前預計使用 PostgreSQL 18 current stable line。

版本號是部署選擇，不是產品規格。若環境或 Docker image 需要改用仍受支援的 PostgreSQL 17，也不應視為規格變更。

---

# 九、ORM / Migration：SQLAlchemy + Alembic

預計：

- SQLAlchemy 2.x
- Alembic

主要理由：

- Python ecosystem 成熟。
- transaction boundary 清楚。
- migration 可追蹤。
- pytest integration 容易。

Migration 示例：

```text
001_core_character
002_character_version
003_room_campaign
004_session_seat
...
```

具體 table schema 要到實作 Phase 再決定，本文件不鎖死。

---

# 十、SRD 5.1 Data Architecture

## 10.1 P0 就導入正式 SRD 資料

P0 不應只塞幾隻測試怪物、幾個 spell。

預計直接建立正式的 SRD content layer，例如：

```text
data/
└─ srd5.1/
   ├─ races.json
   ├─ classes.json
   ├─ subclasses.json
   ├─ backgrounds.json
   ├─ feats.json
   ├─ skills.json
   ├─ spells.json
   ├─ equipment.json
   ├─ monsters.json
   ├─ conditions.json
   ├─ languages.json
   └─ ...
```

之後：

```text
SRD source data
↓
normalize / transform
↓
version-controlled JSON
↓
Pydantic validation
↓
Database seed / import
```

## 10.2 為什麼 JSON 也要留在 Git

如果所有 SRD data 只進 DB：

- 不好 review。
- 不好 diff。
- 不好讓 AI agent 修資料。
- DB 重建比較麻煩。

Versioned JSON 可以直接看到：

```diff
+ new spell
- wrong trait text
+ corrected equipment property
```

## 10.3 Custom Content

預期：

```text
data/
├─ srd5.1/
└─ custom/
```

後續使用者提供非 SRD 資料時逐步加入。

目前不做 Marketplace / publishing system。

## 10.4 重要原則

**資料完整導入 ≠ mechanics 全部在 P0 自動化。**

例如 Fireball 在 P0 可以已有完整 definition；但 Tactical Map 自動畫 20 ft radius 是 P5 才需要。

Goblin 在 P0 可以有完整 stat block；但 Initiative / Attack / HP / Condition resolve 是 Quick Combat Phase 才啟用。

---

# 十一、MCP：Official Python MCP SDK

## 11.1 MCP 是核心入口，不是外掛

Architecture：

```text
              Human HTTP/WebSocket
                      │
                      ▼
              Application Services
                      ▲
                      │
                 MCP Adapter
                      ▲
                      │
                    AI
```

不要：

```text
Human Backend
AI Backend
```

兩套平行邏輯。

## 11.2 MCP Tool 應呼叫同一 Use Case

例如：

```text
Web UI Attack button
        │
        ├────→ execute_attack()
        │
MCP attack tool
        │
        └────→ execute_attack()
```

同一個：

- permission
- validation
- transaction
- result

## 11.3 SDK

目前預計採用 Official MCP Python SDK v2 stable line。

但 MCP protocol / SDK 都仍屬相對快速演進的技術，應隔離在 adapter layer，不要讓 Rules Engine 依賴 MCP-specific types。

---

# 十二、Realtime：WebSocket

需要即時更新的場景：

- Chat
- RollRequest
- PendingAction
- Turn changed
- HP changed
- Combat event
- ReactionRequest
- AI / Human controller changed
- Session state changed

流程例：

```text
Player A attacks
↓
FastAPI receives GameAction
↓
transaction commit
↓
state updated
↓
WebSocket broadcasts event / invalidation signal
↓
DM + Players refresh relevant state
```

重要：

> **WebSocket message 不是 truth。**

Truth 仍是 committed server state。

Client disconnect / missed message 後，可以重新 fetch state。

---

# 十三、Durable Event / AI Reconnect

規格要求：

```text
get_pending_events()
wait_for_event(timeout)
```

以及 AI reconnect 後可以恢復 Pending Roll / Reaction / Current Turn。

第一版建議不要因為「需要 queue」就直接導入 Redis / Kafka / RabbitMQ。

Durable event 先存在 PostgreSQL。

概念：

```text
Persistent Event / Seat Event
↓
PostgreSQL
```

WebSocket / long polling / MCP wait 只是 delivery mechanism。

Server restart 後事件仍存在。

未來真的有 scale / latency 問題，再評估：

- Redis
- NATS
- RabbitMQ

目前不主動加入。

---

# 十四、Tactical Map：PixiJS

## 14.1 為什麼不直接 DOM

P5 Tactical Combat 會需要：

- Grid
- Pan / Zoom
- Tokens
- Walls
- Doors
- Terrain
- Path preview
- AoE preview
- Selection
- Free Drawing

大量 DOM / SVG 也可以做，但 canvas rendering library 更符合這類互動場景。

## 14.2 預計分工

```text
React
├─ Toolbar
├─ Combat Panel
├─ Character Panel
├─ Initiative
└─ Pixi Canvas
   ├─ Map Image
   ├─ Grid
   ├─ Terrain
   ├─ Walls
   ├─ Doors
   ├─ Tokens
   ├─ Path Preview
   └─ AoE Preview
```

React 管 application UI。

PixiJS 管 battlefield rendering / interaction。

Rules Engine 不知道 PixiJS 存在。

## 14.3 重要邊界

Pixi 產生：

```text
Movement Draft
```

但真正合法性：

```text
Server Movement Validation
```

Confirm 前不要把 client position 當 server truth。

---

# 十五、API Contract：OpenAPI → TypeScript

FastAPI / Pydantic 可以產 OpenAPI。

建議流程：

```text
Python models / API
↓
OpenAPI
↓
Generated TypeScript types / client
↓
Next.js
```

目標是避免：

```text
Python Character model
TypeScript Character interface
```

兩邊純手工維護最後 schema drift。

需要注意：

Domain model 不一定應該直接等同 API DTO。

可採：

```text
Domain Model
→ API DTO
→ OpenAPI
→ TS Client
```

避免 API contract 反向控制 domain architecture。

---

# 十六、Testing Strategy

這個專案規則很多，測試不是最後補，而是 architecture requirement。

## 16.1 pytest

Rules / domain tests：

- proficiency bonus
- skill / save calculation
- multiclass prerequisite
- class progression
- subclass timing
- ASI / Feat
- spell access
- multiclass spell slots
- equipment calculation
- attack resolve
- condition
- death save
- concentration
- reaction
- transaction
- permission
- snapshot / rollback

大量 headless test。

## 16.2 SRD Data Validation

每次 data change 都應跑：

```text
schema validation
reference validation
duplicate ID validation
class progression validation
spell source validation
item reference validation
monster action validation
```

資料錯誤應 fail fast，不要靜默忽略。

## 16.3 Vitest

Front-end component / utility tests。

尤其：

- Character Sheet calculation presentation
- Builder choice state
- map utility
- UI permissions

## 16.4 Playwright

真正的 UI workflow 驗證。

P1 重要案例：

```text
Open Character Workshop
↓
Create Human
↓
Fighter 5
↓
Multiclass Wizard
↓
Wizard 5
↓
Review
↓
Confirm
↓
Save
↓
Reload
↓
Character remains identical
```

而且要驗證：

- starting class differences
- multiclass prerequisite
- subclass timing
- ASI / Feat
- total level vs class level
- spellbook
- prepared spell
- spell slots

後續 Combat / AI 也應有 E2E。

---

# 十七、Deployment：Docker Compose

第一版預計：

```text
docker compose
├─ web
│  └─ Next.js
│
├─ server
│  └─ FastAPI + MCP
│
└─ postgres
```

Optional：

```text
Caddy
```

處理：

- HTTPS
- Reverse proxy
- WebSocket proxy

第一版不要主動導入：

- Kubernetes
- Microservices
- Kafka
- Redis Cluster
- Service Mesh
- API Gateway platform

朋友私用完全沒有理由先承擔這些成本。

---

# 十八、Asset Storage

前期：

```text
local filesystem
```

例如：

```text
assets/
uploads/
maps/
adventure_sources/
```

資料庫保存 metadata / path / ownership。

未來如果部署環境需要：

```text
S3-compatible object storage
```

可以轉：

- MinIO
- Cloudflare R2
- AWS S3
- 其他相容服務

這應透過 storage abstraction 隔離，但不要 P0 就實作複雜 object-storage platform。

---

# 十九、Repository 結構提案

初步可能：

```text
adventure-table/
│
├─ apps/
│  ├─ web/                  # Next.js / TypeScript
│  └─ server/               # FastAPI / Python
│
├─ data/
│  ├─ srd5.1/
│  └─ custom/
│
├─ tests/
│
├─ docker-compose.yml
│
├─ PROJECT_BRIEF.md
├─ 規格企劃.md
├─ 技術棧討論.md
├─ 實作規格書.md             # P0 規劃時建立／補充
├─ 開發設計方針.md           # 實際 implementation contract
└─ 測試指南.md               # 操作驗收
```

是否要 monorepo tool、`packages/`、共享 generated client 等，等 P0 開發設計時再決定。

---

# 二十、P0 / P1 對技術架構的要求

P0：Character Core + SRD / Rules Foundation。

P1：Character Builder Complete。

這兩個 Phase 高度相關，因此 P0 的底層 architecture 必須一開始就能支援 P1，不能因為 P0 尚未做完整 Builder 就把資料模型簡化成不可擴充形式。

P0 architecture 必須至少能表示：

```text
Human
Fighter 5 / Wizard 5
Character Level 10
```

即使 P0 的資料可能透過 fixture / developer import 建立。

也就是：

- Character 不可寫死單一 Class。
- 要能保存 class level progression order。
- character_level 與 class level 分離。
- Feature 要能標記 source。
- Spell 要能標記 source class / feature。
- Spell slot 與 spell access 分離。
- Build 與 Current State 分離。
- Character Version 結構要能演進。
- SRD data definition 與 runtime character state 分離。

P1 才要求從 UI 正式完成合法 progression。

---

# 二十一、目前刻意不加入的技術

第一版先不加入：

## Redis

目前 PostgreSQL + WebSocket 足夠。

加入條件：真的遇到 pub/sub、cache 或 event throughput 問題。

## Kafka

完全過度。

## RabbitMQ

目前 durable events 可直接用 DB。

## Elasticsearch / OpenSearch

Campaign history / Adventure retrieval 初期直接 PostgreSQL full text / application search 即可。

真的資料量大到需要時再加。

## Vector Database

AI context retrieval 可能未來需要 semantic search，但第一版不要因為「AI」兩字就先架 vector DB。

初期可以：

- structured lookup
- SQL filtering
- text search

需求明確後再評估 pgvector 或獨立 vector store。

## Microservices

第一版採 modular monolith。

建議：

```text
一個 Python backend deployment
內部分清楚 module
```

而不是：

```text
character-service
combat-service
campaign-service
ai-service
```

各自部署。

邊界可先做好，但不要先付 distributed systems 的成本。

---

# 二十二、目前最大技術風險

## 22.1 Rules Engine 資料模型過早寫死

最大風險之一。

例如一開始假設：

```text
Character has one class
Character has one spell slot pool
Every spell comes from one source
```

P1 Multiclass 很快就會逼整個 model 重寫。

所以 P0 即使功能較少，model 必須以已知 P1 規格為相容目標。

## 22.2 SRD Data 與 Rules Logic 混在一起

不要寫：

```python
if class_name == "fighter" and level == 5:
    add_extra_attack()
```

到處散落。

要清楚分：

```text
Content Definition
+ Structural Rule
+ Calculation Engine
```

哪些是資料、哪些是 5e engine invariant，需在 P0 詳細設計。

## 22.3 Frontend 變成第二個 Game State

如果 React / Zustand 自己修改 HP、slot、combat position，再晚點同步 Server，會非常危險。

Server truth 原則必須從一開始維持。

## 22.4 WebSocket 被當 Event Store

Realtime connection 可能掉線。

所有重要 state change 都要 commit 後可重建。

## 22.5 AI Adapter 污染 Domain Logic

Rules Engine 不應有：

```text
if controller == chatgpt
```

Human / AI 的差異只應在：

- authentication
- adapter
- connection
- context delivery

Role / gameplay permission 共用。

## 22.6 Adventure importer 把 AI output 當 truth

Importer 必須經 validation / Draft / Review / Confirm。

不能讓 LLM generated JSON 直接進正式 Adventure state。

## 22.7 Tactical Map 影響 Quick Combat abstraction

Combat Engine 必須先能在沒有 geometry 的 Quick Combat 運作。

P5 Tactical 只是增加 spatial resolver，不能把核心 Combat Engine 改成「所有戰鬥都必須有 coordinate」。

---

# 二十三、目前技術選型的信心分級

## 高信心

### Python backend

原因：Rules / AI / importer / testing 都吻合。

### PostgreSQL

原因：domain 關係與 transaction 明顯適合 relational DB。

### Pydantic

原因：structured game / AI input validation 非常適合。

### MCP adapter 與 Human UI 共用 backend use cases

這不是 library choice，而是核心 architecture requirement。

### SRD JSON version control

方便 validation、diff、AI agent 開發與 DB rebuild。

### pytest / Playwright

規則與 UI workflow 都非常需要高自動化。

## 中高信心

### Next.js

React / TypeScript 幾乎確定；Next.js vs Vite React 還值得比較一次。

### PixiJS

目前看非常符合 Tactical Map，但在 P5 前仍有重新評估空間。

### SQLAlchemy / Alembic

標準且穩定，但不是不可替換。

## 低綁定／容易替換

- Tailwind
- TanStack Query
- Zustand
- Caddy
- Asset storage provider
- minor library choices

---

# 二十四、希望其他 AI 特別審查的問題

請不要只回答「這套可以」。希望審查者實際挑戰以下項目。

## A. Next.js 是否真的比 Vite + React 更適合？

本產品沒有 SEO 需求，而且是高度 client-heavy application。

請比較：

- complexity
- self-hosting
- WebSocket client
- PixiJS integration
- API client generation
- AI agent coding DX

## B. Python + FastAPI 是否值得承擔雙語言 stack？

另一方案是 TypeScript full stack。

請評估：

- Rules Engine
- MCP
- AI importer
- testing
- schema sharing
- maintenance

是否 Python 的優勢足以抵銷 TS/Python 雙語言成本？

## C. PostgreSQL JSONB 的使用邊界

哪些資料應 relational，哪些可以 JSONB？

尤其：

- Character build
- Feature definition
- Spell definition
- Custom content
- Snapshot
- GameTransaction payload

請避免兩個極端：

- 所有東西正規化成數百 table。
- 所有東西都塞巨大 JSON blob。

## D. SRD reference data 是否應該 import 到 DB？

候選：

1. JSON → DB 全部 import。
2. Static definitions 直接從 versioned files 讀，runtime state 才 DB。
3. Hybrid。

目前提案偏向 Hybrid / DB import，但這一點值得深入討論。

## E. Durable event queue 只靠 PostgreSQL 是否合理？

使用規模是朋友團，不是大型 SaaS。

請評估：

- AI wait_for_event
- WebSocket
- reconnect
- pending reaction
- server restart

是否 PostgreSQL 已足夠？

## F. PixiJS vs Canvas / SVG / Konva / Phaser

請依需求比較：

- Grid
- Tokens
- path preview
- wall / door editor
- terrain
- AoE
- zoom / pan
- touch

本產品不是即時動作遊戲。

## G. Modular Monolith 是否適合

目前明確不想做 microservices。

請指出是否有任何模組真的值得從一開始獨立 process，例如 MCP server、document importer worker 等。

## H. 是否有任何 P0 就必須處理、但目前技術方案遺漏的風險？

尤其請檢查：

- Character multiclass data model
- ruleset versioning
- SRD provenance
- data migrations
- snapshots / character versions
- numeric override vs structural validation
- build vs current state

---

# 二十五、目前建議結論

如果現在直接開始 P0，暫定採：

```text
Frontend
Next.js 16
React
TypeScript
Tailwind

Backend
Python
FastAPI
Pydantic
SQLAlchemy 2.x
Alembic

Database
PostgreSQL 18

AI
Official MCP Python SDK v2

Data
Version-controlled SRD 5.1 JSON
+ validation
+ PostgreSQL persistence/import

Tests
pytest
Vitest
Playwright

Later Tactical UI
PixiJS 8

Deployment
Docker Compose
```

但開工前最值得再接受外部 AI review 的三個問題是：

1. **Next.js vs Vite + React。**
2. **SRD definition data 到底應該以 file 還是 DB 為主要 source of truth。**
3. **Python backend 是否比 TypeScript full-stack 明顯更適合本專案。**

其餘大方向目前看來風險相對低。

---

# 二十六、審查回覆建議格式

如果要讓另一個 AI review，可以要求它用以下格式回答：

```text
1. 我同意的選擇
2. 我反對的選擇
3. 我會替換的技術與原因
4. P0 最可能踩雷的架構點
5. P1 Multiclass Builder 對 P0 地基的要求是否完整
6. 是否有過度設計
7. 是否有設計不足
8. 最終推薦技術棧
```

希望審查重點是「這套架構是否適合 Adventure Table 的實際產品需求」，而不是單純比較各 framework 流行度。
