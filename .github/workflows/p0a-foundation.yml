# Deprecated by U01-A. This historical P1 closeout workflow mixed old
# persistence checks with the pre-isolation Playwright stack. Current browser
# regression is owned by P3 Full-Stack E2E; non-E2E contracts stay in their
# maintained regression workflows/tests.
name: P1 Full Regression (deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::notice::Deprecated historical workflow. Dispatch 'P3 Full-Stack E2E' for cumulative browser regression; use current non-E2E workflows/tests for backend and migration gates."
