# Deprecated by U01-A. The cumulative P3 Full-Stack E2E workflow owns the
# current Playwright regression path and uses the isolated adventure_table_e2e stack.
name: P2 Full-Stack E2E (deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::notice::Deprecated historical workflow. Dispatch 'P3 Full-Stack E2E' instead; it covers the cumulative browser suite on the U01-A isolated E2E stack."
