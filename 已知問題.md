# 已知問題

本檔記錄已確認、但決定暫不處理的問題。每一項都要寫清楚：症狀、根因（或已排除的假設）、影響範圍、暫時處置、以及重啟處理的條件。

已修好的問題不留在本檔，移除即可；歷史從 git log 追。

最後更新：2026-09-02

---

## KI-M01J-001 — M01-J 直創／逐級升等等價 E2E 不穩定

**狀態**：暫停執行（`test.fixme()`）
**發現**：2026-09-02，M01-J 批二（G6/G7 E2E）
**位置**：`apps/web/e2e/m01j-subclass-expansion.spec.ts`，測試名稱 `M01-J direct high-level create matches sequential level up`

### 症狀

同一條測試，同樣的程式碼，跑起來時好時壞。失敗時停在 Review：

```
invalid_subclass_choice_count
  path: draft_payload.choice_selections.m01-j:tce:rune-knight:rune-carver
  message: Rune Knight requires exactly 2 selections; got 0.
```

符文雕刻（Rune Carver）在 Level Up 路徑上沒有被自動填入。

### 根因

**這是測試 harness 的問題，不是產品缺陷。**

該 spec 用一套通用的「掃描所有空的下拉選單、選第一個可選項」邏輯來填必填項。這套邏輯在 Create 路徑穩定，在 Level Up 路徑不穩定：某些控制項第一次點擊後，草稿 revision 不會在逾時內遞增，掃描迴圈因而放棄該控制項並繼續，留下未填的必填選項。加入重試後仍未穩定。

同一份契約（J.8：直接高等級創角 == 逐級升等）已由後端整合測試覆蓋且穩定通過：

- `apps/server/tests/test_m01j_level_up_choice_guard.py`

該檔直接打真實 API 走完「開 Level Up 草稿 → 升到 3 級 → 選符文騎士 → 存入兩個符文 → Confirm」，並驗證邊界（不可抽掉先前選項、不可靜默清空、歷史等級選項仍凍結）。

### 影響範圍

- M01-J 的 J.8 驗收缺少**瀏覽器層**證據；後端層證據完整。
- 不影響使用者：產品的 Level Up 取得子職業流程本身正常。
- 同檔其他 13 條測試（12 個 PHB 職業的完整流程 + 四來源矩陣）不受影響，正常執行。

### 暫時處置

該測試標記 `test.fixme()`，**後續不執行**。標記處註明本條編號。

### 重啟條件

要恢復這條測試，需要先把填值方式從「掃描 + 選第一個」改成明確驅動——例如比照 `fillExactSpellBuckets`，用 `n / m` 計數器判斷完成條件，並對每個子職業選項指名選取而非取第一個可選項。在那之前不要只是重跑。

---

## KI-ENV-001 — Playwright 託管的 vite webServer 會在長跑中途消失

**狀態**：環境限制，已有替代做法
**發現**：2026-09-02
**根因修正**：2026-09-02（原記錄的根因與 TIME_WAIT 量測有誤，見下）
**影響**：本機 Windows 開發環境；CI（docker compose）未觀察到

### 症狀

`npx playwright test`（不指定檔案，由 `playwright.config.ts` 的 `webServer` 啟動 vite）跑到 3～4 分鐘會崩，出現 40～56 個失敗，其中絕大多數是：

```
net::ERR_CONNECTION_REFUSED at http://127.0.0.1:4173
```

第一個失敗通常是草稿 revision 逾時，之後全部是連線被拒。崩潰時的測試編號會變（觀察到 18、29、30），但經過時間固定在 3～4 分鐘。

### 根因

**問題在 Playwright 的 `webServer` 生命週期，不在 vite 本身。**

決定性對照：改用外部 wrapper 自行啟動 `npm run dev:e2e`，再以 `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173` 執行**完全相同的 72 個測試、同一次呼叫**（此時 config 會跳過 `webServer`，Playwright 不託管 dev server）：

```
71 passed / 1 skipped (4.0m)
```

同一支 vite 從頭活到尾，wrapper 每 5 秒取樣的資源曲線全程持平：

| 指標 | 起 | 迄 |
|---|---|---|
| handle count | 376 | 374（區間 374～387） |
| working set | 91 MB | 159 MB |
| threads | 87 | 82 |
| 自有 TCP 連線 | 1 | 2（峰值 22） |
| `4173` / `8000` health | 200 / 200 | 200 / 200 |

vite stderr 全程空白。**沒有任何資源累積或耗盡的跡象**，所以 vite 不是被自己拖垮的；由 Playwright 託管時才會中途消失。

**尚未定案**：Playwright 究竟為何收掉子行程——webServer stdout/stderr pipe、Windows job object / process-group kill，或 runner 內部的 lifecycle 判定，都還沒分辨。要定案需在 Playwright 託管模式下攔截 vite 的 exit code 與訊號來源。

已量測排除的假設：

| 假設 | 量測結果 |
|---|---|
| vite process 資源耗盡 | 外部託管跑滿 4.0 分鐘，handle / memory / thread / 連線全部持平 |
| 整機 TCP 連線數過高 | 兩個半套各自衝到約 4700 / 5010 條仍全綠；原記錄的 2700 只是路過的數字，不是門檻 |
| TCP 埠耗盡 | 峰值 `rport=4173` TimeWait 679、`rport=8000` TimeWait 117，遠低於 Windows 動態埠上限 16384 |
| 記憶體不足 | 全程 18 GB free |
| Chromium 行程洩漏 | 跑完殘留 0 |
| 後端掛掉或變慢 | 崩潰後 `8000 /ready` 仍為 200 |
| 某些測試本身壞掉 | 拆兩批 71 passed / 1 skipped；外部託管整套亦同，零失敗 |
| 分支的程式碼改動 | 將唯一的產品改動 stash 後跑對照組，舊程式碼產生相同崩潰 |
| M01-J 造成的回歸 | `git diff 7f3873d..HEAD -- apps/web` 只有兩個 e2e spec 檔，`apps/web/src` 零改動 |

**關於 M01-J**：M01-J 期間才第一次觀察到本問題，但 M01-J 沒有動任何 frontend 程式碼。最後一次整套一次跑成功的紀錄是 M02-H closeout 的 37 passed；其後套件長到 72，跑得夠久才踩到這個 webServer 缺陷。M01-J 是壓垮的最後一根稻草，不是來源。

**三處原記錄已作廢**：原文寫「連線爬到 2700 把 vite 灌爆」、「TIME_WAIT 僅 7，排除埠耗盡」，以及一度改寫成的「單一 vite process 撐不過 3 分鐘」。第一項被上表推翻；第二項是量測範圍抓錯，實際是幾百（埠耗盡結論不變但理由不同）；第三項被外部託管的 4.0 分鐘全綠推翻。

### 暫時處置

**自己啟動 dev server，不要讓 Playwright 託管。** 先清空 `character_states` / `character_versions` / `characters` / `character_build_drafts` 並重新 seed fixture，再開一個終端機跑：

```
cd apps/web && npm run dev:e2e
```

等 `http://127.0.0.1:4173` 回 200 後，另一個終端機跑整套：

```
cd apps/web && PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173 npx playwright test
```

`playwright.config.ts` 在 `PLAYWRIGHT_BASE_URL` 存在時會跳過 `webServer` 區塊，因此 dev server 的生死完全由你控制。跑完自己關掉 vite。

**備援做法（已驗證，若上面失效再用）**：拆兩批、各自讓 Playwright 託管，每批之間重新 reset。批一為 `app-shell` / `character-builder` / `character-sheet` / `m01c` / `m01d` / `m01e` / `m01f` / `m01g` / `m01i`（35 tests，約 2.2 分鐘）；批二為 `m01j` / `m02a` / `m02b` / `m02f` / `m02g` / `m02h-bilingual` / `m02h-localization` / `p1f` / `p1g` / `p1h`（37 tests，約 1.9 分鐘）。

回報時不要用單一次 Playwright 託管的整套指令結果宣稱全綠，也不要寄望重跑會過。

### 重啟條件

外部託管目前只有**一次**整套全綠的證據，先前的崩潰則重現過多次。累積數次穩定的整套結果後，可考慮直接改掉 `playwright.config.ts` 的 `webServer` 設定並移除本條。要真正修掉而非繞過，需先攔到 Playwright 託管模式下 vite 的 exit code 與收掉它的訊號來源。
