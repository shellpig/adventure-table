# B1 — persistence：事件寫入者蓋章與 migration

## Scope

- `apps/server/app/persistence/rooms/table_runtime.py`：`session_events` 新增可為空欄位 `acting_ai_controller_grant_id`（Uuid，無 FK）、`acting_grant_generation`（Integer）；`append_in_transaction` 在 `expected_actor_binding` 不為 None 且 `actor_kind == "ai"` 時寫入 binding 的 `ai_controller_grant_id`／`grant_generation`，其餘寫 NULL；`StoredTableEvent` 新增兩欄、`_event(row)` 帶出。
- 新 migration `apps/server/alembic/versions/0034_m06b_event_actor_stamp.py`（web track，down_revision `0033_p6e_adventure_imports`，不回填）。
- `TableEvent`（公開 DTO）不新增欄位。
- 新 `apps/server/tests/test_m06b_own_echo_suppression.py`（測試指南指定檔名；B2 續加 service 行為測試）：蓋章（AI binding／Human binding／系統事件）、`TableEvent` JSON 不含兩欄；新 `apps/server/tests/test_m06b_postgres_migration.py`（`P4_POSTGRES_URL` 才跑：upgrade heads、downgrade 一步、再 upgrade，既有事件可讀；web track migration 只在 PostgreSQL 上測，沿用 `test_p6e_postgres_migration.py` 模式）。
- 契約：`實作規格.md` B.6；`開發設計方針.md` §1、§4.1；`測試指南.md` B.6 第四點、B.9 第二點、B.10。

## 前置

- M06-A 已關門。

## 驗收

- focused＋migration 相關回歸（見 prompt）全綠；指揮者另以 `P4_POSTGRES_URL` 跑 PG migration 測試。

## 紀錄

- **起始**：2026-09-25，worker agy（`Gemini 3.8 Flash (High)`），1 回合 10 分鐘，prompt `C:/_work/AI_Work/Tools/agy-runs/agy-m06b-B1.prompt.txt`，conversation `2d3acfce-2a39-44bb-9796-c068115a4361`
- **交付**：`session_events.acting_ai_controller_grant_id`／`acting_grant_generation`（nullable、無 FK）；`append_in_transaction` 依 `expected_actor_binding.actor_kind == "ai"` 蓋章；`StoredTableEvent` 兩欄、`_event` 帶出；migration `0034_m06b_event_actor_stamp`；`test_m06b_own_echo_suppression.py` 5 個測試（AI 蓋章、Human／系統 NULL、idempotent replay、`TableEvent` 與 REST `/events` JSON 無兩欄）與可重用的 `create_m06b_fixture()`；`test_m06b_postgres_migration.py`。
- **指揮者審核修正**：
  - **revision id 超長**：agy 命名 `0034_m06b_event_acting_controller`（33 字元），PostgreSQL `alembic_version.version_num` 是 VARCHAR(32)，upgrade heads 直接失敗並拖垮所有 PG migration 測試；SQLite 不檢查長度，agy 本機測不到。改為 `0034_m06b_event_actor_stamp`（27 字元），檔名同步。之後的 migration 命名要算長度。
  - `StoredTableEvent` 新欄位去掉 `= None` 預設（唯一 constructor 是 `_event`，預設值會掩蓋漏改）。
  - fixture 移除沒人傳的 `engine` 參數。
- **測試**：`P4_POSTGRES_URL=…/adventure_table_p4f` 下 `test_m06b_own_echo_suppression.py test_m06b_postgres_migration.py test_p6e_postgres_migration.py test_migration_heads.py test_m03d_schema_parity.py test_m03_import_boundary.py test_p3a_table_events.py test_p3a_event_api.py test_p3e_mcp_event_loop.py test_code_quality_gate.py`：僅 `test_p6e_postgres_migration.py::test_p6e_imports_status_and_sources_kind_checks` 失敗——該檔 SQL 字串少右括號（`... 'sha-2'` 未閉合），本 branch 未動此檔，是 P6-E 既有測試 bug，只在有 PG URL 時才會跑到；其餘全綠（M06-B PG migration 測試實跑通過）。全套 backend（無 PG）exit 0，2568 passed。
- **未解問題／下一步**：`test_p6e_postgres_migration.py` 既有 bug 登記於 closeout；B2。
