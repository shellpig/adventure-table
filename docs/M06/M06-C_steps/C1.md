# C1 — 精簡 roll.resolved 投影與接線

## Scope

- 新 `apps/server/app/domain/rooms/ai_event_projection.py`：`DICE_DETAIL_KEYS`、`compact_roll_resolved(payload, *, dc, auto_fail, is_current_dm)`。
- `apps/server/app/persistence/rooms/p3c_rolls.py`：`RollRepository` 新增一次查詢取多個 request 的 `(dc, auto_fail)`；`apps/server/app/domain/rooms/rolls.py` `RollService` 對應方法。
- `apps/server/app/domain/rooms/ai_tools.py`：`wait_for_event` 取得 page 後，對 `roll.resolved` 事件一次批次查詢再套投影；`get_pending_events` 不變。
- 新 `apps/server/tests/test_m06c_compact_roll_projection.py`：C.1～C.5。
- 契約：`實作規格.md` C.1～C.5；`開發設計方針.md` §5；`測試指南.md` C.1～C.6。

## 前置

- M06-B 已關門。

## 驗收

- focused＋P3-E／P4-E MCP／P6 MCP 回歸（見 prompt）全綠。

## 紀錄

- **起始**：2026-09-25，worker agy（`Gemini 3.8 Flash (High)`），1 回合 10 分鐘，prompt `C:/_work/AI_Work/Tools/agy-runs/agy-m06c-C1.prompt.txt`，conversation `845b9008-db47-4999-8990-711d1a9bb0da`
- **交付**：`app/domain/rooms/ai_event_projection.py`（`DICE_DETAIL_KEYS`、單顆保留 d20 formula regex、`compact_roll_resolved`）；`RollRepository.list_request_outcome_inputs`（一次 SELECT 取 `(dc, auto_fail)`，不濾 Combat 列）與 `RollService.request_outcome_inputs`；`AIToolApplicationService._compact_roll_results`；`tests/test_m06c_compact_roll_projection.py` 24 個 case（純函式參數化＋經 B2b `integration_fixture` 的真實 MCP：DM／Player 投影、pending 與 Human 路徑完整、一頁一次查詢 spy、Monster 先攻擲骰秘密投影）。
- **指揮者審核修正**：
  - `wait_for_event` 對必定存在的 `roll_request_id` 做 `try/except (ValueError, TypeError): pass` 並重複解析 UUID——改為 `_compact_roll_results` helper，直接解析；沒有 `roll.resolved` 時原 page 直接回傳。
  - `compact_roll_resolved` 在 `total` 非數字時補 `0`（假資料補值）——改為直接讀 `payload["total"]`（`assert isinstance(total, int)`，`app/` 既有同型寫法）；`outcome` 合併為一行判斷。
  - agy 為遷就新接線替 `test_p3e_mcp_event_loop.py` 的 `_EmptyEventPage` stub 加了 `model_copy` 與 events 參數——還原，只補 `self.events = []` 一行。
- **測試**：focused `test_m06c_compact_roll_projection.py test_m06b_mcp_echo_integration.py test_m06b_own_echo_suppression.py test_m06a_wait_timeout_cap.py test_p3e_shared_roll_integration.py test_p3e_mcp_event_loop.py test_p3e_mcp_tools.py test_p4e_event_secrecy.py test_p4e_mcp_combat_lifecycle.py test_code_quality_gate.py` exit 0；全套 backend exit 0，2627 passed。既有 `wait_for_event` payload 斷言無需修改。
- **未解問題／下一步**：C2 關門、全套 E2E、合併。
