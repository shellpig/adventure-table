# A1 — backend：`wait_after` 上限參數與 MCP 接線

## Scope

- `apps/server/app/domain/rooms/table_events.py`：`TableEventService.wait_after` 新增 keyword `max_timeout: float = 60.0`，截斷改為 `min(float(timeout), max_timeout)`；其餘行為不變。
- `apps/server/app/domain/rooms/ai_tools.py`：`AIToolApplicationService.wait_for_event` 傳 `max_timeout=WAIT_EVENT_MAX_TIMEOUT_SECONDS`。
- Human `/events/wait`（`app/api/rooms/table_events.py`）不改，沿用預設 60。
- 新 `apps/server/tests/test_m06a_wait_timeout_cap.py`：fake notifier 記錄 `wait(handle, timeout)` 收到的值；A.1（120／90）、A.2（輸入上限、`WAIT_TIMEOUT_SECONDS`、傳入的 `max_timeout`、guide／briefing 秒數都等於常數）、A.3（Human route 60 與 61→422）。
- 契約：`實作規格.md` A.1～A.3；`開發設計方針.md` §3.1、§3.2；`測試指南.md` A.1～A.4。

## 前置

- 無程式依賴。

## 驗收

- `pytest tests/test_m06a_wait_timeout_cap.py tests/test_m04b_wait_timeout_cap.py tests/test_m04c_briefing.py tests/test_m04c_mcp_guide.py tests/test_p3e_*.py tests/test_p3a_*.py tests/test_code_quality_gate.py`（cwd `apps/server`）全綠，既有斷言不改。

## 紀錄

- **起始**：2026-09-25，worker agy（`Gemini 3.8 Flash (High)`），1 回合 9 分鐘，prompt `C:/_work/AI_Work/Tools/agy-runs/agy-m06a-A1.prompt.txt`，conversation `2fd477d1-6a66-46f4-be00-4635277b9d85`
- **交付**：`TableEventService.wait_after(..., max_timeout: float = 60.0)`；`AIToolApplicationService.wait_for_event` 傳 `max_timeout=WAIT_EVENT_MAX_TIMEOUT_SECONDS`；`tests/test_m06a_wait_timeout_cap.py` 7 個測試（fake notifier 記錄 timeout，不做真等待）。Human route 以 `dependency_overrides[get_table_event_service]` 注入帶 fake notifier 的 service 子類（只換 `list_after`／`resolve_human_actor`，`wait_after` 用真實作）。
- **指揮者審核修正**：
  - agy 在 `wait_for_event` 包了 `try/except TypeError` 回退成不帶 `max_timeout` 的呼叫，只為遷就 `test_p3e_mcp_event_loop.py` 的 `_WaitEventService` stub——刪掉回退，改為 stub 簽名補 `max_timeout`（斷言不變）。
  - 刪掉兩個 actor builder 沒人傳的 `session_id`／`access_session_id` 參數。
- **測試**：focused `test_m06a_wait_timeout_cap.py test_m04b_wait_timeout_cap.py test_m04c_briefing.py test_m04c_mcp_guide.py test_p3e_mcp_event_loop.py test_p3e_mcp_tools.py test_code_quality_gate.py` 44 passed；全套 backend 見 A2。
- **未解問題／下一步**：無；A2 關門。
