# A4b-2 — web：entry 圖片上傳／連結／縮圖

## Scope

- `apps/web/src/api/roomAssets.ts`：新增 `getRoomAssetContent(roomId, assetId, token): Promise<Blob>`（同 `getSessionStageImage` pattern）；`roomAssetContentUrl` 保留。
- `AdventureEditorPage.tsx`：每個 entry 顯示已連結 assets（role image／map 顯示縮圖，其餘顯示檔名）；縮圖經 `AssetThumbnail` 元件：fetch blob → `URL.createObjectURL`，unmount／換 asset 時 `revokeObjectURL`（同 `SessionTableSurface` stage image pattern；**指揮者 2026-09-20 拍板：不改 server、不加 token query param**）。
- 上傳並連結：`<input type="file" accept="image/png,image/jpeg,image/webp">`＋visibility（`room`／`dm_only`）＋role（`image`／`map`）→ `uploadRoomAsset` → `linkAdventureEntryAsset`（順序固定）；unlink 按鈕（`unlinkAdventureEntryAsset`）。archived 唯讀。
- `adventuresCopy.ts` 追加 asset copy 兩 locale。
- Vitest：`roomAssets.test.ts` 加 `getRoomAssetContent`；`AdventureEditorPage.test.tsx` 加 upload→link 順序（mock client 呼叫序）、unlink、copy parity。

## 對應契約

實作規格 P6-A「Room 可保存 P6 reusable assets…asset download 必須走 Room authority / projection」；設計 §A.1（visibility、media type）、`link_entry_asset` role↔kind 規則。

## 前置

A4b-1、A1b asset routes。

## 驗收

- `npm test -- --run`、`npm run build`（cwd `apps/web`）通過。

## 紀錄

（派工後補）
