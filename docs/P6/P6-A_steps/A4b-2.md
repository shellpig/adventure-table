# A4b-2 — web：entry 圖片上傳／連結／縮圖

## Scope

- `apps/web/src/api/roomAssets.ts`：新增 `getRoomAssetContent(roomId, assetId, token): Promise<Blob>`（同 `getSessionStageImage` pattern）；`roomAssetContentUrl` 保留。
- `AdventureEditorPage.tsx`：每個 entry 顯示已連結 assets（role image／map 顯示縮圖，其餘顯示檔名）；縮圖經 `AssetThumbnail` 元件：fetch blob → `URL.createObjectURL`，unmount／換 asset 時 `revokeObjectURL`（同 `SessionTableSurface` stage image pattern；**指揮者 2026-09-20 拍板：不改 server、不加 token query param**）。
- 上傳並連結：`<input type="file" accept="image/png,image/jpeg,image/webp">`＋visibility（`room`／`dm_only`）＋role（`image`／`map`）→ `uploadRoomAsset` → `linkAdventureEntryAsset`（順序固定）；unlink 按鈕（`unlinkAdventureEntryAsset`）。archived 唯讀。
- `adventuresCopy.ts` 追加 asset copy 兩 locale。
- Vitest：`roomAssets.test.ts` 加 `getRoomAssetContent`；`AdventureEditorPage.test.tsx` 加 upload→link 順序（mock client 呼叫序）、unlink、copy parity。

## 對應契約

實作規格 P6-A「Room 可保存 P6 reusable assets…asset download 必須走 Room authority / projection」；設計 §A.1（visibility、media type）、`link_entry_asset` role↔kind 規則。

## 前置

A4b-1、A1b asset routes。

## 驗收

- `npm test -- --run`、`npm run build`（cwd `apps/web`）通過。

## 紀錄

- **起始**：2026-09-20，worker agy（`Gemini 3.8 Flash (High)`），1 回合 5 分 10 秒，prompt `C:/_work/AI_Work/Tools/agy-runs/agy-p6a-A4b-2.prompt.txt`，conversation `5613bc5b-6486-46ab-bd0d-67843abc9a40`。
- **交付**：`roomAssets.ts` `getRoomAssetContent(roomId, assetId, token): Promise<Blob>`；`AssetThumbnail.tsx`（fetch blob → `createObjectURL`，cleanup revoke；未就緒 render null）；`AdventureEditorPage.tsx` `EntryAssetUploadForm`（file／role image|map／visibility 預設 `dm_only`，submit 後以 key bump 清空 input）、`AdventureEntryList` 新增 `roomId`／`token`／`onLinkAsset`／`onUnlinkAsset`／`onAssetError`，每 entry 顯示 assets（image／map 縮圖＋role label＋dm_only badge，其餘顯示檔名，Unlink 走 confirm）；page 端 `onLinkAsset` = `uploadRoomAsset(...).then(linkAdventureEntryAsset)` 單一 pending 週期；`adventuresCopy` 14 個 asset key 兩 locale＋`room_asset_not_found`／`asset_empty` 對照；`rooms.css` asset strip／thumb／upload 規則；`roomAssets.test.ts` 加 1、`AdventureEditorPage.test.tsx` 加 2、擴 2。
- **指揮者審核修正**：`entry.assets && entry.assets.length` 對非 optional 欄位的防禦 guard → 移除；zh-TW `assetRoleLabel` '角色'（與 D&D 角色混淆）→ '用途'。
- **測試**：`npm test -- --run` 94 files／556 passed；`npm run build` exit 0（指揮者本機重跑）。
- **未解問題／下一步**：每個 entry row 常駐一組上傳表單，畫面偏長，P8 Polish 再收；縮圖失敗只顯示在頁首 `form-error`。下一步 A4c。
