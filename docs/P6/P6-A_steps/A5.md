# A5 — E2E、關門 gate、closeout、合併 main

## Scope

- `apps/web/e2e/p6a-adventures.spec.ts`：owner 建 Room → 建 Adventure（只 Name）→ editor 加 scene／npc／secret entry＋上傳 image attach → finalize → Campaign attach 兩個 Adventure → list 兩筆 → detach 一個 → 第二個 Campaign 零 Adventure 時 Start Session＋post narration 照常；member 進 `/adventures` 看到無權限、直接打 API 404。
- 關門 gate（AGENTS 工程守則 4）：全套 backend pytest（含 `P4_POSTGRES_URL` 的 PG migration）、`npm test -- --run`、`npm run build`、`docker compose config`、全套 Docker E2E（Subphase 動到 `apps/web`）。
- `docs/P6/P6-A_CLOSEOUT.md`（對照 M05-B closeout 格式）；`PROJECT_BRIEF.md` P6-A ✅；ROADMAP_HISTORY 不動（P6 未全關）。
- 合併 `main`（`--no-ff`），push。

## 對應契約

測試指南 A.1～A.5 全部；實作規格「P6 Closeout 原則」適用於 Subphase 的部分。

## 前置

A1a～A4c。

## 驗收

- 上述 gate 全綠並記入 closeout；merge commit hash 回報。

## 紀錄

- **起始**：2026-09-20，指揮者自做。
- **交付**：`apps/web/e2e/p6a-adventures.spec.ts`（Journey：workspace → 建 Adventure → editor 建 scene／secret／suggested_check → 上傳 dm_only image 縮圖為 `blob:` → finalize 後仍可編輯 → Campaign attach 兩個、draft attach 409、duplicate 409 → detach 一個 → attached delete 409 → 零 Adventure Campaign Start Session／narration／Player action／Request Check／Start Combat；Member：UI 無權限且零 `/adventures` API 呼叫、API 404）；`docs/P6/P6-A_CLOSEOUT.md`；`PROJECT_BRIEF.md` P6-A ✅、下一步 P6-B。
- **指揮者審核修正**：spec 三次修正皆為測試本身（entry 表單與每列上傳表單各有 Visibility select 需 scope；vite module URL 含 `/api/adventures.ts` 誤入 request 過濾；fresh Combat 狀態為 `initiative_pending`）。
- **測試**：見 closeout「關門 gate」。
- **未解問題／下一步**：Builder flake 的 server-e2e 長跑退化證據見 closeout 觀察第 5 點；合併 `main`。
