# D5 — Session DM UI（set Stage、`needs_review`）＋E2E

## Scope

- `SessionCampaignRuntimePanel`：加「從 Adventure／Runtime image set Stage」（呼叫 D3 route）與 entry／override 的 `needs_review` toggle；Quick Add／Current Scene／Situation 已在 P6-B，不重做。
- Player 端不出現上述控制；zh-TW／en copy 同步。
- Subphase 關門的 E2E spec：DM set Stage → Player 看到新 stage image、拿不到 asset；needs_review 標記顯示。

## 契約與輸入

- 設計「REST / UI surface」Session DM 條；測試 D.5。前置：D3。
- **派工決策（2026-09-22）**：(1) 前端新增 `getSessionStage`／`setSessionStageImageSource` client，送出前先 GET canonical stage revision 再 PUT `stage/image-source`，走既有 `executeActiveSessionMutation` 與 `generateRuntimeIdempotencyKey`；Stage 畫面更新沿既有 `stage.updated` event，不改渲染；(2) 候選圖片開啟 picker 時才 lazy load：attached Adventure entries 的 image／map asset、有 `source_adventure_entry_id` 的 runtime entry（送 `runtime_entry_image`）、Room image assets 三組；(3) `needs_review` toggle 走既有 active-session update entry／override route（`expected_revision` 取自 view）；(4) 不動 server、不動 P6-B Campaign Changes 頁；(5) 新 vitest 檔獨立於 1,074 行的既有 panel 測試；E2E spec 由 agy 撰寫、指揮者以 `npm run test:e2e:docker` 執行。

## 驗收

- vitest：面板兩種 role 的渲染與 action 呼叫；locale parity。
- E2E：`npm run test:e2e:docker` 對應 spec 綠。

## 完成紀錄

- **起始**：2026-09-22；agy 1 回合，683 秒；conversation `f25b7166-af4d-4f1c-9d5b-346a3731a364`。D5 依使用者拍板維持單一步驟。
- **交付**：`api/sessions.ts` 新增 `StageImageSource` 三種 source type、`SetStageImageSourceRequest`、`getSessionStage`（GET `/stage`）與 `setSessionStageImageSource`（PUT `/stage/image-source`）；`sessionCampaignRuntime.ts` 新增 `buildStageCandidates`（attached Adventure image／map asset → `adventure_entry_asset`、有 `source_adventure_entry_id` 的 runtime entry → `runtime_entry_image`、Room image → `room_asset`，option value 用不透明 key 不含 asset id）、picker 開啟時 lazy load、submit 先 GET canonical revision 再 PUT、entry／override `needs_review` toggle（走 `updateActiveRuntimeEntry`／`updateActiveOverride`，`expected_revision` 取自 view）；authority-loss／revision-conflict 判斷加入 exploration router 的 `table_actor_unauthorized`／`session_not_active`／`stage_revision_conflict` machine code，`campaignRuntimeErrorMessage` 對映五個 stage code；`SessionCampaignRuntimePanel.tsx` 新增 `StageImagePicker` 與 `SessionReviewList`；en／zh-TW copy 15 key；`sessionTable.css` 樣式；E2E `p6d-stage-and-review.spec.ts`。
- **指揮者審核修正**：(1) `SessionCampaignRuntimeActions` 十二個新欄位全是 optional、panel 全用 `?.()`（optional-everything），改為 required，兩個測試檔各加 `fakeActions()`／`stageAndReviewActionDefaults` fixture；(2) 兩個 toggle handler 40 行重複抽成 `toggleNeedsReview(action)`，`overlay.override!` 三處 non-null assertion 改先取值；(3) `isCampaignRuntimeAuthorityLossError`／`...RevisionConflictError` 多餘的 `(err as { code: unknown }).code` cast 還原為 narrowing 後的 `err.code`；`!entry.assets ||` 對必有欄位的防禦讀取移除；(4) panel 229 行加在 view 內含兩個 JSX IIFE、三段複製的 `<optgroup>`、title fallback 顯示 raw `entry.id`——抽成 `StageImagePicker`／`ReviewRow`／`SessionReviewList`，group 用 `STAGE_CANDIDATE_GROUPS` map，fallback 改 `copy.unnamedEntry`；(5) toggle button 的 `aria-label` 蓋掉可見文字的 accessible name，E2E 以文字找不到按鈕——移除 aria-label 與兩個 copy key；(6) E2E spec 漏掉 owner 取 DM seat（`startSession` 找不到 Start Session 按鈕），補 `addSeat(..., 'dm', ...)`。
- **測試**：`npm test -- --run` → 101 files／734 tests passed；`npm run build` 通過；`git diff --check` 通過。E2E（`npm run test:e2e:docker`，需 `ADVENTURE_TABLE_E2E_ALLOW_DESTRUCTIVE_RESET=1`）：`p6d-stage-and-review.spec.ts` 1 passed（DM 設 Stage → DM／Player 皆見 `.session-stage img`、Player HTML 不含 asset id／`/assets/`、Player 無 Set Stage／review 控制；DM 標 needs_review 見 badge、Player Journal 無 badge）；`p6b-campaign-runtime.spec.ts`＋`p3b-exploration-chat-actions.spec.ts` 4 passed（panel 重構前跑；closeout 全套 E2E 再覆蓋）。
- **未解問題／下一步**：D5 vitest 的 submit／toggle 案例沿既有 panel 測試慣例以 `executeActiveSessionMutation`＋spy 重演 action，未 render hook；hook 真實路徑由 E2E 覆蓋。picker 候選只在首次開啟載入，Session 中新上傳的 asset 需重新進入 Session 才出現。接 Subphase 關門 gate／closeout。
