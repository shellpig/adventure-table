# D5 — Session DM UI（set Stage、`needs_review`）＋E2E

## Scope

- `SessionCampaignRuntimePanel`：加「從 Adventure／Runtime image set Stage」（呼叫 D3 route）與 entry／override 的 `needs_review` toggle；Quick Add／Current Scene／Situation 已在 P6-B，不重做。
- Player 端不出現上述控制；zh-TW／en copy 同步。
- Subphase 關門的 E2E spec：DM set Stage → Player 看到新 stage image、拿不到 asset；needs_review 標記顯示。

## 契約與輸入

- 設計「REST / UI surface」Session DM 條；測試 D.5。前置：D3。

## 驗收

- vitest：面板兩種 role 的渲染與 action 呼叫；locale parity。
- E2E：`npm run test:e2e:docker` 對應 spec 綠。

## 完成紀錄

（待補）
