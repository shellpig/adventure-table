# D1 — `CampaignWorldService` 八個 intent

## Scope

新增 `app/domain/campaign_runtime/world.py` `CampaignWorldService`，以契約 D.1 的 intent 名稱統一 Human UI 與 MCP：`create_world_entry`、`update_world_entry`、`archive_world_entry`、`set_adventure_override`、`clear_adventure_override`、`set_current_context`、`grant_character_knowledge`、`set_needs_review`。

- 兩條路徑都委派既有 `CampaignRuntimeService`：management（`RoomAccessContext`，Session 外 Human owner／dm；無 TableEvent、無 notifier）與 active（`TableActorContext`，active Session Human／AI current DM；`world.*` event＋notifier）。AI actor 一律 active。
- `grant_character_knowledge`＝entry patch 到 `visibility="character"`＋recipient character ids（沿用 `validate_runtime_visibility_recipients`）；`set_needs_review`＝entry／override 的 `needs_review` patch。不新增 persistence、不新增 event kind。
- 不做 `resolve_world_action`（D2）、不碰 Character `inventory_state`。
- **派工決策**：既有 P6-B REST route 不改接本 service（見接手摘要）。

## 契約與輸入

- 規格 P6-D 全段；設計 D.1；測試 D.1、D.3、D.4。前置：D0b。

## 驗收

- `tests/test_p6d_world_service.py`（parametrize）：同 fixture 的 Human current DM 與 AI DM 走同一 intent 得同 validation／event shape（D.1）；Session 外 Human management 有 mutation record／revision audit、無 `session_events`、無 notifier，AI 同情境被拒零副作用（D.1）；同 idempotency key retry 只成功一次、回同 result、不重複 `world.*` event（D.3）；`needs_review=true` 不影響後續讀取（D.4）；Player／AI Player／revoked grant／stale epoch 全拒零副作用。

## 完成紀錄

- **起始**：2026-09-22；agy 3 回合、總計 1,417 秒；conversation `01f4edac-a50b-42af-a6f9-925ed2cb9281`。D1 依使用者拍板維持單一步驟，沒有再拆子步。
- **交付**：新增 `campaign_runtime/world.py` 與 package exports；`CampaignWorldService` 提供八個固定 intent，依 `TableActorContext`／`RoomAccessContext` 分派 active／management 路徑，所有 canonical authority、revision、idempotency、audit、event 與 notifier 行為仍由 `CampaignRuntimeService` 負責。新增 typed knowledge／review／override intent；override partial update 以 `model_fields_set` 保留未提供欄位，允許明確 `note=None` 清除 note。
- **測試**：新增 `test_p6d_world_service.py`，覆蓋 Human／AI DM event 與 validation parity、八 intent 的 active／management 路徑、management／active idempotency、override partial update、knowledge secrecy、entry／override `needs_review`、Player／AI Player／revoked grant／stale epoch／inactive Session 零副作用拒絕，以及 Runtime Item 不修改 Character `inventory_state`。指揮者獨立執行 P6-A～C＋D1、`test_code_quality_gate.py`、`test_m03_import_boundary.py`：302 passed／12 skipped；skip 均為未提供 `P4_POSTGRES_URL` 的既有 PostgreSQL job gate。`git diff --check` 通過。
- **指揮者審核修正**：第一輪補上真正的 Human／AI event／validation shape 比對、互不污染的 revoked／stale authority case、全部八 intent 的 Session 外 management flow 與 management retry；第二輪發現並修正 override update 預設值誤清空未提供欄位，另把 stale revision 驗證提升為完整 row、mutation、event、notifier 均不變。
- **驗證 commit**：見步驟板。
- **未解問題／下一步**：D1 無未解 blocker；接 D2。D2～D5 均未在本步提前實作，既有 P6-B REST route 亦未改接。
