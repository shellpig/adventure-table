# D3 — Stage bridge：asset／entry image → P3 Stage

## Scope

- `set_stage_image(actor, source)`：source 為 Room asset id、Adventure entry asset 或 Runtime entry image；application boundary 經 Room／asset authority（`RoomAssetService.open_content`）讀 bytes，**複製一份**成既有 `room_stage_images`，再沿用 `ExplorationStageService.replace_stage` 的 revision／event／notifier 語意。
- `session_stages.image_id` 仍只指向 stage image；不公開 asset URL／storage key；dm_only asset 只有 DM 能當來源，Player 只讀合法 stage image。
- 不遷移 `room_stage_images` schema，不改 `ExplorationStageService` contract。
- 一條 Session DM REST route（依既有 `/api/rooms/{room}/...` convention）供 D5 UI 使用。

## 契約與輸入

- 設計 D.3；測試 D.5。前置：D0b（與 D1／D2 獨立）。

## 驗收

- `tests/test_p6d_stage_bridge.py`：bytes 複製後 stage image 與來源 asset 獨立（刪 asset 不影響 stage）；`image_id` 指向 stage image；Player 讀 stage 拿不到 asset id／storage key；非 DM／非本場 actor／revoked 拒絕零副作用；route 測試沿 P6-B API 模式。

## 完成紀錄

- **起始**：2026-09-22；agy 2 回合、總計 1,111 秒；conversation `8433e258-c372-4791-b4fa-cbf9833f737e`。D3 依使用者拍板維持單一步驟，沒有再拆子步。
- **交付**：新增 `campaign_runtime/stage.py` 的三種 discriminated source 與 `CampaignStageBridgeService.set_stage_image`；Room asset、已 attach Adventure 的 image／map entry asset，以及具有 `source_adventure_entry_id` 的 Runtime entry 都經 current DM authority與 `RoomAssetService` 讀取 bytes，再轉成既有 `StageImageUpload` 呼叫 `ExplorationStageService.replace_stage`。新增 `PUT .../stage/image-source` Human DM route與 dependency wiring；Stage text、revision、event、idempotency、notifier完全沿用 P3 contract。
- **安全與邊界**：Human／AI current DM皆可用 bridge；Player、inactive、revoked、stale actor拒絕且零副作用。`session_stages.image_id`只指向新複製的 `room_stage_images`，Stage DTO／event不含 source asset id、URL或 storage key；刪除 direct Room source asset不影響 Stage bytes。Runtime source只沿既有 Adventure source linkage，不新增 image欄位、migration或 geometry。
- **測試**：新增 `test_p6d_stage_bridge.py`，覆蓋 Human／AI parity、三 source validation、image／map role、wrong Room／Campaign、bytes independence、dm_only authority、Player projection、Stage text preservation、stale revision、canonical idempotency／notifier與 REST success／error mapping。指揮者獨立執行 P3-B Stage、P6-A～D、`test_code_quality_gate.py`、`test_m03_import_boundary.py`：344 passed／17 skipped；skip 均為未提供 P3／P4 PostgreSQL job URL 的既有環境 gate。`import app.main`、`git diff --check` 通過。
- **指揮者審核修正**：補齊 Adventure／Runtime source 的 image／map role gate、wrong-Room與各拒絕路徑完整零副作用快照、canonical notifier replay證據與 REST精確 machine code；移除無關 export並以精確 media type cast取代 ignore。
- **驗證 commit**：`a0cae16a`（Codex 指揮者驗證後額度用盡，由 Claude 指揮者依其紀錄提交，未重跑測試）。
- **未解問題／下一步**：D3 無 blocker；接 D4。提交時 code review 留三個 nit 給 D4 順手清：(1) `stage.py::_require_active_dm_authority` 與 `room_assets/service.py::open_content_for_table_actor` 各有一段 `try: … except TableEventActorUnauthorizedError: raise` 無作用；(2) 兩處 active-actor 驗證 20 行重複，且與 `CampaignRuntimeService._require_active_actor` 同型，應抽共用 helper；(3) `open_content_for_table_actor` 的 `visibility not in ("room", "dm_only")` 是不可能分支（`RoomAssetVisibility` 只有這兩值）。D4／D5 未在本步提前實作。
