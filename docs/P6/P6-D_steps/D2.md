# D2 — `resolve_world_action` atomic world＋narration

## Scope

- 抽 `TableEventRepository.append_in_transaction(connection, ...)`（`append` 改為開 transaction 後委派）與 `ExplorationMessageRepository.append_message_in_transaction`；純抽取，既有 `append`／`append_message` 行為不變。
- `CampaignWorldService.resolve_world_action(actor, request, idempotency_key)`：只在 active Session 合法；同一 DB transaction：validate → apply Runtime／Override／Current Context changes（重用 D0b 的 `execute_*_in_transaction`）→ append `world.action.resolved` event → 若 supplied narration，append 既有 `exploration.narration`＋`session_messages(kind=narration)` projection → commit → notifier。任何 validation／persistence 失敗整體 rollback，不留 definitive narration。**不得**新造 `world.narration`。
- Existing P3／P4 mutation 不被包進來；需要 Attack／Check 先呼叫原 service。
- **派工決策**：只開 MCP（D4），不開 Human REST route。

## 契約與輸入

- 設計 D.2、Events 段；測試 D.2、D.3、D.7。前置：D1。

## 驗收

- `tests/test_p6d_world_action.py`（parametrize）：invalid override → reject、無 world change、無 narration event（D.2）；成功時 `world.action.resolved`＋`exploration.narration`＋`session_messages` 同時存在、無 `world.narration`（D.2）；commit 失敗（monkeypatch 最後一步 raise）兩邊皆不存在；同 key retry 只一次、不重複 event（D.3）；`wait_for_event` 被 notifier 喚醒、cursor 語意不變（D.7）；Player／pre-session／revoked／stale epoch 拒絕零副作用。
- P3-B／P3-C／P3-D／P4-C／M05 regression 全綠（`append` 抽取）。

## 完成紀錄

- **起始**：2026-09-22；agy 2 回合、總計 1,264 秒；conversation `1b5c0966-2dc9-49d0-814d-0d5a1e381afb`。D2 依使用者拍板維持單一步驟，沒有再拆子步。
- **交付**：抽出 `TableEventRepository.append_in_transaction` 與 `ExplorationMessageRepository.append_message_in_transaction`，既有 public method 只開 transaction 後委派；新增六種 discriminated world change、`ResolveWorldActionRequest／Result` 與 `CampaignWorldService.resolve_world_action`。active Human／AI current DM 的單一 world mutation、`world.action.resolved`、可選既有 `exploration.narration`＋`session_messages` 在同一 transaction，commit 後才 notifier；retry 以 campaign-scoped outer mutation 保存完整 request／result，衝突與失敗均零副作用。
- **安全與邊界**：action event 沿用 entry／override／context envelope，只帶 reference／revision，不含 body、dm_notes 或 raw state；public narration只來自 caller 明確文字，不建立 `world.narration`。內部 mutation key 使用 reserved namespace，public D1／D2 key 不可占用；campaign lock 先於 outer idempotency lookup。沒有新增 REST／MCP／Stage／UI、migration、P7 batch engine，亦未包裝 P3 Roll／P4 Combat。
- **測試**：新增 `test_p6d_world_action.py`，實際執行六種 change，覆蓋 Human／AI parity、with／without narration、完整 rollback、forced final projection failure、same-key retry／changed-request conflict、waiter wake＋cursor order、Player／inactive／revoked／stale authority、secret／character projection、legacy append extraction、reserved key、blank／length validation。指揮者獨立執行 P3-B／C／D、P4-C、M05、P6-A～D、`test_code_quality_gate.py`、`test_m03_import_boundary.py`：497 passed／39 skipped；skip 均為未提供 P3／P4 PostgreSQL job URL 的既有環境 gate。最後常數／export 收斂後 D2＋code-quality：24 passed；`git diff --check` 通過。
- **指揮者審核修正**：發現原交付的 archive branch 傳入不存在的 `room_id` 且 update／archive／clear override 未執行測試；退回同一 D2 回合修正並補齊六 branch。另抽出 D1／D2 共用 override intent conversion、把 campaign lock 移到 authoritative idempotency lookup 前、加入 reserved internal key 與完整 narration validation；最後移除同一 prefix 的重複常數／不必要 package export。
- **驗證 commit**：見步驟板。
- **未解問題／下一步**：D2 無未解 blocker；接 D3。D3～D5 均未在本步提前實作。
