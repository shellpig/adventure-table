# D4 — MCP 十個 write tool

## Scope

- 註冊 `world_create_entry`、`world_update_entry`、`world_archive_entry`、`world_set_override`、`world_clear_override`、`world_set_current_context`、`world_grant_knowledge`、`world_set_needs_review`、`world_resolve_action`、`set_stage_image`：全部 DM-only、active-session only；facade 只轉 intent 到 D1／D2／D3 service，不直接 repo write。
- 沿 P6-C C3 模式：擴充 `campaign_runtime/ai_tools.py` facade、`mcp/tools.py` definition＋dispatch、`guide_tool_names._EXPECTED`、M04 catalog parity、English＋zh-TW description；錯誤對映沿 `call_tool` base-class 分派。

## 契約與輸入

- 設計 D.4；測試 D.8。前置：D1、D2、D3。
- **派工決策（2026-09-22）**：(1) facade 沿 P6-C 直接擴充 `CampaignContextAIToolApplicationService`，注入 `campaign_world_service`／`campaign_stage_service`（base class 已佔用 `stage_service` 名稱）；(2) 十個 tool input 的 `idempotency_key` 一律 **required**（world／stage service 都要求 key，與 P3 tool optional 不同）；input 直接組合既有 domain model（`RuntimeWorldEntryCreate`、`RuntimeWorldEntryPatch`、`SetAdventureOverrideIntent`…），不重抄欄位；(3) 錯誤對映沿 `call_tool` base-class except 鏈，不加分支；(4) 不新增 Human REST route（`resolve_world_action` MCP-only 已拍板）；(5) 順手清 D3 三個 nit：無作用 re-raise、active-actor 驗證抽共用 helper、不可能的 visibility 分支。

## 驗收

- `tests/test_p6d_mcp_tools.py`（parametrize，沿 `test_p6c_mcp_tools.py` 三段式）：catalog／guide parity；Player catalog 不含十個 tool；pre-session DM、revoked grant、stale epoch、inactive Session 全拒零 dispatch；dispatch 一次到位；facade 輸出＝service `model_dump`；`test_m04c_tool_descriptions.py` pinned catalog 同步。

## 完成紀錄

- **起始**：2026-09-22；agy 1 回合，644 秒；conversation `ac7a44c6-c078-4beb-a85d-eba533e20e9d`。D4 依使用者拍板維持單一步驟。
- **交付**：`campaign_runtime/ai_tools.py` 十個 tool input（`WorldCreateEntryToolInput`…`SetStageImageToolInput`，直接組合 `RuntimeWorldEntryCreate`／`RuntimeWorldEntryPatch`／`SetAdventureOverrideIntent`／`ClearAdventureOverrideIntent`／`CampaignRuntimeContextPatch`／`GrantCharacterKnowledgeIntent`／`SetNeedsReviewTarget`／`ResolveWorldActionRequest`／`StageImageSource`，`idempotency_key` 一律 required）與 `CampaignContextAIToolApplicationService` 新增 `campaign_world_service`／`campaign_stage_service` 注入與十個 facade method（`_actor` → service → `model_dump`）；`mcp/tools.py` 十個 definition＋`_WHEN_TO_USE`＋dispatch，全部 `dm` only；`guide_tool_names._EXPECTED`、`mcp/dependencies.py` 組合、`test_m04c_tool_descriptions.py` `DM_CATALOG` 同步。D3 三個 nit 清完：`app/domain/rooms/table_events.py` 新增 `require_active_table_actor(connection, actor, repository) -> StoredTableActorBinding`，`CampaignRuntimeService._require_active_actor`、`CampaignStageBridgeService._require_active_dm_authority`、`RoomAssetService.open_content_for_table_actor` 三處共用；移除無作用 re-raise 與不可能的 visibility 分支。
- **指揮者審核修正**：(1) agy 把 `campaign_world_service`／`campaign_stage_service` 宣告成 `| None = None` 並在十個 method 各放一段 `is None → RuntimeError`（缺陷表「防禦式寫法」），改為 required 並刪除十處 guard；(2) `stage.py`／`room_assets/service.py` 抽 helper 後留下的 `TableEventNotFoundError`／`TableEventSessionNotActiveError` 未用 import 清掉；(3) 契約缺口：`world_set_current_context` 需要 context `expected_revision`，但 P6-C 讀 tool 沒有任何一個回傳 current context revision——`CampaignContextDmView` 新增 `current_context_revision`（DM only，無 row 時為 0，與 `stored_context_to_domain` 一致），tool description 指向該欄位；(4) 測試檔 `_invoke` 100 行 if-chain 改為 `_INPUT_TYPES` map＋`_sample_input`＋`model_validate`（parametrize 共用同一 map）、移除與 `p6_active_fixture` 重複的本地 `context_seeded_fix` fixture、`pytest.raises(Exception)`＋九項 isinstance 改為 `pytest.raises((PermissionError, RuntimeError, LookupError))`、六個未用 import 清掉（750 → 654 行）。
- **測試**：`tests/test_p6d_mcp_tools.py` 11 個 test／104 cases：catalog／guide parity、Player catalog 不含十個 tool、pre-session DM／Player／actor 拒絕零 dispatch、revoked／epoch／inactive 三種 × 十個 tool 零 DB 副作用、dispatch 一次到位、缺 idempotency_key／malformed → `invalid_arguments`、facade 輸出＝service `model_dump`（create／resolve／stage）、Human／AI parity（同 view shape、同 `world.entry.created`）、`call_tool` 錯誤對映。指揮者獨立執行 P3-B、P3-E、P4-C／P4-E MCP、M04、P6 全部、code quality、M03 boundary：611 passed／22 skipped（均為未提供 PostgreSQL job URL 的既有環境 gate）；`import app.main`、`git diff --check` 通過。
- **未解問題／下一步**：`test_p6d_mcp_tools.py` 的 `_DispatchSpy` 十個同型 method 沿 P6-C 前例保留；`mcp/tools.py` catalog 一行一 definition 的既有慣例讓 >140 字元行數由 92 增至 114。接 D5。
