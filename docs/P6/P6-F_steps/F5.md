# F5 — UI：Draft Review 面板、Finalize、zh-TW／en、E2E

## Scope

- Importer 區擴充 Review 能力（DM-only，沿既有 `canAuthor` gate）：
  - 每個 draft entry：Accept／Edit／Ignore／Mark uncertain 動作與目前 `review_status` 顯示；View source（跳到該 entry `source_ref` 對應 source 的 chunk 位置，沒有 source_ref 時不顯示）；
  - Warnings 分區依 Info／Warning／Blocking 顯示，Blocking 未解時 Finalize 停用並說明原因；warning 可 resolve；question 可作答；
  - Finalize 表單（Adventure name／summary）與成功後導向既有 Adventure editor；重複送出不產生第二個 Adventure（依 F2b idempotency）。
  - **不要求逐條 Accept**：UI 不得把 Accept 做成 finalize 前置條件。
- 開工前先決定是否拆 `AdventureImporterPanel.tsx`（P6-E closeout 觀察 1：1,241 行）。建議拆成 Source 區與 Draft／Review 區兩個元件再加功能；拆與不拆都要在本檔說明理由。
- locale：所有新 copy、review status／warning level／finalize 錯誤文案 zh-TW／en parity。
- E2E `apps/web/e2e/p6f-import-review.spec.ts`：DM 建 import → 貼文字 → 建 draft entry → 製造一個 blocking warning → Finalize 被擋 → resolve → Finalize 成功 → Adventures 列表出現新 Adventure → 重整後 import 顯示 finalized 且指向同一 Adventure；Player 看不到 Review／Finalize 控制。

## 契約與輸入

- 設計「REST / UI surface」Importer 段；測試 F.3、F.6。前置：F3。
- 參考檔：`apps/web/src/features/rooms/AdventureImporterPanel.tsx`、`adventureImporterCopy.ts`、`AdventureEntryPayloadFields.tsx`、`RoomAdventuresPage.tsx`、`e2e/p6e-importer-source.spec.ts`。

## 驗收

- vitest：新／改元件測試（parametrize，不超過元件本體兩倍）、locale key parity、Blocking 停用 Finalize、Accept 非必要條件、Player 不渲染控制。
- `npm run build`。
- 指揮者：docker E2E `p6f-import-review.spec.ts`＋`p6e-importer-source.spec.ts`（確認 E5 行為未回歸）。

## 完成紀錄

- **起始與 worker**：2026-09-23，agy（Gemini 3.8 Flash High）1 回合，約 751 秒；worker 只改 `apps/web`，code commit 由指揮者建立。
- **交付**：`AdventureImporterPanel` 拆成協調元件、Source 區、Draft／Review 區與 pure helper；Draft entry 顯示審閱狀態與 Accept／Ignore／Mark uncertain／View source，warnings 依三級分組並可 resolve，questions 可回答，blocking 未解時停用 Finalize；定稿後導向 Adventure editor，重載仍顯示 target Adventure。新增雙語 copy 與 `adventure_import_blocking_warnings` 錯誤文案；`buildDraftMutationInput` 保留 review、warning resolve、question、asset 與 visibility 等既有欄位。
- **拆分決定**：原 `AdventureImporterPanel.tsx` 約 1,242 行，F5 加入多個操作區後拆成 Source／Review 兩個元件與 helper；主面板縮為約 300 行，避免在既有大元件繼續堆疊 UI 與副作用。
- **指揮者審核修正**：修正 source／review sibling key 重複造成兩個 Source 區、切換 import 時表單狀態殘留、Source asset effect 因 inline callback 反覆觸發；Finalize 改用最新 draft revision；E2E 的 suggested check ability／DC 改為後端合法型別、finalized card 依 import 名稱定位；警告的 Code／Entry 標籤補雙語；移除 CSS EOF 多餘空行。
- **測試與證據**：`apps/web` 全套 Vitest 103 檔／767 tests 通過，`npm run build` 通過；`apps/server` 專案 venv 的 `test_p6f_*.py`＋`test_code_quality_gate.py` 通過；Docker E2E `p6f-import-review.spec.ts`＋`p6e-importer-source.spec.ts` **2 passed**，使用受資料庫名稱保護的專用 `adventure_table_e2e` 重置；`git diff --check` 通過。驗證 code commit：`c3c71755`。
- **未解與下一步**：F5 無待修項；下一步辦理 P6-F Subphase 關門 gate／closeout，再進 P6-G。
