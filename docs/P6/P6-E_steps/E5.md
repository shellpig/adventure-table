# E5 — Web UI：Importer Source 區＋最小 Draft editor、zh-TW／en、E2E

## Scope

- `RoomAdventuresPage` 新增 Importer 區（owner／dm 才渲染；Player 不見）：
  - import 列表（name／status／revision）、建立 import；
  - Source：貼文字、上傳 txt／md／pdf／docx、從既有 room asset 選、URL + 貼 content；顯示 source metadata、warnings、chunk 檢視（offset 分頁）；
  - 最小 Draft editor：以 entry 表單新增／編輯／刪除 Draft entry（`entry_kind` 沿 P6-A editor 既有 payload 表單元件，可重用則重用），provenance 固定顯示 `user_explicit`；儲存帶 `expected_revision`，stale → 提示重新載入。
  - **不做** Accept／Ignore／Mark uncertain、warning 分級 UI、Finalize（P6-F）。
- locale：所有新 copy、source kind／status／provenance／warning 文案 zh-TW／en parity。
- E2E `apps/web/e2e/p6e-importer-source.spec.ts`：DM 建 import → 貼文字 → 看 chunk → 編 Draft entry → 重新整理後 entry／revision 保留；Player 進 Adventures 頁看不到 Importer 區。

## 契約與輸入

- 設計「REST / UI surface」Importer 段；測試 E.1、E.4 的 UI 面、locale。前置：E4。
- 參考檔：`apps/web/src/features/rooms/RoomAdventuresPage.tsx`、`AdventureEditorPage.tsx`、locale 檔、`e2e/p6d-stage-and-review.spec.ts`。

## 驗收

- vitest：新元件測試（parametrize，不超過元件兩倍）、locale key parity 測試；`npm run build`。
- 指揮者：docker E2E `p6e-importer-source.spec.ts`。

## 完成紀錄

（待填）
