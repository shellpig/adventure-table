# E1 — migration `0033`＋persistence＋Draft schema v1＋boundary gate

## Scope

- 新增 alembic migration `0033_p6e_adventure_imports.py`：`adventure_imports`、`adventure_import_sources`、`adventure_import_drafts` 三張表，欄位依設計方針 E.1；`status` 限 `source | drafting | review | finalized | cancelled`；`adventure_import_sources.asset_id` 為 nullable FK 指向 P6-A room asset；`adventure_import_drafts.import_id` 為 PK。
- 新增 `app/persistence/adventure_imports/`（`tables.py`、`repository.py`）：import／source／draft 的 CRUD 與 revision-guarded draft update（沿 P6-B runtime repository 的 expected revision 模式）。
- 新增 `app/domain/adventure_imports/schemas.py`：
  - Draft JSON `schema_version = 1`；parsed entry shape 對應 `app/domain/adventures/payloads.py` 的 typed entry payload（`entry_kind` + payload），每個 entry 帶 `entry_id`（stable）、`provenance`（`source_document | user_explicit | user_approximation | ai_generated`）、`source_ref?`（source id + locator）。
  - warnings／questions 帶 stable id、`level`（Info／Warning／Blocking 由 F 使用，E 只定義 enum）、`message`、`entry_id?`。
  - typed view model：`AdventureImport`、`AdventureImportSource`（不含 `normalized_text` 全文）、`AdventureImportDraft`。
- 同 commit 擴充 `tests/test_m03_import_boundary.py`：`FORBIDDEN_MODULE_RE` 加 `adventure_imports?`（現有 `adventures?` 後接 `(?:\.|$)`，不會命中 `adventure_imports`），`EXACT_PROTECTED_MODULES` 視需要補；並加 regex 正向斷言（`app.domain.adventure_imports.service`、`app.persistence.adventure_imports.tables`）。
- 不做 service、不做 extractor、不做 route。

## 契約與輸入

- 規格 P6-E 全段；設計 E.1；測試 E.4（draft／warning id 穩定性）、E.5（boundary）。前置：無。
- 參考檔：`alembic/versions/0032_p6b_campaign_world_runtime.py`（migration 風格）、`app/persistence/campaign_runtime/`（repository 與 revision 模式）、`app/domain/adventures/payloads.py`（entry payload）、`tests/test_p6a_postgres_migration.py`／`test_m03d_schema_parity.py`（parity 測試模式）。

## 驗收

- `tests/test_p6e_import_persistence.py`（parametrize）：三張表 CRUD、status enum 拒絕非法值、draft update expected revision stale 拒絕零副作用、restart（重開 session）後 draft entry／warning id 不變（E.4）。
- schema parity 測試涵蓋 `0033`；`test_m03_import_boundary.py` 對 `adventure_imports` 生效；`test_code_quality_gate.py` 通過。
- 指揮者：P6-A～D regression pytest。

## 完成紀錄

- **起始**：2026-09-22；agy 1 回合、371 秒；conversation `4d0ae896-98f3-4270-8055-a93d35a2596c`。
- **交付**：migration `0033_p6e_adventure_imports`（三張表、status／source_kind CHECK、`(import_id, sha256)` unique、cascade）；`persistence/adventure_imports/`（`tables.py`、`repository.py`：`AdventureImportRepository` 的 create／get／list import、`update_import_status`（revision-guarded）、add／get／list／`find_source_by_sha256` source（list 不回 `normalized_text`、回 `text_length`）、get／`upsert_draft`（expected_revision 0 插入 revision 1，否則 revision-guarded update））；`domain/adventure_imports/`（`errors.py` 五個 error、`schemas.py`：`ImportDraft` schema v1、`DraftEntry` 重用 `parse_entry_payload`、`DraftWarning`／`DraftQuestion` stable id、三個 view model 與 `*_from_stored`）；`test_m03_import_boundary.py` regex 加 `adventure_imports?`；`test_m03d_schema_parity.py` 禁止表清單加三張表。
- **指揮者審核修正**：`upsert_draft` 插入路徑移除多餘的 savepoint／IntegrityError 猜測分支，改為 revision 檢查＋import 存在檢查後直接 insert；`warnings_json` 型別收斂為 `list[dict[str, object]]`；測試檔中段 import 移到頂部、移除兩個未使用 import。
- **測試**：`test_p6e_import_persistence.py`（6）、`test_p6e_import_schemas.py`（7，含 14 entry kind parametrize）、`test_p6e_postgres_migration.py`（4，無 `P4_POSTGRES_URL` 時 skip）。指揮者執行 E1 focused＋parity／heads／boundary／quality gate＋P6-A 全部＋`test_p6b_runtime_repository.py`：**120 passed／8 skipped**；`git diff --check` 通過。
- **未解問題／下一步**：既有 test-ordering 問題（`test_p6b_runtime_repository.py` 與 migration／parity 測試同一 xdist worker 時 `roll_requests → combat_entries` `NoReferencedTableError`），與 E1 無關、在無 P6-E 檔案的組合下同樣重現，全套 pytest 不受影響；未修。接 E2。
