# G2b-2 — Adventure journey：Quick Combat

## Scope

- 延續 G2b-1 同一場次，在 Adventure Current Scene／Stage 背景下用既有 P4 Quick Combat 起戰、先攻、attack／damage／End Combat；核對角色／敵人狀態與 Player secrecy。
- 不加 Tactical geometry，也不把 Combat board 自動升為 Campaign world truth。write-back 留 G2b-3。

## 契約與驗收

- 規格 P6-G G.2；測試指南 G.2、G.6。參考 G1b-1 的 helper pattern、`p4e-quick-combat.spec.ts`。
- 擴充後 spec Docker E2E、P4/P6 受影響 regression、frontend unit／build 通過。

## 完成紀錄

- **起始與 worker**：2026-09-24，agy（Gemini 3.8 Flash High）1 回合，CLI 回報約 442 秒；擴充 Adventure E2E，將重複的 Quick Enemy／End Combat 操作抽至 `support/quickCombat.ts` 並讓 G1 journey 共用。
- **交付**：G2b-1 的 active Adventure Session 接 Quick Combat：DM Start／Quick Enemy、雙方 initiative、Player attack／DM 裁定／真 HP damage、DM End；Stage image 在戰鬥前後仍顯示，無 Tactical grid；驗敵人精確 HP／AC 只在 DM projection，Player 不能 DM adjudication／End，Adventure Definition baseline 與 Current Scene 保留。
- **指揮者審核修正**：無；審核共用 helper 只包 UI 操作及 API response，Combat／secrecy／state 斷言仍留 journey，未建立 test-only production bypass。
- **測試與證據**：指揮者於獨立 Docker E2E DB 跑 `p6g-adventure-journey.spec.ts`＋`p6g-empty-campaign.spec.ts`＋`p4e-quick-combat.spec.ts` **3 passed**；全套 frontend Vitest **103 files／775 tests passed**、`npm run build` 通過；`apps/server` 專案 venv `test_code_quality_gate.py` **2 passed**；`git diff --cached --check` 通過。驗證 code commit：`f7ca324b`。
- **未解與下一步**：本步無未解。G2b-3 在 Combat 已 End、Session 仍 active 位置寫持久 world truth、End／next Session，驗 current truth 與 Adventure baseline 區分。
