# G4 — 真實網頁版 AI agent connector world-state journey

## Scope

- 指揮者使用既有 M04 正式 connector 與真實網頁版 AI agent AI DM，跑 `get_session_context`／相關 P6 context、敘事／正式 Check／Quick Combat、Campaign Fact／Runtime write-back、`wait_for_event`、重連／next Session。
- 在 AT UI／資料庫核對外部 AI 的實際 write 與後續 context；模型文字自述不算證據。
- 不新增網站 LLM client、API key 或 AI-specific authority bypass。

## 契約與驗收

- 規格 P6-G G.2；測試指南 G.4。記錄 connector 授權、tool result、AT state／revision 與 reconnect 證據；Player／stale grant 拒絕仍有效。

## 測試方法（AI 自主主持）

- 依正式流程開新網頁版 AI agent 對話，**只貼大廳產生的 AI Join Kit 全文**（`apps/web/src/features/rooms/AIJoinKit.tsx`）；**不另給工具名稱、entry id、關鍵字或劇情提示**，後續也不逐步指定要呼叫哪個工具。程式更新後只需讓 AI 重新呼叫 `get_session_context` 取得新 briefing，不必重貼 Join Kit。
- Human 只扮演 Player（在 Player UI 行動／擲骰）；指揮者只查 DB／event／UI，不代替 AI 做決定。
- AI 靠 briefing／guide／context 自己想不出下一步時，視為產品缺陷：修程式與契約，不以改寫提示詞補救。逐步口述指令得到的結果不算 G4 證據。

## 發現與處置

- **2026-09-24 第一次嘗試（作廢）**：Session `412d30c9`（grant `c6205321` gen 2，OAuth authorization `89d0b63d`）。AI DM 讀 `get_campaign_context`／`get_scene_context` 只得到 Adventure 名稱、current scene `none`，看不到 Adventure 內任何條目；當時契約只給 `attached_adventure_count`，唯一發現途徑是猜關鍵字搜尋，guide／briefing 也沒有 Adventure／World 主持段落。此外該輪由指揮者逐步口述工具與步驟，違反上列測試方法。已驗到的只有機制（AI DM `roll.requested` seq 4 → Player 自擲 `roll.resolved` seq 5），不列入 G4 證據。
- 處置：見 [G4a](G4a.md)（P6-C 補 Adventure outline 與 AI DM 主持指引），完成後依本檔測試方法以新 Token、新 ChatGPT 對話重跑。
- **2026-09-24 第二次嘗試（進行中）**：舊 Session 已 abandoned；新 Session `12518d5a`（grant `7d7e7dfd` gen 5），只貼 Join Kit。AI 自行 `world_set_current_context` 設 Adventure scene（seq 1，revision 1）、`set_stage_text`、中文 narration；對 Player 的 dialogue 自行選 Investigation／Arcana／Survival `request_check`，皆等 Player 自擲（`roll.resolved` 由 Player Seat、server 擲）後才敘事；dm_only 秘密未外洩；Player 離開大廳時自行寫回 Runtime（seq 19，revision 2：清 current scene、寫 current_situation）。
- 同輪發現兩個指引缺口：(1) Stage／current_situation 首次照抄英文 Adventure 原文，AI 不知道桌面語言；(2) 測試 Adventure 只在 secret 寫「擊敗 guardian 後開門」卻無 guardian／遭遇，AI 忠實停在原地形成 soft-lock，未依規格 〇.7 即興。兩者屬指引缺口而非資料驗證問題，處置見 [G4b](G4b.md)；測試 Adventure 刻意不補 guardian，保留作為即興測試案例。Session `12518d5a` 保留，更新後讓 AI 重新讀 context 續跑（兼驗重連）。
- **G4b 更新後續跑（同 Session `12518d5a`）**：AI 重讀 briefing 後 Stage／寫回改用中文；Player 反覆觸碰符文後 AI 自行即興：`world_create_entry` 建 NPC「符文守衛」（seq 40，dm_notes 註明依 guardian defeated 條件即興，`source_adventure_entry_id` 連回 secret）→ Quick Combat（seq 45 開戰、加入 monster、先攻 Player 18／守衛 9、Player 提請 range 裁定、AI 判 in_range、Player 攻擊 21、AI `monster_outcome_set` dead、seq 55 結束）→ 寫回 NPC 更新、Fact「東側地窖門封印已解除」、runtime Scene「地窖內室」、Item「銀鑰匙」，current context revision 7。觀察（非 blocker）：NPC 在出場前 8 分鐘即以 public 建立；守衛命中即判死、未走 damage；未使用 Adventure override（改以連回 Adventure 的 runtime entries）。
- 同輪發現：AI 嘗試把銀鑰匙 `holder_ref` 設為玩家角色被拒，只得到無 detail 的 `invalid_arguments`。根因：`patch.state` schema 是 open object、description 未寫 state 形狀，AI 只能猜欄位；錯誤又丟棄驗證原因，AI 無法自行修正。處置見 [G4c](G4c.md)。

- **2026-09-25 跨 Session、跨 AI agent 讀回**：Session `12518d5a` 已 ended（82 events）。新 Session `47f0edf6` 由 **Claude 網頁版**擔任 AI DM：舊 grant `7d7e7dfd`（gen 5）與其 OAuth authorization `0445b61c` 於 01:18:30 同時 revoke；新 grant `fab7538c`（gen 7）綁 OAuth authorization `6db3da05`（DCR client redirect `https://claude.ai/api/mcp/auth_callback`；前一 client 為 `chatgpt.com`）。AI 只憑 Join Kit 與 context，在玩家問「之前發生的事」時回顧瞭望塔、東側封印地窖、符文守衛以戰斧擊敗、封印解除、地窖內室銀鑰匙收在行囊、走到石橋村——逐項對上 `campaign_world_mutations` 前一 Session 的寫回，且未洩漏鑰匙用途。同場續寫 runtime Scene「過橋酒館」、NPC「薇拉（兜帽旅人）」（create 後 update），current context revision 10，Session 以 seq 10 `session.ended` 結束。使用者拍板：跨 AI 跨 Session 讀回是更強的證據，connector gate 不限 ChatGPT（措辭改動見 [G4d](G4d.md)）。
- 同日回饋：ChatGPT 與 Claude 網頁版都偶有工具呼叫連續失敗 2–3 次、稍後即恢復；處置見 [G4d](G4d.md)。

## 完成紀錄

- **結論：通過**（2026-09-25）。以下對照 `測試指南.md` G.4；證據皆為 AT 資料庫／event，不以模型自述代替。

| G.4 項目 | 證據 |
|---|---|
| 讀 session context／相關 P6 context | Session `12518d5a`：AI 自行讀 outline，`world_set_current_context` 設 Adventure scene（seq 1，revision 1） |
| 敘事／正式 Check 並觀察結果 | `roll.requested`→`roll.resolved` seq 5/6、9/10、15/16、26/27、75/76；Player Seat 自擲後 AI 才敘事 |
| 開始／參與 Quick Combat | seq 45 `combat.started`→46 entry_added→47–50 先攻→51/52 adjudication→53 attack→54 `monster_outcome_set`→55 `combat.ended` |
| 寫入持久 Campaign Fact／Runtime | `campaign_world_mutations`（`created_by_actor_kind=ai`）：NPC「符文守衛」、Fact「東側地窖門封印已解除」、Scene「地窖內室」「石橋村」、Item「銀鑰匙」（`holder_ref` 指向角色 `060fe572`，G4c 後成功）；runtime context revision 1→9 |
| `wait_for_event` | 使用者於 ChatGPT Web 工具呼叫 UI 觀察到 AI 以 `wait_for_event` 接收 Player 事件；AT 不記錄讀取類呼叫，故為 host UI 觀察證據 |
| 重連／next Session | 同 Session 內程式更新後 AI 重讀 briefing 續跑（G4b／G4c）；跨 Session 由新 grant gen 7 與新 OAuth authorization 接手，舊 family 同時 revoke |
| 回傳改變後的 world | Session `47f0edf6` 回顧逐項對上前一 Session 寫回（見上方 2026-09-25 條目） |
| Player／stale grant 拒絕仍有效 | 本輪 DB：舊 authorization 隨 grant revoke 失效；拒絕行為由既有自動測試鎖定：`test_m04b_oauth_authority_states.py`、`test_p3e_mcp_token_lifecycle.py`、`test_p4e_mcp_combat_lifecycle.py`（`ai_token_unauthorized`） |

- 秘密：兩場 AI DM 均未把 dm_only 內容寫入 Stage 或公開 narration；未說出銀鑰匙用途。
- 非 blocker 觀察（留 G5 已知限制）：NPC 在出場前約 8 分鐘即以 public 建立；守衛命中即判死未走 damage；未使用 Adventure override（以連回 Adventure 的 runtime entries 代替）；wait_for_event 只有 host UI 觀察證據。
- 附帶相容性結果：Claude 網頁版經同一 Tailscale Funnel 入口完成 DCR／OAuth／MCP `2026-07-28` 無狀態呼叫與寫入，補上 M04-A A.8 未量測的 Claude chat 路徑。
