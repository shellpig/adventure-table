# G1b-2 — Empty Campaign：world change、End／next Session

## Scope

- 延續 G1b-1 的同一 empty Campaign journey，更新 Current Situation／Runtime world truth、End Session、建立 next Session；核對 NPC／Fact／Situation 與 Character／Combat 結果仍在正確的 Campaign／Character state。
- 保持 Story optional：不附加 Adventure、不強迫 Scene／Quest。使用既有 Session lifecycle，不製造第二套保存路徑。

## 契約與驗收

- 規格 P6-G G.1；測試指南 G.1、G.6。參考 `e2e/p2f-session-lifecycle.spec.ts`、`p6b-campaign-runtime.spec.ts`、`p4f-full-combat-journey.spec.ts`。
- 新 spec Docker E2E、P2／P4／P6 受影響 regression、frontend unit／build 通過。

## 完成紀錄

- **起始與 worker**：2026-09-24，agy（Gemini 3.8 Flash High）1 回合，CLI 回報約 590 秒；只擴充 `apps/web/e2e/p6g-empty-campaign.spec.ts`，未改 production code／docs。
- **交付**：同一場 Combat 已結束的零 Adventure Campaign，DM UI 更新 Current Situation、Quick Add 事後 Fact、正常 End Session；回 Lobby 保留 Seat／Character，再由 UI Start next Session。第二場 DM Runtime 可見 NPC、兩筆 Fact、Situation；Character HP 12／12 仍在，沒有 active Combat；上一場 `monster-instances` 保留敵人傷後 HP、event 有 `combat.ended`／`world.context_changed`。Player 重連仍只見 Journal 公開 Fact，不見 DM Notes／DM controls，直接 Adventure read 404。Scene／Quest 保持 optional。
- **指揮者審核修正**：無；核對上一場與下一場的 API 讀取 scope、實際 UI End／Start、Player projection，未發現 test-only bypass。
- **測試與證據**：指揮者於獨立 Docker E2E DB 跑 `p6g-empty-campaign.spec.ts`＋`p4e-quick-combat.spec.ts`＋`p6b-campaign-runtime.spec.ts` **4 passed**；全套 frontend Vitest **103 files／775 tests passed**、`npm run build` 通過；`apps/server` 專案 venv `test_code_quality_gate.py` **2 passed**；`git diff --cached --check` 通過。驗證 code commit：`8ea3600e`。
- **未解與下一步**：G.1 empty Campaign 完整 journey 已有 browser 證據，無未解；下一步 G2a Adventure-driven journey。
