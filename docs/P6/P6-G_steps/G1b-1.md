# G1b-1 — Empty Campaign：Quick Combat attack／damage／End

## Scope

- 延續 G1a 的同一 `p6g-empty-campaign.spec.ts` journey，在已解的 Player roll 後進入 Quick Combat；DM 使用現有 P4 流程加入敵人／完成 initiative、Player 或 DM 合法完成一次 attack 與 damage，然後 End Combat。
- 斷言 Combat entry、attack resolution、HP／Character Current State 的實際 AT state；Player 不操作 DM 專屬裁定，隱藏敵人資源不洩漏。
- 本步保持零 Adventure、零 Tactical geometry；Current Situation 與 next Session 留 G1b-2。擴充後 spec 可單獨通過。

## 契約與驗收

- 規格 P6-G G.1；測試指南 G.1 Quick Combat 段、G.6。參考 `e2e/p4e-quick-combat.spec.ts`、`p4f-full-combat-journey.spec.ts` 與 `support/quickCombat.ts`，核對最新 helper／route／DTO。
- 新 spec Docker E2E、相關 P4 regression、frontend unit／build、code quality gate 通過。不改 production combat 以迎合測試；若發現 A～F 真缺口，回報來源。

## 完成紀錄

- **起始與 worker**：2026-09-23～24，agy（Gemini 3.8 Flash High）1 回合，CLI 回報約 707 秒；只擴充 `apps/web/e2e/p6g-empty-campaign.spec.ts`，未改 production code／docs。
- **交付**：在 G1a 同一零 Adventure active Session 接 Quick Combat：DM Start、Quick Enemy、雙方合法 initiative、Player attack／DM range adjudication／damage、DM End Combat；以實際 Combat detail／Character sheet／Table Event 驗 HP 與事件，檢查 Player UI／API 不含敵人精確 HP／AC、不能做 DM 裁定或 End。
- **指揮者審核修正**：無；核對 P4 helper、role-scoped credential、敵人投影與攻擊結果，不見 test-only production bypass。敵人 AC 1 降低隨機 miss，最多 6 回合仍需實際命中／傷害才通過。
- **測試與證據**：指揮者在獨立 Docker E2E DB 跑 `p6g-empty-campaign.spec.ts`＋`p4e-quick-combat.spec.ts` **2 passed**；全套 frontend Vitest **103 files／775 tests passed**、`npm run build` 通過；`apps/server` 專案 venv `test_code_quality_gate.py` **2 passed**；`git diff --cached --check` 通過。驗證 code commit：`31a62256`。
- **未解與下一步**：本步無未解。G1b-2 接在已 End Combat、Session 仍 active 的位置，更新 Current Situation，再 End／Start next Session 並核對 Runtime 與角色／Combat 結果延續。
