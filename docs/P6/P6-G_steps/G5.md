# G5 — Full regression、static audit 與 P6 closeout

## Scope

- 彙整 G1～G4 證據，依測試指南 G.8 核對 requirements／lifecycle／API／DTO／serialization／platform／failure／UI／single-source consistency。
- 跑全套 backend、frontend unit＋build、`docker compose config`、Phase closeout 全套 Docker E2E；核對真 PostgreSQL、Standalone、secrecy、網頁版 AI agent 證據及已知限制。
- 完成 P6 closeout、更新當前簡報／歷史索引；未滿 G contract 不宣稱 Phase 關門。

## 契約與驗收

- 規格與測試指南 P6-G 全部、AGENTS Phase 關門 gate。只用有效的既有證據，不為文件整理無故重跑已通過的檢查。

## 完成紀錄

（G4 與全套 E2E 完成後填 closeout；以下為不會重置 G4 測試 Room 的預先 gate）

- 2026-09-24 指揮者在 `apps/server` 用專案 venv 跑全套 pytest：**2528 passed／78 skipped**、0 failed；PG job 因未帶 test URL skip，G3a 的真 Docker PostgreSQL restart 已單獨通過。
- `apps/web` 全套 Vitest **103 files／775 tests passed**；`npm run build` 通過；`docker compose config --quiet` 通過。
- G1 empty Campaign、G2 Adventure-driven、G3a real restart 與 G3b secrecy 的個別 Docker E2E／focused evidence 已各自記在 step 檔；Phase 關門仍需 G4 真實網頁版 AI agent 與**全套** Docker E2E，不因個別通過而宣稱完成。
