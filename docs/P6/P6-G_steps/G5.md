# G5 — Full regression、static audit 與 P6 closeout

## Scope

- 彙整 G1～G4 證據，依測試指南 G.8 核對 requirements／lifecycle／API／DTO／serialization／platform／failure／UI／single-source consistency。
- 跑全套 backend、frontend unit＋build、`docker compose config`、Phase closeout 全套 Docker E2E；核對真 PostgreSQL、Standalone、secrecy、網頁版 AI agent 證據及已知限制。
- 完成 P6 closeout、更新當前簡報／歷史索引；未滿 G contract 不宣稱 Phase 關門。

## 契約與驗收

- 規格與測試指南 P6-G 全部、AGENTS Phase 關門 gate。只用有效的既有證據，不為文件整理無故重跑已通過的檢查。

## 完成紀錄

**結論：通過（2026-09-25）**，P6 closeout 見 [P6-G closeout](../P6-G_CLOSEOUT.md)。靜態審核發現的 AI context 上限缺口已由 [G5a](G5a.md) 補上。

- **Backend**：G5a 後全套 pytest **2556 passed／78 skipped**、0 failed（先前 G4d 後 2545／78、2026-09-24 預先 gate 2528／78）；PG case 未帶 test URL 而 skip，真 PostgreSQL restart 由 G3a 證明。
- **Frontend／compose**：全套 Vitest **103 files／775 passed**、`npm run build` 通過（G4d 後；G5a 未改 `apps/web`）；`docker compose config --quiet` 通過（其後 compose 未變）。
- **全套 Docker E2E（Codex 執行，無參數）**：
  - 第一次：parallel **101 passed／3 failed／3 skipped**。P1-G Builder 斷言失敗（已知 Builder flake），重啟 `server-e2e` 後單獨重跑通過；M01-M、P6-D 為 worker crash（exit code `3221226505`＝0xC0000409，測試本體未執行）。
  - targeted 補跑 M01-M＋P6-D（`--workers=1`）：P6-D 通過；M01-M 的 `rejects a forged MTF bloodline plus SCAG variant payload` 以 KI-P1D-001 簽章失敗一次（Draft revision 2／Saving…），整檔單獨重跑 **5 passed**。
  - 第二次：parallel **101 passed／3 failed／3 skipped**，三個都是 worker crash（P4-E `p4e-quick-combat`、P6-B `p6b-campaign-runtime`、P6-G `p6g-adventure-journey`），各自 `--workers=1` 重跑 **1／2／1 passed**；M01-M、P1-F、P1-G、P6-D 本趟通過。
  - parallel 非零退出使 script 中止，後續趟次手動補跑：baseline-room **30 passed／1 skipped**；serial-restart **2 passed**。
  - xge-less M03-C 子集（指揮者依 `e2e-docker.mjs` 最後一段手動執行：內容包去掉 `xge` 重啟 E2E 服務、`M03C_E2E_DISABLE_XGE=1`）：`m03c-character-import.spec.ts` **7 passed**；之後還原完整內容包並確認含 `xge`。
  - G5a 修正後重建 `server-e2e`，跑呼叫 AI context 工具的 `m04c-ai-join-kit`、`m05-session-history`、`p3e-mcp-browser-integration`、`p6g-adventure-journey`：**8 passed**。
  - 兩次 parallel 共 **5 次** worker crash，分布在不同 spec，單獨重跑全部通過；登記為 [KI-ENV-002](../../../已知問題.md)。
- **靜態審核**：逐條對照測試指南「P6 closeout blocker」與 G.8，證據對照表見 closeout；唯一缺口為 AI context 自由文字／清單無上限（G5a 已修）。
- **KI-P1D-001 證據**：[error-context](G5_KI-P1D-001_2026-09-25_error-context.md)；判讀與 DB 狀態見已知問題。
