# G2b-1 — Adventure journey：Exploration／formal Check

## Scope

- 延續 G2a 同一 `p6g-adventure-journey.spec.ts` 與 active Session／Stage；DM 根據 Scene 敘事、Player exploration action、DM 正式 Request Check、Player roll，驗證 AT event／result。
- Context 仍 bounded、Adventure baseline／Runtime 不混寫；Player 不可直接讀 Secret／DM Note。此步只到 formal Check，Combat 留 G2b-2。

## 契約與驗收

- 規格 P6-G G.2；測試指南 G.2、G.5。參考 `p3b-exploration-chat-actions.spec.ts`、`p3c-roll-check-pending-action.spec.ts`、`p6g-empty-campaign.spec.ts` 的 production pattern；核對最新 route／DTO／helper。
- 擴充後 spec Docker E2E、P3/P6 受影響 regression、frontend unit／build 通過。

## 完成紀錄

（待填）
