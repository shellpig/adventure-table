# G2b-1 — Adventure journey：Exploration／formal Check

## Scope

- 延續 G2a 同一 `p6g-adventure-journey.spec.ts` 與 active Session／Stage；DM 根據 Scene 敘事、Player exploration action、DM 正式 Request Check、Player roll，驗證 AT event／result。
- Context 仍 bounded、Adventure baseline／Runtime 不混寫；Player 不可直接讀 Secret／DM Note。此步只到 formal Check，Combat 留 G2b-2。

## 契約與驗收

- 規格 P6-G G.2；測試指南 G.2、G.5。參考 `p3b-exploration-chat-actions.spec.ts`、`p3c-roll-check-pending-action.spec.ts`、`p6g-empty-campaign.spec.ts` 的 production pattern；核對最新 route／DTO／helper。
- 擴充後 spec Docker E2E、P3/P6 受影響 regression、frontend unit／build 通過。

## 完成紀錄

- **起始與 worker**：2026-09-24，agy（Gemini 3.8 Flash High）1 回合，CLI 回報約 480 秒；只擴充 `apps/web/e2e/p6g-adventure-journey.spec.ts`。
- **交付**：同一 Adventure Current Scene／Stage 的 active Session，DM UI narration、Player exploration action、DM 正式 Investigation Check（DC 14）、Player formal roll；查 Table Event／roll result，Player 不見 DC 或 Adventure／secret direct-read。Stage image 在雙方畫面繼續顯示。
- **指揮者審核修正**：移除新增的未使用 `Seat` type import，以及與 G2a 重複的三條 secret asset／Adventure direct-read；保留 Player 的後續 Adventure 404 與 DOM secrecy 斷言。
- **測試與證據**：指揮者於獨立 Docker E2E DB 跑 `p6g-adventure-journey.spec.ts`＋`p3c-roll-check-pending-action.spec.ts` **2 passed**；全套 frontend Vitest **103 files／775 tests passed**、`npm run build` 通過；`apps/server` 專案 venv `test_code_quality_gate.py` **2 passed**；`git diff --cached --check` 通過。驗證 code commit：`f3ddd7c7`。
- **未解與下一步**：無未解；G2b-2 在 formal roll resolved 後接 Quick Combat。
