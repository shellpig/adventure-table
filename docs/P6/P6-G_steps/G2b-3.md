# G2b-3 — Adventure journey：持久 write-back／next Session truth

## Scope

- 延續 G2b-2 的 Adventure Session，經既有 P6 World service／Human UI 寫入 Fact／Override，使 Runtime truth 明確不同於 Adventure baseline；End Session、reconnect／next Session。
- 驗新 Session DM／AI context 以 Runtime／Override 為現況，保留原 Adventure Definition baseline；Player 只見 public／own-character projection。重要世界變化不能只存在 narration。

## 契約與驗收

- 規格 P6-G G.2；測試指南 G.2、G.5。參考 P6-B override／context、P6-C context retrieval 與 P6-D world write tests。
- 擴充後 spec Docker E2E、P6 focused regression、frontend unit／build 通過；缺失的 A～F 核心能力回原 Subphase 補。

## 完成紀錄

（待填）
