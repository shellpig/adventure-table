# G2b-3 — Adventure journey：持久 write-back／next Session truth

## Scope

- 延續 G2b-2 的 Adventure Session，經既有 P6 World service／Human UI 寫入 Fact／Override，使 Runtime truth 明確不同於 Adventure baseline；End Session、reconnect／next Session。
- 驗新 Session DM／AI context 以 Runtime／Override 為現況，保留原 Adventure Definition baseline；Player 只見 public／own-character projection。重要世界變化不能只存在 narration。

## 契約與驗收

- 規格 P6-G G.2；測試指南 G.2、G.5。參考 P6-B override／context、P6-C context retrieval 與 P6-D world write tests。
- 擴充後 spec Docker E2E、P6 focused regression、frontend unit／build 通過；缺失的 A～F 核心能力回原 Subphase 補。

## 完成紀錄

- **起始與 worker**：2026-09-24，agy（Gemini 3.8 Flash High）1 回合，CLI 回報約 569 秒；只擴充 `apps/web/e2e/p6g-adventure-journey.spec.ts`，未改 production code／docs。
- **交付**：在 Combat End 後，DM UI 更新 Current Situation、Quick Add public Fact，並經同一 P6 World REST service 寫 Adventure Scene Override；World／Event API 與 Campaign Changes UI 均能讀到新 truth，原 finalized Adventure Scene baseline 未改。正常 End／Start next Session 後 Context、Override、Fact、Character HP 與上一場 Combat outcome 延續；Player 只見公開 Fact／Stage，不能讀 Override／DM context／Adventure 或秘密 asset。
- **指揮者審核修正**：無；核對持久 write-back 先於事後敘事、Override 與 baseline 分離、Human REST 走既有 service、Player 403／404 投影。真 AI DM `get_session_context` 需合法 AI grant，不能在 Human DM journey 偽造，G4 處理。
- **測試與證據**：指揮者於獨立 Docker E2E DB 跑 `p6g-adventure-journey.spec.ts`＋`p6b-campaign-runtime.spec.ts` **3 passed**；全套 frontend Vitest **103 files／775 tests passed**、`npm run build` 通過；`apps/server` 專案 venv `test_code_quality_gate.py` **2 passed**；`git diff --cached --check` 通過。驗證 code commit：`a1994360`。
- **未解與下一步**：G.2 Human/browser journey 已有完整證據；合法 AI DM context／write-back 留 G4。下一步 G3a PostgreSQL restart／reconnect。
