# B4 — REST routes、stable errors、完整 secrecy matrix

## Scope

- 依 Room-first convention 暴露 Runtime entries、overrides、context 的 Human HTTP read/write routes；route 只轉 intent，不能直改 repository。
- pre-session owner/dm 與 active-session current DM authority 對齊 service；Player route 只讀 public + own active Character knowledge。
- stable machine errors 對 revision conflict、not attached、detach blocker、invalid scene、forbidden／not found。
- serialized DTO 直接驗證 secrecy，不以 UI hide 或 `?` 佔位。

## 契約與輸入

- 依賴 B2c/B3a/B3b；P6 設計「REST / UI surface」「Shared authority / projection」；測試 B.4～B.6。

## 驗收

- Current DM：全部；Human Player／AI Player 等價 projection：public + own、無 other/dm-only/dm_notes/Adventure entry。
- 非 participant、wrong Room/Campaign、stale session actor 拒絕且零副作用。

## 完成紀錄

- 待補。
