# B6 — E2E、完整 gate、closeout、合併

## Scope

- 新增 P6-B browser journey：空 Campaign quick add、Adventure override、Current Context、Player Journal secrecy、reload/reconnect、detach blocker。
- 跑 Subphase 關門 gate：全 backend pytest、全 frontend unit/build、`docker compose config`、P6-B 對應 Docker E2E。
- 核對 PostgreSQL migration、Standalone boundary、所有驗收契約與 evidence；建立 closeout，更新 PROJECT_BRIEF／ROADMAP_HISTORY 後合併 main。

## 契約與輸入

- 依賴 B1～B5e；P6 測試指南 P6-B 與 AGENTS 關門 gate。

## 驗收

- B.1～B.6 每條均有可定位證據；無未審 diff／失敗 gate。
- commit、push、merge 均依專案規則完成；不提前宣稱 P6-C 能力。

## 完成紀錄

- **起始**：2026-09-21；Codex 撰寫 `apps/web/e2e/p6b-campaign-runtime.spec.ts` 並執行全部關門 gate，於 E2E 回傳後撞到額度上限；指揮者接手收尾。
- **交付**：兩條 browser journey——(1) Session 外 Override＋Current Scene 同時阻擋 Detach → 清除後 Detach 成功；(2) 空 Campaign active Session 內 DM Quick Add fact、Current Situation、Player Journal secrecy（REST key 集合、零 context／overrides／adventure 請求）、DM／Player reload。
- **Gate**：全套 backend pytest 2011 passed／74 skipped；指揮者補跑 P6-B 真 PostgreSQL 33 passed／0 skipped；vitest 100 files／718 passed；build 成功；`docker compose config` 通過；全套 Docker E2E exit 0（parallel 99／3 skipped、baseline-room 30／1 skipped、serial-restart 1、xge-less 7）。證據與驗收對應見 [P6-B closeout](../P6-B_CLOSEOUT.md)。
- **指揮者審核**：spec 逐行核對 B6 scope 六項；無程式修正。
- **未解問題**：closeout 觀察三點（B.1 Monster／inventory 正向 assertion 缺口、E2E artifacts 只留最後 project、Builder 延遲未重現），不阻擋關門。
