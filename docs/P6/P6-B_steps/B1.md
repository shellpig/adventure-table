# B1 — Runtime schema／migration／Standalone boundary

## Scope

- 新增 `campaign_world_entries`、`campaign_world_entry_characters`、`campaign_adventure_overrides`、`campaign_runtime_context`、`campaign_world_mutations` 的 SQLAlchemy metadata 與 web migration `0032_p6b_campaign_world_runtime`。
- FK／cascade／check／unique／index 對齊 P6-B 契約；Adventure provenance detach 後不得 dangling，override 與 current Adventure scene 必須能成為 detach blocker。
- 新 multiplayer module 同步擴充 M03 import boundary 與 Standalone forbidden-table parity；不得進 character migration track。
- PostgreSQL migration upgrade／downgrade 與 metadata 對照測試。

## 契約與輸入

- P6 設計：「P6-B」「Migration / PostgreSQL / Standalone」「Concurrency / idempotency」。
- 依賴 `0031_p6a_room_assets_adventures` 與既有 Campaign／Character／Adventure／Monster schema。

## 驗收

- focused migration/schema、`test_m03_import_boundary.py`、`test_m03d_schema_parity.py` 通過。
- Standalone schema 不含五張新表；Character／Standalone module 不可 reach 新 runtime module。

## 完成紀錄

- 待補：日期、worker 回合、交付、指揮者修正、測試結果、驗證 commit、未解問題。
