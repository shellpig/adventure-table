# C2 — search_campaign_context：bounded、deterministic、零 side channel

## Scope

- `CampaignContextService.search_campaign_context(actor, query, kinds?, limit?)`：PostgreSQL normalized text／simple indexed matching（SQLite 相容 fallback 供單元測試），deterministic 排序、limit＋pagination、max payload；空字串／全空白 query 拒絕。
- DM：Runtime truth／Override > attached Adventure baseline，結果標示來源與 override 狀態。
- Player／AI Player：只搜 Runtime public＋own-character knowledge；不得因命中 Adventure entry 或 secret 而回傳 id／title／body／source ref／命中數／存在提示。
- 若需要 index／migration，走 `0033`，含真 PostgreSQL upgrade／downgrade test。
- **派工決策（2026-09-21）**：第一版不加 migration／index／新 repo 讀法；search 走既有 `list_entries_in_transaction`＋`_list_adventure_entry_overlays_in_transaction` 在 Python 端 casefold token 比對（title＋body），輸出以 `limit`（預設 20、上限 50）、`offset`、snippet 上限字數與 `has_more` 取代總命中數來 bound。既無 SQL 變更，真 PostgreSQL case 沿用 P6-B repository PG 覆蓋，不另建。同步把 P6-B `ActiveFixture`／`fix` 與 C1 seeded fixture 抽到 `tests/p6_active_fixture.py`，消除跨測試模組 import。

## 契約與輸入

- 設計 C.1 Search 段；測試 C.3、C.4。依賴 C1 的 service 與 projection helper。

## 驗收

- `tests/test_p6c_context_search.py`：大量 entries 下 limit／pagination／payload 受控；空 query 拒絕；DM vs Player 同 query 結果差異；Player 端 count 與 Adventure 命中不可推導；排序 deterministic；真 PostgreSQL case。

## 完成紀錄

- **起始**：2026-09-21；agy 1 回合，959 秒（最終回報後 API 502，status ERROR 但產出完整）；conversation `5f8a94f3-cf67-4a30-a959-71cb645a30c4`。
- **交付**：`CampaignContextService.search_campaign_context(actor, query, kinds=None, limit=None, offset=0)`；`context_schemas.py` 新增 `SEARCH_DEFAULT_LIMIT`＝20／`SEARCH_MAX_LIMIT`＝50／`SEARCH_SNIPPET_MAX_CHARS`＝160、`SearchKind`、`CampaignSearchHitDmView`（`source`／`kind`／`title`／`snippet`／`visibility`／`adventure_id`／`has_override`／`current_truth`）、`CampaignSearchHitPlayerView`（僅 `id`／`kind`／`title`／`snippet`）、`CampaignSearchDmResult`／`PlayerResult`（`query`／`limit`／`offset`／`has_more`／`hits`，無總數）。比對只看 title＋body；非 DM 完全不走 adventure 分支。`tests/p6_active_fixture.py` 承接 P6-B `ActiveFixture`／`active_fix` 與 C1 `context_seeded_fix`／`_scan_str`／`_snapshot`，兩個既有測試改為 import。
- **指揮者審核修正**：(1) 三個 hit builder 各自重複 kinds／match 判斷、兩個 sort key 幾乎相同、DM／Player 分支各自 sort＋page＋wrap → 合併成單一 `_search_sort_key`，match／kinds 過濾提到呼叫端 comprehension，sort／page 只做一次；(2) `visibility: str` 改為 `RuntimeVisibility | AdventureEntryVisibility`；(3) 空 query 檢查改用 `tokens` 為空、unknown kinds 用 set 差集一次回報；(4) 移出的兩個測試檔留下 20＋6 個未用 import（agy 沒清），已清；`RecordingNotifier` 也不再被 p6b 測試引用。
- **測試**：`tests/test_p6c_context_search.py` 10 個 parametrized test（21 cases，280 行）；P6-A／P6-B／P6-C＋code-quality＋M03 boundary＋M03-D schema parity：241 passed／12 skipped（PostgreSQL skip）；`git diff --check` 通過；`context.py` 無 >140 字元行。
- **驗證 commit**：`5c0e4f29`。
- **未解問題／下一步**：搜尋為 Python 端全量比對，campaign 內 entry 數量大時每次 search 都載入全部 runtime entries 與所有 attached adventure overlays；第一版契約允許（bounded 指輸出），若日後成為瓶頸再走 SQL／index。接 C3。
