# C3 — get_session_context compact summary 與五個 MCP read tool

## Scope

- `get_session_context` 加 compact P6 summary／refs：current scene label、current situation、attached adventure count／relevant refs（DM only）、world entry refs、next context tool hints；不含 Adventure entry bodies；`BRIEFING_MAX_CHARS` gate 全綠。
- 註冊 `get_campaign_context`、`get_scene_context`、`search_campaign_context`、`get_world_entry`（player＋dm）、`get_adventure_entry`（dm only）：全部 active-session only；`guide_tool_names.py` `_EXPECTED`、M04 catalog parity、English＋zh-TW description；MCP adapter 只轉 intent。
- Authority lifecycle：AI grant revoke／controller epoch 改變後立即失效；pre-session DM grant 不新增 P6 context。
- **派工決策（2026-09-21）**：沿 P4-C 前例，新增 `app/domain/campaign_runtime/ai_tools.py` `CampaignContextAIToolApplicationService(CombatAIToolApplicationService)`，只轉 `CampaignContextService` intent；`app/mcp/dependencies.py` 改組最外層 facade。`AIToolApplicationService.get_session_context` 的 active 分支加一個 `_active_context_extension(actor) -> dict` hook（base 回 `{}`），P6 facade 覆寫回 `{"campaign_context": …}`：current scene／situation、`attached_adventure_count`（DM only）、world entry refs（上限 20＋truncated 旗標）、`next_context_tools` 靜態 per-role 提示；不改 `ai_guidance.py` briefing 文案（cap 仍 3,000，現用 2,240）。錯誤對映沿用 `call_tool` 既有 base-class 分派（Authority→`permission_denied`、NotFound→`not_found`、Validation／InvalidSceneRef→`invalid_arguments`、SessionNotActive→`table_conflict`），不新增 except 分支。測試沿 `test_p4c_mcp_adapter.py` 的 catalog／dispatch spy／`_actor` override 三段式，配 `tests/p6_active_fixture.py`。

## 契約與輸入

- 規格 P6-C 全段；設計 C.1 `get_session_context` 段、C.2 MCP tool contract；測試 C.1、C.5、C.6。依賴 C1、C2。

## 驗收

- `tests/test_p6c_mcp_tools.py`：catalog／guide parity；role set；active-session only；pre-session 無 P6 context；revoke／epoch 後五個 tool 全拒；briefing cap；Player tool 輸出無 Adventure 欄位。

## 完成紀錄

- **起始**：2026-09-21；agy 1 回合，570 秒；conversation `b12003b1-885c-4ff7-874e-0bd6fef1e49e`。
- **交付**：`app/domain/campaign_runtime/ai_tools.py` `CampaignContextAIToolApplicationService(CombatAIToolApplicationService)`（`campaign_context_service` 注入；五個 tool method 只做 `_actor` → service → `model_dump`）與四個 tool input（`SceneContextToolInput`／`SearchCampaignContextToolInput`／`WorldEntryToolInput`／`AdventureEntryToolInput`）；`AIToolApplicationService._active_context_extension` hook（base 回 `{}`，active 分支 merge）；`campaign_context` key set：DM `current_scene`／`current_situation`／`attached_adventure_count`／`world_entry_refs`（≤ `SESSION_CONTEXT_MAX_WORLD_REFS`＝20）／`world_entry_refs_truncated`／`next_context_tools`，Player 無 `attached_adventure_count`、`next_context_tools` 無 `get_adventure_entry`。`mcp/tools.py` 五個 definition＋`_WHEN_TO_USE`＋dispatch；`guide_tool_names._EXPECTED`；`mcp/dependencies.py` 改組最外層 facade。agy 對 `test_m04c_tool_descriptions.py` 的 pinned catalog 依 hard rule 停手回報，未越界。
- **指揮者審核修正**：(1) `campaign_runtime/__init__.py` 不再 re-export facade／tool input（否則 package import 即拉進整個 combat＋rooms MCP facade 鏈）；測試改從 `campaign_runtime.ai_tools` import；(2) 測試 (3)／(5) 兩段重複的 tool 呼叫 if-chain 抽成 `_invoke`，(2) 以 `_FacadeUnderTest` 取代 `object.__new__` 硬塞 `_actor`；(3) `setup_authority_failure_actor` 的 `"revoked_ai_grant"` 別名移除，統一 `"revoked_ai"`；(4) 抽 helper 後 `test_p6c_context_search.py` 留下的未用 import 清掉（agy 兩步都沒清）；(5) `test_m04c_tool_descriptions.py` `PLAYER_ACTIVE`／`DM_CATALOG` 加入五個名稱；(6) `test_p6a_campaign_adventures.py` 的「MCP tool 名不得含 adventure」guard 放行契約固定的 `get_adventure_entry`（其餘仍禁）。測試 (4) 仍以 `object.__new__` 組 facade 以免建 11 個 combat service，屬已知取捨。
- **測試**：`tests/test_p6c_mcp_tools.py` 6 個 test／34 cases（349 行，facade 158 行——超過「≤ 元件兩倍」約束，主因 `_DispatchSpy` 五個方法與 full-path 測試）；P3-E／P4-C／P4-E MCP、M04 全部、P6 全部、code-quality、M03 boundary／schema parity：418 passed／12 skipped（PG）；`git diff --check` 通過；real composition smoke（`get_ai_tool_application_service` 回 `CampaignContextAIToolApplicationService`）通過。
- **驗證 commit**：`094aeb26`。
- **未解問題／下一步**：`_active_context_extension` 每次 `get_session_context` 多跑一次 `get_campaign_context`（含 party／combat 查詢後丟棄）；briefing 文案未提五個 tool（cap 2,240／3,000，C4 可視需要加一句）。接 C4 gate／closeout。
