# C4 — 完整 gate、closeout、合併

## Scope

- Subphase 關門 gate：全套 backend pytest（含 `P4_POSTGRES_URL` 真 PostgreSQL case 非 skip）、`docker compose config`；P6-C 不動 `apps/web`，依 AGENTS 工程守則第 4 條為 backend-only，不需 E2E。
- 核對 C.1～C.6 每條驗收有可定位證據；建立 `P6-C_CLOSEOUT.md`，更新 PROJECT_BRIEF 後 `--no-ff` 合併 `main`。

## 契約與輸入

- 依賴 C1～C3；P6 測試指南 P6-C 與 AGENTS 關門 gate。

## 驗收

- 無未審 diff／失敗 gate；不提前宣稱 P6-D 能力。

## 完成紀錄

- **起始**：2026-09-21；指揮者自己做。
- **交付**：[P6-C_CLOSEOUT.md](../P6-C_CLOSEOUT.md)（C.1～C.6 驗收對應、gate 結果、技術決策、觀察事項）；補 `controller_epoch` 失效案例（`28bd076e`）補齊 C.5「controller epoch 改變」的直接證據；PROJECT_BRIEF 更新；`--no-ff` 合併 `main`。
- **指揮者審核修正**：無 worker 產出。
- **測試**：全套 backend pytest 2,095 passed／74 skipped（tree `c95edf2c`）；最終 tree P6＋M04-C 子集 319 passed／12 skipped；`docker compose config --quiet` 通過；backend-only，不跑 E2E。真 PostgreSQL case 不適用（P6-C 無 schema／SQL 變更，見 C2 派工決策）。
- **驗證 commit**：見 closeout 與 merge commit。
- **未解問題／下一步**：見 closeout「觀察到但不屬 P6-C 的事項」；接 P6-D。
