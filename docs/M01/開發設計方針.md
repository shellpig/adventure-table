# M01 — 開發設計方針

> Phase：**M01 — Multi-Source Character Content Expansion**  
> 類型：**M Phase（Modification / Maintenance Phase）**  
> 本文件是 M01-A～M01-J 的具體實作契約。M01 必須建立在 P0/P1 已完成的 Content / Character / Builder / Rules / Inventory / Versioning 上，不能為非 SRD 內容另做第二套角色系統。  
> 「完成後必須為真」以 `實作規格.md` 為準；測試證據以 `測試指南.md` 為準。

最後更新：2026-08-30

---

## 1. M01 Subphase 順序

三份 M01 文件固定使用以下名稱與順序：

```text
M01-A — Multi-Source Content Pack Foundation
M01-B — PHB Character Origins & Background Expansion
M01-C — SCAG / GoS Background Expansion
M01-D — VGM Race Expansion
M01-E — SCAG Half-Elf Variant & Grant Replacement
M01-F — VRGR Lineage & Dhampir
M01-G — TCE Artificer Core
M01-H — TCE Artificer Advanced Features & Infusions
M01-I — TCE Magic Items
M01-J — Full M01 Integration & Closeout
```

M01-B 完成後先做第一次真人創角 Gate，但 M01 仍繼續 C～J。

每個 Subphase 只加入自己真正需要的資料模型／module；禁止 M01-A 為了預想 M01-I 建立大而全的 effect DSL，也禁止 M01-H 因為 Artificer feature 提到炮台／守衛就提前建立 P4 Combat entity。

---

## 2. 現有地基與不可破壞契約

M01 開工時應以當下 `main` 真正 codebase 為準再次核對 API 簽名。目前已確認的重要地基：

```text
data/srd5.1/
data/rules/dnd5e-2014/character-builder.json

apps/server/app/content/
├── registry.py
└── schemas.py

apps/server/app/domain/character/
├── schemas.py
└── ...

apps/server/app/domain/character_builder/
├── basics.py
├── choices.py
├── compiler.py
├── equipment.py
├── progression.py
├── schemas.py
├── spellcasting.py
├── structural.py
└── ...

apps/server/app/domain/rules/
apps/server/app/persistence/
apps/server/app/api/

apps/web/src/features/character-builder/
apps/web/src/features/character-sheet/
```

現有重要契約：

- `CharacterBuild` 是 immutable versioned snapshot。
- `CharacterState` 是 mutable live state。
- Builder Draft 保存未完成輸入／選擇，不是假 `CharacterBuild`。
- Builder choices 由 Server authoritative logic 產生。
- Frontend 不複製 D&D progression engine。
- Starting Equipment 只在 Create Confirm 初始化一次 live Inventory。
- Level Up / Build Edit 不重新建立 live Inventory。
- Spell Access / Spellbook / Known / Always Prepared 等長期取得屬 Build。
- Daily Prepared selection 與 resource usage 屬 State。
- Version History 必須保留舊 Build，不做 in-place rewrite。
- Numeric Override 仍只覆 numeric value，不繞過 Structural Validation。

M01 的所有修改都必須延續這些邊界。

---

## 3. 已知 SRD-only 耦合：M01-A 必須先解除

目前 codebase 已確認至少存在以下硬耦合：

### 3.1 `ContentEntry` envelope

`apps/server/app/content/schemas.py` 目前有類似：

```text
key regex -> ^srd5.1:...
source    -> Literal["srd5.1"]
ruleset   -> Literal["dnd5e-2014"]
license   -> Literal["CC-BY-4.0"]
```

`ContentManifest` 同時把：

- id = `srd5.1`
- CC BY metadata
- extraction source / MIT metadata
- P4-A Monster / Beast scope guard

綁在同一個 SRD-specific model。

### 3.2 `ContentRegistry`

目前：

```text
DEFAULT_CONTENT_ROOT = data/srd5.1
ContentRegistry.from_directory(root)
```

並且：

- expected key 寫死 `srd5.1:<kind>:<index>`。
- `resolve(kind, index)` 隱含 `srd5.1`。
- cross-reference 從 `/api/2014/...` 推導 `srd5.1:*`。

### 3.3 Character StableKey validator

`apps/server/app/domain/character/schemas.py` 的 `require_stable_key()` 目前要求第一段必須是 `srd5.1`。

### 3.4 Builder reference normalization

`basics.py`、`structural.py` 等目前各自存在 `_stable_key()` 類邏輯：

```text
/api/2014/<route>/<index>
→ srd5.1:<kind>:<index>
```

這會讓新 pack 只能靠 fake URL 才能運作。

### 3.5 Class lookup / multiclass path

目前部分 Builder code使用：

```text
entry.key.startswith("srd5.1:class:")
```

或：

```text
srd5.1:level:<class-index>-<n>
```

Artificer 是 `tce:class:artificer`，因此這些 source assumptions 必須消失。

### 3.6 Build provenance

`CharacterBuild.content_sources` 已存在，預設：

```text
("srd5.1",)
```

但目前 compiler 建 candidate 時沒有真正 derive 多來源 provenance。

### 3.7 Spellcasting contribution

`data/rules/dnd5e-2014/character-builder.json` 目前用：

```text
slot_contribution = full | half | none
```

無法區分：

```text
Paladin / Ranger half → floor
Artificer half → ceil
```

此項在 M01-G 才正式泛化；M01-A 不提前做 Artificer規則。

---

## 4. 全 M01 共通 Content Pack 契約

### 4.1 Directory layout

正式方向：

```text
data/
├── srd5.1/
├── phb2014/
├── scag/
├── gos/
├── vgm/
├── vrgr/
├── tce/
└── rules/
    └── dnd5e-2014/
```

`data/rules/dnd5e-2014/` 是 ruleset-level computation config；它不是某本書的 Content Pack。

### 4.2 Pack IDs

M01 固定使用：

```text
srd5.1
phb2014
scag
gos
vgm
vrgr
tce
```

Pack ID lexical contract：

```regex
^[a-z0-9][a-z0-9.-]*$
```

禁止 `:`，因 StableKey 使用 colon 分隔。

### 4.3 StableKey

正式格式：

```text
<pack-id>:<kind>:<index>
```

建議集中 parser，而不是各 module 自己 `split(":", 2)`：

```python
@dataclass(frozen=True)
class ParsedStableKey:
    source: str
    kind: str
    index: str
```

Schema-level：

- 驗三段 syntax。
- 驗 caller 要求的 kind。

Registry/service-level：

- 驗 pack 是否 installed。
- 驗完整 key 是否存在。
- 驗 cross-reference target kind。

Pydantic schema本身不要依賴 global registry singleton。

### 4.4 新 Content Reference shape

新的 non-SRD normalized data優先使用 explicit stable ref：

```json
{
  "key": "srd5.1:skill:stealth",
  "name": "Stealth"
}
```

可選擇附：

```json
{
  "key": "srd5.1:skill:stealth",
  "index": "stealth",
  "name": "Stealth"
}
```

identity以 `key` 為準。

### 4.5 Legacy SRD Reference compatibility

現有 SRD仍可能是：

```json
{
  "index": "stealth",
  "name": "Stealth",
  "url": "/api/2014/skills/stealth"
}
```

建立一個共用 helper，例如：

```text
reference_to_stable_key(reference)
```

行為：

1. 有 explicit `key` → parse + validate。
2. 沒有 key，但符合 legacy SRD `index + /api/2014/...` → normalize成原 `srd5.1:*`。
3. 其他 shape → invalid；不要猜來源。

`basics.py`、`structural.py`、`equipment.py`、`progression.py`、`spellcasting.py` 等全部共用；不能每個 module各寫一套 URL parser。

### 4.6 Generic `ContentEntry`

M01-A後至少：

```text
key
index
name
source
ruleset
data
license?       # backward-compatible / entry override only
provenance?    # only when entry-level override is actually needed
```

具體修改：

- `key` regex不再寫死 `srd5.1`。
- `source`從 `Literal["srd5.1"]`改成 generic pack id。
- `ruleset` M01仍可限定 `dnd5e-2014`，但不得因 source不同失敗。
- `license`不再每筆要求 `CC-BY-4.0`；既有 SRD entry若仍帶該欄位必須可原樣載入。

來源／license / extraction主要住 pack manifest；entry-level metadata只在真正需要 override時存在。

### 4.7 Generic Manifest — 現有 model 的明確 migration 契約

M01-A 不得只新增新 manifest 範例後寫「依現有 model 最小修改」。現行 `ContentManifest` / supporting models 的 SRD-only 欄位要按以下方式泛化。

#### `ContentManifest.id`

現有：

```text
Literal["srd5.1"]
```

改成 generic Pack ID string，套 4.2 的 lexical validator。

#### `ContentManifest.license` / `ManifestLicense`

現有 `license.spdx` 鎖死 `CC-BY-4.0`。

改成：

```text
license: ManifestLicense | None = None
```

有 `license` object 時仍要求 object內欄位完整；但 `spdx` / identifier改為 generic non-empty string，不鎖死 CC-BY-4.0。手動整理的 private pack可以整個省略 `license`，不得偽造授權資料。

既有 SRD manifest完整 CC-BY metadata必須原樣通過。

#### `ContentManifest.extraction` / `ExtractionSource`

現有 `extraction` required，`commit`要求 40-hex，`license`鎖死 MIT。這是 P0 SRD extraction provenance，不是 generic pack requirement。

改成：

```text
extraction: ExtractionSource | None = None
```

手工整理的 PHB/SCAG/GoS/VGM/VRGR/TCE pack不提供 extraction也合法。若某 pack真的由 Git repository extraction產生，`commit`可繼續要求 40-hex；但 `ExtractionSource.license`改為 generic non-empty string，不鎖死 MIT。

Generic manifest另保留／新增 `provenance`（或等價欄位），讓手工 pack可以誠實表示：

```text
source_type = user-supplied-private-reference
reference_documents = [...]
```

#### `ContentManifest.scope_guard` / `ScopeGuard`

現有 model強制：

```text
excluded_categories包含 monsters + beasts
deferred_to = P4-A
```

這是 P0 SRD scope-cut，不是 generic pack contract。

改成：

```text
scope_guard: ScopeGuard | None = None
```

並把 monsters/beasts + P4-A 的驗證限制在 `srd5.1` 的 SRD-specific compatibility validator／metadata；非 SRD pack沒有 `scope_guard` 必須合法。

#### `ManifestCategory.upstream_file`

現有 required，改成：

```text
upstream_file: str | None = None
```

只有真有 upstream file provenance才填；手工 pack不偽造。

#### `ManifestCategory.count` / `ContentManifest.total_entries`

維持 required與 strict consistency：

```text
count >= 0
total_entries == sum(categories.count)
```

M01仍要 deterministic completeness / fail-fast。

#### Generic manifest shape

優先沿用現有 `name`，不要為了範例同時維護 `name` + `display_name` 兩個重複欄位：

```json
{
  "id": "phb2014",
  "name": "Player's Handbook 2014",
  "ruleset": "dnd5e-2014",
  "version": "local-1",
  "provenance": {
    "source_type": "user-supplied-private-reference",
    "reference_documents": [
      "docs/暫用規則資訊/背景_PHB.md"
    ]
  },
  "usage": {
    "visibility": "private-project"
  },
  "categories": [
    {
      "name": "backgrounds",
      "kind": "background",
      "file": "backgrounds.json",
      "count": 18
    }
  ],
  "total_entries": 18
}
```

SRD-specific CC BY / extraction / scope guard可以繼續存在，但只能是 optional/SRD-specific metadata，不能逼 private pack偽裝成 SRD。

### 4.8 Registry indexes

至少維護：

```text
by_key: dict[StableKey, ContentEntry]
by_kind: dict[kind, tuple[ContentEntry, ...]]
by_source: dict[source, ContentPack]
by_source_kind: dict[(source, kind), tuple[ContentEntry, ...]]
```

Duplicate policy：

```text
same full StableKey → fatal
same kind:index but different source → legal
same display name → legal
```

### 4.9 Installed pack / fixture isolation policy

Private project不做 pack manager UI。

**正式定案：production/dev 使用 explicit deterministic enabled pack list，不掃描 `data/` 所有 child directories自動啟用。**

例如：

```text
DEFAULT_CONTENT_PACKS = (...)
```

或等價 config。

Registry / loader同時必須可注入：

```text
content_root
enabled_pack_ids
```

或等價 dependencies。測試用 `testpack` / `pack-a` / `pack-b` / `future` 放 tests專用 fixture root，由測試明確 enable；不得放 production `data/`後依賴命名規則或掃描排除。

每個 Subphase只有在 pack正式資料完成後才加入 default enabled list。M01-A不可先把不存在或未完成的 `tce/`等資料夾當 mandatory startup dependency。

### 4.10 `content_sources` canonical semantics

`CharacterBuild.content_sources` = **Build實際引用到的 pack集合**。

Compiler完成 candidate後收集：

- race / subrace。
- race variant / lineage（後續有時）。
- background。
- class progression / subclasses。
- features / feats。
- spell access sources。
- starting equipment references。
- 其他 persisted Build refs。

然後：

```text
dedupe
→ deterministic sort
→ CharacterBuild.content_sources
```

如果 Builder要控制「此 Draft可選哪些來源」，用獨立：

```text
enabled_content_sources
```

或 server policy。

**`content_sources` 絕不可作為 selection allowlist / permission gate。** 正確順序是：

```text
allowed-source policy
→ emit / validate selectable refs
→ compile final refs
→ derive content_sources provenance
```

actual provenance不能反過來決定自己是否允許。

### 4.11 Existing JSONB compatibility

新 optional fields一律有 backward-safe default。

原則：

- 不因新增 optional JSON field bulk rewrite所有舊 Build。
- old `content_sources=["srd5.1"]`合法。
- old Inventory沒有 `attuned`時 default false。
- old Build沒有 variant / lineage時 default null。

---

# M01-A — Multi-Source Content Pack Foundation

## A.1 主要修改面

開工時先 grep並核對最新 signatures：

```text
srd5.1:
DEFAULT_CONTENT_ROOT
/api/2014/
resolve(
startswith("srd5.1:class:")
level:
```

優先涉及：

```text
apps/server/app/content/schemas.py
apps/server/app/content/registry.py
apps/server/app/domain/character/schemas.py
apps/server/app/domain/character_builder/basics.py
apps/server/app/domain/character_builder/structural.py
apps/server/app/domain/character_builder/progression.py
apps/server/app/domain/character_builder/equipment.py
apps/server/app/domain/character_builder/spellcasting.py
apps/server/app/domain/character_builder/compiler.py
reference APIs / frontend reference DTOs
```

Known SRD anomaly corrections可保留 `srd5.1:*` hardcode，因為它們真的只針對特定 SRD source anomaly；要移除的是 generic runtime假設。

## A.2 StableKind策略

A只泛化現有 kinds。

新 kinds在真正需要時加入：

```text
M01-B → race-variant
M01-F → lineage
M01-H → infusion（若 feature subtype不足）
```

Variant Human與 M01-E Half-Elf variants共用 B建立的 `race-variant`，不得到 E才再定第二套 identity。

## A.3 Multi-pack loader

採 explicit enabled packs + injectable root；具體 class/function名稱可按現有 code調整，例如：

```text
ContentRegistry.from_directories([...])
```

或：

```text
ContentPackLoader(content_root, enabled_pack_ids)
→ ContentRegistry
```

重點：

- 只載 explicit enabled packs。
- 每個 manifest各自 validation。
- 合併 entries後做 global StableKey uniqueness。
- cross-pack reference validation要在所有指定 packs load完成後執行。
- tests可注入 fixture root與fixture pack list。

不要逐 pack load時就因 target pack還沒讀到而錯判 dangling，也不要掃描所有子資料夾自動 enable。

## A.4 Cross-reference validation

Reference walker只處理明確 reference shape：

- explicit `key` reference。
- legacy SRD `index + url` reference。

不要看到任意 dict有 `key`字串就當 content ref。

必要時建立 typed `ContentReference` model。

## A.5 Source-aware class level lookup foundation

將：

```text
srd5.1:class:fighter
→ srd5.1:level:fighter-5
```

泛化成：

```text
<class source>:level:<class index>-<n>
```

或由 class data提供 explicit level refs。

A不要求重寫 SRD levels格式，只要求 helper不再固定 `srd5.1`。

## A.6 Multiclass option path

目前 compiler / progression中的：

```text
entry.key.startswith("srd5.1:class:")
```

應改成 StableKey kind判断：

```text
parsed.kind == "class"
```

是否為可用 class由 Registry / content policy判斷，不由 source prefix判斷。

## A.7 Registry query API

保留：

```text
get(full_key)
get_optional(full_key)
list_kind(kind)
```

把模糊：

```text
resolve(kind, index)
```

改成：

```text
resolve(source, kind, index)
```

或要求 caller直接用 full key。

新 code不得用 bare `kind:index`做 lookup。

## A.8 Source-aware reference API / UI

ReferenceDTO至少增加：

```text
source_id
source_label
```

Selector：

```text
Wood Elf · PHB
Goblin · VGM
```

不需要建立 Book tab。

## A.9 Fixture packs

A的 foundation tests用 tiny fixture packs，不用正式 PHB當唯一驗證。

所有 fixture packs放 tests專用 content root，透過 injected `content_root + enabled_pack_ids`載入。

需涵蓋：

- same index different source。
- cross-pack ref。
- wrong-kind ref。
- duplicate full key。
- invalid manifest。
- unlisted fixture directory不會自行進 registry。

## A.10 M01-A migration policy

如果 DB沒有 source enum constraint，A可能完全不需要 Alembic。

若有 relational constraint：只做必要 generalization。

不 rewrite舊 JSONB StableKeys。

---

# M01-B — PHB Character Origins & Background Expansion

## B.1 Pack layout

建議：

```text
data/phb2014/
├── manifest.json
├── backgrounds.json
├── subraces.json
├── race-variants.json
├── traits.json         # 需要 reusable racial traits時
├── features.json       # background / racial feature需要時
└── ...
```

不要為形式建立空 category files。

B正式新增 `race-variant` StableKind，供 Variant Human與 M01-E Half-Elf variants共用。

## B.2 Background schema

現有 `BackgroundData`至少已有：

```text
starting_proficiencies
starting_equipment
```

B要把 Builder實際會用的欄位正式 typed / normalized：

```text
starting_proficiency_options
language_options
tool_options
starting_equipment_options
feature / feature_refs
variant_of optional
roleplay_suggestions optional
```

能沿用 P1 canonical option shape就沿用，不建立 `PhbBackgroundChoice`第二套 engine。

## B.3 Roleplay suggestions

建議：

```json
"roleplay_suggestions": {
  "personality_traits": ["..."],
  "ideals": ["..."],
  "bonds": ["..."],
  "flaws": ["..."]
}
```

Frontend把點選結果寫入 existing `roleplay_profile`；仍可手動修改或留空。

## B.4 PHB subrace parent refs

例如：

```json
{
  "key": "phb2014:subrace:wood-elf",
  "race": {"key": "srd5.1:race:elf"}
}
```

Builder subrace filtering依 parent full key；不能要求 subrace與 race同 source。

## B.5 Base + subrace composition

不要把 SRD Elf / Dwarf / Halfling / Gnome全部 grants複製到 PHB subrace entry。

Compiler仍做：

```text
base race grants
+
selected subrace grants
→ resolved origin grants
```

避免 base source日後修正時兩份漂移。

## B.6 Variant Human representation — 正式定案

Variant Human不假裝成 subrace，也不建立成獨立 parent race。

Content identity：

```text
phb2014:race-variant:variant-human
```

並在 data保存：

```text
base_race_ref = srd5.1:race:human
```

`CharacterBuild` 在 M01-B 增加：

```python
race_variant_ref: StableKey | None = None
```

validator要求 kind=`race-variant`。

Final Build：

```text
race_ref         = srd5.1:race:human
subrace_ref      = None
race_variant_ref = phb2014:race-variant:variant-human
```

Legacy compatibility：

- 舊 P0/P1 Build JSON沒有 `race_variant_ref`時由 default `None`載入。
- 不 bulk rewrite `character_versions.build_payload`。
- 舊 immutable Version保持原 payload；新 Build才持久化新欄位。

Builder Draft新增或延伸 typed variant selection，例如：

```python
race_variant_selection: BuilderReferenceSelection | None = None
```

Patch DTO、Review DTO、Create/Build Edit seed同步。M01-E直接沿用，不再新增 Half-Elf-specific variant欄位。

## B.7 Variant Human choices

Server-generated choices：

```text
Ability Bonus: choose 2 distinct abilities, +1 each
Skill: choose exactly 1 skill
Feat: choose exactly 1 eligible feat
```

Ability choices要：

```text
choose_count=2
allow_duplicates=false
category=ability_bonus
bonus=1
```

Feat options來自：

```text
registry.list_kind("feat")
```

並套 existing `feat_failure_reason()` / structural prerequisite logic。

Compile順序：

```text
resolve Human base grants
→ validate race_variant belongs to Human
→ remove/replace standard Human ability package using B-specific variant data path
→ apply +1/+1, skill, feat choices
→ preserve Human grants not replaced
```

B只做 Variant Human需要的最小 variant handling；E才抽 generic Grant Replacement primitive。

## B.8 Racial spell access

Existing `SourceType`已有 `race`，優先沿用。

Drow / Forest Gnome spell access：

- `source_key`指 race/subrace feature identity。
- spell identity用正式 StableKey。
- level gate由 Character Level derive。
- once-per-rest racial casting不是 normal spell slot。

必要時在 feature metadata保存：

```text
casting_ability
uses
recharge
uses_spell_slot=false
```

## B.9 Background Starting Equipment

沿用 P1：

```text
starting_equipment_choices
compile_starting_equipment()
```

不能把 Equipment branch塞回 generic `choice_selections`。

Create Confirm初始化 live Inventory一次；Build Edit / Level Up不重建。

## B.10 Builder UI

Origin step延續 single wizard：

- searchable Race / Subrace / Background。
- Race Variant在適用 parent race時以 server-driven option/branch呈現。
- secondary source label。
- server-provided choices。
- roleplay suggestions optional。
- source badge只輔助辨識。

不新增 PHB-only頁面。

---

# M01-C — SCAG / GoS Background Expansion

## C.1 Pack layout

```text
data/scag/
├── manifest.json
├── backgrounds.json
└── optional features / roleplay tables

data/gos/
├── manifest.json
├── backgrounds.json
└── optional features / roleplay tables
```

## C.2 Roleplay inheritance != mechanical inheritance

SCAG supplied資料有背景寫：

```text
使用 PHB Soldier / Guild Artisan / Sage 等表格作為建議
```

建議 normalized：

```json
"roleplay_suggestions": {
  "inherits_from": "phb2014:background:soldier",
  "guidance": "..."
}
```

Resolver只讀 target的：

```text
personality_traits
ideals
bonds
flaws
```

**不得連同 target Background的 skill / tool / equipment / feature一起繼承。**

## C.3 Background variant identity

如果 supplied Background本身就是官方 variant，優先讓它有完整 `background` StableKey，再用：

```text
variant_of: StableKey | None
```

原因：P1 CharacterBuild已有單一 `background_ref`，Version History也容易表達。

只有「同一 Background內選一個 mechanical branch」才放 generic choices。

## C.4 Flavor table handling

GoS Fisher Tale、Marine Hardship等：

- 可作 optional roleplay/custom fields。
- 不屬 Structural Validation。
- 不填可 Confirm。

不要為这些表格新增 Ship / Fishing / Faction system。

## C.5 Currency / special equipment

Source若寫「pouch with X gp」：

- 若現有 CharacterState已有 currency欄位，接到既有 state contract。
- 若沒有，不在 C建立完整 economy subsystem。
- 但 normalized source data要保留 quantity/value/provenance，不可直接丟失。

實作時以 P1既有 starting equipment policy為準。

## C.6 Source collision

同名/近似名 Background：

- 全 StableKey可共存。
- selector顯示 source。
- persisted `background_ref`是 full key。

不做 display name dedupe。

---

# M01-D — VGM Race Expansion

## D.1 Pack layout

```text
data/vgm/
├── manifest.json
├── races.json
├── subraces.json
├── traits.json
└── features.json
```

正式 identities至少：

```text
vgm:race:goblin
vgm:race:hobgoblin
vgm:race:aasimar
vgm:subrace:protector-aasimar
vgm:subrace:scourge-aasimar
vgm:subrace:fallen-aasimar
```

## D.2 Race rule representation

現有 `RaceData`若不足，優先透過 Trait/Feature entries承載複雜規則，只對真正需要 query / compile的欄位加 typed schema。

M01-D常見需要：

```text
size / size_options
movement modes
resistances
racial spellcasting
level_gated_features
limited-use resource metadata
```

不要把全部東西塞進 untyped `data`後在 compiler用 dict magic string。

## D.3 Generic limited-use resource descriptor

建議 feature-level metadata：

```json
"resource": {
  "capacity": {"type": "fixed", "value": 1},
  "recharge": ["short_rest", "long_rest"]
}
```

或：

```json
"capacity": {"type": "proficiency_bonus"}
```

M01-D只需要 derive / initialize state；正式 Rest auto-recovery留後續 Rest substrate。

Resource ID deterministic：

```text
feature:<feature-stable-key>
```

不用 display name。

## D.4 Goblin

Race entry + feature refs。

Fury of the Small metadata至少：

```text
trigger=damage target larger than self
bonus=character_level
uses=fixed 1
recharge=short_rest,long_rest
```

Nimble Escape metadata：

```text
bonus_action options=disengage,hide
```

不在 M01-D實作 turn/action economy。

## D.5 Hobgoblin weapon choice

Martial Training：

```text
Light Armor automatic
choose 2 Martial Weapons
```

武器 option應從 Registry / proficiency category filter產生，而不是 hardcode幾把武器。

Saving Face metadata保存 max +5 / ally count / recharge；30 ft spatial count等待 runtime。

## D.6 Aasimar parent/subrace

Parent：

```text
vgm:race:aasimar
```

Subraces：

```text
vgm:subrace:protector-aasimar
vgm:subrace:scourge-aasimar
vgm:subrace:fallen-aasimar
```

Parent `subraces` refs指三者。

Builder選 Aasimar時需要 exactly one subrace；不能以 parent-only完成 Confirm。Subrace filtering依 full parent key，其他 race的 subrace不能誤掛。

Parent只承載共同 grants；三個 subrace只承載自己的 ASI / feature差異，不複製整份 parent package。

## D.7 Aasimar level-gated feature

三個 subrace都要各自有 Character Level 3 threshold：

```text
Protector → Radiant Soul
Scourge   → Radiant Consumption
Fallen    → Necrotic Shroud
```

例如：

```json
{
  "key": "vgm:feature:radiant-soul",
  "minimum_character_level": 3,
  "resource": {
    "capacity": {"type": "fixed", "value": 1},
    "recharge": ["long_rest"]
  }
}
```

是 Character Level gate，不是 class level。

Direct high-level Create與 Level Up跨 Lv3要走同一 eligibility resolver。

## D.8 Aasimar Light cantrip

Use racial spell source：

```text
source_type=race
source_key=vgm:feature:light-bearer
casting_ability=charisma
```

不建立 fake class spell profile。

## D.9 Aasimar subrace swap

使用 existing Build Edit / Correction seed current Build，換 `subrace_ref`與derived grants，Confirm產生 Version N+1。

不新增 Aasimar-specific transformation persistence。

---

# M01-E — SCAG Half-Elf Variant & Grant Replacement

## E.1 `race-variant` identity沿用 M01-B

`race-variant` StableKind已由 M01-B為 Variant Human建立。E只增加 SCAG entries，不再第二次改 identity model。

E仍需確認 generic reference endpoints / manifest category validation / Builder validation已接受：

```text
race-variant
```

Variant Human不 retro-fit；它本來就已使用：

```text
race_ref = srd5.1:race:human
race_variant_ref = phb2014:race-variant:variant-human
```

## E.2 `RaceVariantData`

建議最小 schema：

```text
index
name
base_race_ref
replacement_rules[]
choices[]
level_gated_grants[] optional
```

Replacement rule至少：

```text
target_grant_id
action=remove
replacement_group_id
```

不要用 display string `"Skill Versatility"`比對。

## E.3 Stable grant identity

如果 SRD Half-Elf的 Skill Versatility目前只存在 `starting_proficiency_options`，沒有 content feature StableKey，compiler需建立 deterministic grant identity，例如：

```text
grant:srd5.1:race:half-elf:skill-versatility
```

這是 compiler grant identity，不一定要變成 external ContentEntry；但 replacement target必須穩定、可測、可 provenance。

## E.4 BuilderDraft

沿用 M01-B 已加入的 typed variant selection：

```python
race_variant_selection: BuilderReferenceSelection | None = None
```

E只需讓該欄位可指向 SCAG Half-Elf variants，並確保 Build Edit從 `race_variant_ref`正確回填。

## E.5 CharacterBuild

沿用 M01-B 已加入的：

```python
race_variant_ref: StableKey | None = None
```

validator仍要求 kind=`race-variant`。E不得新增 `half_elf_variant_ref`等專用欄位。

Final Build：

```text
race_ref = srd5.1:race:half-elf
race_variant_ref = scag:race-variant:<descent>
```

## E.6 Compile order

固定順序：

```text
base race grants
→ subrace grants
→ validate variant belongs to selected base race
→ identify variant replacement branch
→ remove target grant(s)
→ compile selected replacement option
→ apply level-gated variant grants
→ dedupe
→ structural validation
→ final Build
```

被 remove的 Skill Versatility兩個skill不得先偷偷進 final Build後只隱藏 UI。

## E.7 Four descent option sets

### Moon / Sun

- Wizard cantrip：filter installed spells where level=0 + Wizard access。
- Elf Weapon Training。
- Keen Senses。

### Wood

- Fleet of Foot。
- Mask of the Wild。
- Elf Weapon Training。
- Keen Senses。

### Aquatic

- swim speed 30。
- Keen Senses。

### Drow

- Drow Magic。
- Keen Senses。

另保留標準 Skill Versatility branch。

## E.8 Movement modes

若 Character Rules目前只有單一 Speed，E需要最小 structural expansion：

```text
walking_speed
optional swim_speed
optional climb_speed
optional fly_speed
```

E至少需要：

- Wood walking 35。
- Aquatic swim 30。

不能把 swim 30塞成 generic `speed=30`導致語意錯誤。

Character Sheet顯示 secondary movement speeds。

## E.9 Drow Magic

Use race-variant feature source identity。

Faerie Fire / Darkness若 installed spell content不存在，不能 dangling；需在對應 pack補足可合法 reference的 spell data或把該 content gap blocking，不能只存文字名稱假裝已完成。

## E.10 Stale branch policy

Draft可選擇：

- 保留 stale child values方便切回；或
- 切 branch時清理。

無論哪個方案，**compiler / Review / Confirm只讀 active branch**。

---

# M01-F — VRGR Lineage & Dhampir

## F.1 新 StableKind

```text
lineage
```

Pack：

```text
data/vrgr/
├── manifest.json
├── lineages.json
└── features.json
```

## F.2 CharacterBuild model

推薦最小：

```python
class AncestralLegacySelection(FrozenModel):
    retained_skill_refs: tuple[StableKey, ...] = ()
    retained_movement_modes: tuple[...] = ()

class CharacterBuild(...):
    lineage_ref: StableKey | None = None
    ancestral_origin_ref: StableKey | None = None
    ancestral_legacy: AncestralLegacySelection | None = None
```

`ancestral_origin_ref`可以等於原 `race_ref`；保留 explicit field是為了 Version History / migration / future readability。

不要把整份 old race JSON copy進新 Build。

## F.3 Active mechanics semantics

推薦：

- `race_ref`仍保存原 ancestry / P1 compatibility。
- `lineage_ref != None`時，Lineage成為 active race-package mechanics。
- 原 race所有 grants預設不套用。
- 只有 `ancestral_legacy` whitelist明確帶回的項目加入新 Build。

Character Sheet可顯示：

```text
Lineage: Dhampir
Ancestry: <original race>  # 有時
```

不要讓使用者以為 old Race仍完整生效。

## F.4 BuilderDraft

至少有 typed lineage identity，其他 selection可重用 generic choice：

```text
lineage_selection
ancestral_origin_ref / seeded origin
ability branch choice
size choice
ancestral legacy choice
```

不要把 lineage identity藏在 generic `choice_selections`，否則 Version History難以解釋。

## F.5 Direct Create

Direct Create Dhampir：

- 沒有 ancestral origin可繼承時，`ancestral_origin_ref`可 null或依 chosen ancestry policy保存。
- Ancestral Legacy branch生成 choose 2 skills。
- language依 direct-create規則。

## F.6 Existing transformation

重用 P1 `BUILD_EDIT`：

```text
base_version_id
→ seed old Build
→ select lineage
→ derive eligible ancestral legacy
→ review
→ confirm Version N+1
```

不要新增「Transform Character」第二套 persistence service；UI日後可以只是同一 Build Edit的 shortcut。

## F.7 Provenance of racial skills

最大陷阱：P1 final Build可能只有整體：

```text
skill_choices / proficiencies
```

其中同時包含 race / background / class來源。

F不能直接把所有已有技能都列為可保留。

解法優先級：

1. 使用 Builder choice / grant source_ref provenance。
2. 從 old race content重新 derive racial grant候選，再與 Build交集。
3. 必要時在 M01-F開始把 compiled grants保存更明確 provenance，但不要 destructive rewrite舊 Build。

Ancestral Legacy options只列原 race來源的 eligible skill。

## F.8 Movement retention

只保留：

```text
climb
fly
swim
```

不保留 old walking speed。

Dhampir自己提供：

```text
walk 35
climb 35
```

若 old race有 fly/swim，加入 secondary movement modes。

## F.9 Ability branch

Generic choice要能表達：

```text
Branch A: +2 one + +1 a different one
Branch B: +1 three distinct
```

distinctness由 server validate。

## F.10 Vampiric Bite metadata

不需要先造 AttackAction，但 Feature schema可以保存 future-runtime metadata：

```text
natural_weapon
attack_ability=constitution
damage=1d4 piercing
conditional_advantage=hp_at_or_below_half_max
resource_capacity=proficiency_bonus
recharge=long_rest
empowerment_modes=[heal,next_check_or_attack_bonus]
invalid_empowerment_target_types=[construct,undead]
```

P4接入時讀這些正式資料。

## F.11 Reconciliation

Lineage Build Edit仍走 P1 reconciliation：

- Max HP按新 Build重新 derive後保留 damage delta。
- Temp HP保留。
- Conditions保留。
- Inventory保留。
- Starting Equipment不重建。
- prepared spells重新驗 legality。
- resources按 capacity做必要 reconciliation。

---

# M01-G — TCE Artificer Core

## G.1 TCE pack增量 layout

```text
data/tce/
├── manifest.json
├── classes.json
├── levels.json
├── subclasses.json
├── features.json
└── spells.json   # 只有真正 supplied且足以成為正式 SpellEntry者
```

Magic Items在 I再加入同一 tce manifest category。

## G.2 Artificer ClassData

Entry至少提供既有 Builder真正使用的：

```text
hit_die
saving_throws
starting_proficiencies
starting_proficiency_options
starting_equipment
multi_classing
subclasses
spellcasting identity
```

如果 generic ClassData目前對這些欄位只是 `extra=allow`，G應對 code真正依賴的 fields補 typed validation，不要在 compiler用無驗證 magic dict。

## G.3 LevelData

建立：

```text
tce:level:artificer-1
...
tce:level:artificer-20
```

每級至少：

- level。
- prof bonus。
- parent class explicit ref。
- feature refs。
- ASI cumulative / marker data。
- cantrips known。
- slots 1–5。
- infusion progression metadata。

不偽造 `/api/2014/classes/artificer/...`。

## G.4 ASI resolver

現有 structural code若依 SRD level anomalies / cumulative counter，應泛化成 source-independent helper。

Artificer ASI由正式 level data驅動；禁止：

```python
if class_ref == "tce:class:artificer":
    if class_level in (...):
```

## G.5 Spellcasting config generalization

現有 `data/rules/dnd5e-2014/character-builder.json` 使用 legacy strings：

```json
"slot_contribution": "full"
"slot_contribution": "half"
"slot_contribution": "none"
```

Canonical shape泛化成概念：

```json
"slot_contribution": {
  "formula": "full|half|none",
  "rounding": "floor|ceil|none"
}
```

Backward compatibility loader必須 normalize舊 string：

```text
"full" → {formula=full, rounding=floor}
"half" → {formula=half, rounding=floor}
"none" → {formula=none, rounding=none}
```

Artificer：

```json
{
  "access_model": "prepared",
  "slot_contribution": {
    "formula": "half",
    "rounding": "ceil"
  },
  "resource_pool_type": "normal_multiclass_slots",
  "prepared_formula": "half_class_level_floor_plus_ability",
  "prepared_minimum": 1
}
```

Rules engine只consume canonical representation；不要同時維護「字串計算路徑」和「object計算路徑」。M01-G不得要求先 rewrite既有 SRD config才能 startup。

## G.6 Contribution pure function

建立 pure helper：

```text
caster_level_contribution(class_ref, class_level, config)
```

Combined slot aggregator只加 contribution結果，不知道 Artificer名字。

Warlock Pact Magic仍不參與 combined normal caster slots。

## G.7 Prepared formula独立

另有 pure helper：

```text
prepared_limit(profile, class_level, effective_ability_modifier)
```

Artificer odd level：

- slot contribution → ceil。
- prepared half-level → floor。

兩者不能共享同一 rounding flag。

## G.8 Spell list integrity

Selectable Artificer spell：

- 必須是 installed valid spell ContentEntry。
- class eligibility可由 spell `classes` refs或 dedicated class spell list relation判斷。
- cross-pack spell refs正常。

Supplied source不足以建立某 non-SRD spell時：

- 不建 dangling ref。
- 不建只有 name的 fake entry。
- closeout列 explicit content gap，或先補足完整正式 spell data再開放選擇。

## G.9 Specialist identities

```text
tce:subclass:alchemist
tce:subclass:armorer
tce:subclass:artillerist
tce:subclass:battle-smith
```

parent → `tce:class:artificer`。

Subclass timing由 class data / progression rule驅動，不在 UI hardcode level 3。

## G.10 Specialist spells

只有 supplied資料足夠確認完整 spell identity / threshold者才建立 granted/always-prepared access。

`source_type=subclass`。

若 reference文件只是摘要列舉，不得假裝是完整 Specialist spell table。

## G.11 UI

沿用 generic Builder：

```text
Class Progression
→ Subclass
→ Choices
→ Spells
→ Review
```

不建立 Artificer-only wizard。

---

# M01-H — TCE Artificer Advanced Features & Infusions

## H.1 Infusion identity

H開工時正式決定：

推薦新增 StableKind：

```text
infusion
```

理由：

- Builder需要 list/filter。
- 有 minimum level。
- 有 item applicability。
- 有 attunement metadata。
- 有 Replicate Magic Item target relation。

如果當時 `feature` subtype已足夠且 query不混亂，可不新增 kind；但必須有 typed schema與清楚 API identity。

## H.2 `InfusionData`

建議：

```text
index
name
minimum_artificer_level
requires_attunement
item_filter
modifiers
charges/resource metadata
replicates_item_ref optional
description
```

Item filter至少能表示 supplied常見：

```text
armor
shield
simple_or_martial_weapon
thrown_weapon
ammunition_weapon
rod_staff_wand
boots
ring
helmet
crystal_or_gem
```

不只 free text。

## H.3 Known Infusions persistence

Known Infusions屬 Build。

推薦：

```python
infusion_refs: tuple[StableKey, ...] = ()
```

或等價 typed Build structure。

把 Known Infusions塞普通 `feature_refs`只有在 subtype/provenance仍清楚可 query時才可接受。

Version History應能顯示 Known Infusions變化。

## H.4 Builder acquisition

每次 Artificer level progression生成 deterministic choice id，例如：

```text
artificer:infusions-known:<character/class-level acquisition point>
```

Level Up只要求新增差額，不重問整份。

Build Edit可依規則調整整體 Known list。

## H.5 Active Infusions persistence

Active Infusions屬 Current State。

推薦：

```python
class ActiveInfusion(FrozenModel):
    inventory_entry_id: str
    infusion_ref: StableKey

active_infusions: list[ActiveInfusion] = []
```

或把 `infusion_ref`放 `InventoryEntry`，二選一。

**禁止兩邊各存一份。**

## H.6 Active Infusion mutation validation

Service順序：

```text
load Build + State
→ check infusion is Known
→ check Artificer level prerequisite
→ resolve Inventory entry
→ check item filter
→ check item not already infused
→ check same infusion active-copy rule
→ check active count < capacity
→ atomically write state
```

拒絕零副作用。

## H.7 Capacity expressions

只支援 supplied data需要的有限 expression types：

```text
fixed
proficiency_bonus
ability_modifier(intelligence, minimum=N)
ability_modifier_x2(intelligence)
class_level
```

不建 general formula parser。

## H.8 Replicate Magic Item

Infusion：

```text
replicate-magic-item:<variant>
```

引用正式 item StableKey。

Known的是 recipe/Infusion；active的是某個 Inventory instance。

若 target item尚未在 I正式存在，可先以 verified future ref / same TCE pack staged data處理，但 H closeout前不得留 registry dangling ref。必要時把被 H直接依賴的 replicated item最小正式資料提前放 TCE pack，I再補全84件。

## H.9 Feature resources

CharacterState已有 generic `resources`，可延伸使用 deterministic resource IDs。

例如：

```text
feature:tce:feature:flash-of-genius
feature:tce:feature:arcane-jolt
```

Capacity由 Build derive；State只存 used/remaining。

Level Up reconciliation維持：

- capacity增加不等於自動全補滿。
- 已用量保留，再按 invariant計算 remaining。

## H.10 Attunement capacity helper

Rules Layer新增：

```text
attunement_capacity(build) -> int
```

依 feature progression derive。

Frontend只顯示 server DTO結果，不寫：

```text
if artificerLevel >= 18: 6
```

Magic Item Savant另提供：

```text
ignore_attunement_class_requirement
ignore_attunement_race_requirement
ignore_attunement_spell_requirement
ignore_attunement_level_requirement
```

或等價 structured flags，供 I validator使用。

## H.11 Armor Model live state

Guardian / Infiltrator不是永久 Build choice；角色已取得兩種可用 model後，Current mode可切換。

可新增 generic minimal：

```text
feature_modes: dict[feature_key, mode_key]
```

或 typed Artificer state extension。

要求：

- one source of truth。
- validation限定 legal modes。
- switch不產生 Build Version。
- reload可恢復。

## H.12 Spell-Storing Item state

建議：

```text
spell_storing_item:
  inventory_entry_id
  spell_key
  remaining_uses
```

Eligibility：

- feature已取得。
- item存在。
- spell符合 supplied spell level / casting time限制。

M01-H不做 actual cast resolution。

## H.13 Subclass boundaries

### Alchemist

- Experimental Elixir count progression / options可結構化。
- random result / consume effect可 manual。

### Armorer

- Armor Model state可做。
- Arcane Armor / Armor Modifications的 persistent/inventory interaction只做能明確表達部分。

### Artillerist

- Cannon available types / formula metadata可保存。
- active cannon combat entity留 P4。

### Battle Smith

- Steel Defender stat-generation metadata可保存。
- combatant instance留 P4。

## H.14 Build Edit conflict

如果 Build Edit移除一個目前仍 active的 Known Infusion：

- Review必須 blocking並要求處理；或
- reconciliation preview明確列出 active infusion將被解除，需使用者確認。

不得 silent delete。

---

# M01-I — TCE Magic Items

## I.1 TCE manifest增量

在既有 `data/tce/manifest.json`加入：

```text
kind=item
file=magic-items.json
count=<actual>
```

Supplied source宣告 84件；converter / manifest / file三者必須一致。

## I.2 Item schema

現有 `ItemData`需對 M01真正依賴的欄位增加 typed optional structures：

```text
rarity
item_type
requires_attunement
attunement_requirements
charges
recharge
modifiers
manual_effects
family_id optional
```

保留來源資料 `extra=allow`沒問題，但 code讀取的欄位不能全是 untyped magic dict。

## I.3 Attunement requirement

建議：

```python
class AttunementRequirement:
    required: bool
    allowed_class_refs: tuple[StableKey, ...] = ()
    allowed_race_refs: tuple[StableKey, ...] = ()
    spellcaster_required: bool | None = None
    minimum_level: int | None = None
    other_text: str | None = None
```

能結構化的「Artificer / Wizard / Cleric or Paladin」不要只存翻譯文字。

`other_text`只承載暫時無法合理結構化的要求。

## I.4 Item families / variants

+1/+2/+3為不同 items：

```text
tce:item:all-purpose-tool-plus-1
tce:item:all-purpose-tool-plus-2
tce:item:all-purpose-tool-plus-3
```

可同時：

```text
family_id=all-purpose-tool
power_bonus=1/2/3
```

family只給 UI grouping / common metadata reuse；不是 identity。

## I.5 `InventoryEntry.attuned`

新增 backward-compatible：

```python
attuned: bool = False
```

Attuned是 State。

舊 persisted JSON沒有 field時正常 load為 false。

## I.6 Attune / Unattune domain service

不要允許 frontend任意 PATCH：

```text
attuned=true
```

正式 action：

```text
attune_item(character, inventory_entry_id)
unattune_item(character, inventory_entry_id)
```

驗證順序：

```text
inventory entry exists
→ content item resolves
→ item requires attunement
→ item is in eligible state (carried/equipped policy)
→ derive character capacity
→ check requirement / Artificer bypass
→ check current count
→ atomic state update
```

## I.7 Item-instance charges

如果 item有 live charges，charges屬 instance state，不能寫回 shared ContentEntry。

可新增：

```text
item_resources[inventory_entry_id]
```

或 typed `InventoryEntry`附屬 state；依現有 State API選單一 Source of Truth。

不要為所有 item都建立 charge record；只在需要時。

## I.8 Simple modifier model

只建立有限 typed modifiers，例如：

```text
spell_attack_bonus
spell_save_dc_bonus
weapon_attack_bonus
weapon_damage_bonus
armor_class_bonus
saving_throw_bonus
```

Modifier一定要有 scope，例如：

```text
class_ref=tce:class:artificer
spell_source=wizard
item_ref=self
all_saves
```

All-Purpose Tool不能變成全角色所有 spell attack / DC加值。

## I.9 Manual effects

建議：

```json
"manual_effects": [
  {
    "summary": "...",
    "reason": "requires per-dawn trigger / combat target selection"
  }
]
```

UI顯示：

```text
需手動處理 / 尚未自動化
```

不得顯示「Active」但實際 calculation沒接。

## I.10 Deterministic converter

可建立 maintainer script，例如：

```text
scripts/vendor_private_tce_items.py
```

或 generic private-content converter。

要求：

- same input → same keys/order。
- stable index deterministic。
- duplicate display names explicit disambiguation。
- conversion後立刻 schema / registry validate。

Runtime不執行 converter。

## I.11 Artificer integration

Attunement service使用 H提供：

```text
attunement_capacity(build)
attunement restriction bypass flags
```

Soul of Artifice若 static save bonus已接 Rules Layer：

```text
bonus = current attuned item count
```

derive即可，不存 duplicated bonus state。

---

# M01-J — Full M01 Integration & Closeout

## J.1 J不再發明 major rule shapes

預設 J不新增：

- new content kind。
- major Character schema redesign。
- new generic effect engine。

如果 J才發現 A～I的核心 schema根本缺失，應回修對應 Subphase contract與測試，再重新驗，不把隱藏大功能塞進 J。

## J.2 Default installed packs

最後至少：

```text
srd5.1
phb2014
scag
gos
vgm
vrgr
tce
```

Loader order deterministic，但 content identity不得依載入順序。

同名 UI排序建議：

```text
display name
→ source label
→ stable key
```

## J.3 Consolidation

grep移除 M01過程產生的重複 helper：

```text
StableKey split/parser
reference normalization
source label mapping
class-level lookup
resource capacity expression
attunement capacity
```

不留下 `temporary M01 helper`到處複製。

## J.4 Migration policy

M01大部分新增 optional JSONB fields未必需要 DB migration。

只有真正：

- new relational table。
- new relational column。
- DB constraint。

才用 Alembic。

禁止只為填入：

```text
race_variant_ref=null
lineage_ref=null
attuned=false
```

就 bulk rewrite所有舊 Build/State JSON。

## J.5 Version History summary

Build Version summary至少能表達：

```text
Race / Subrace
Race Variant
Lineage / Ancestry
Background
Class / Subclass
Content Sources
Known Infusions（如適用）
```

Dhampir Version N+1不能看起來只是「多幾個feature」，而要能辨認 lineage change。

## J.6 Source / provenance UX

Builder Review / Character Sheet不需要顯示每個 internal StableKey，但至少能讓使用者辨識重要內容來源：

```text
Wood Elf · PHB
Background · SCAG
Artificer · TCE
```

Variant / Lineage identity需要明確。

## J.7 Manual / Deferred automation report

Closeout文件分類：

```text
Fully automated
Structured + state tracked, runtime manual
Deferred because P3 Roll substrate
Deferred because P4 Combat substrate
Deferred because future Rest/day-clock substrate
```

這是已知 implementation boundary，不是未決產品問題。

## J.8 Docs同步

M01 closeout同步：

```text
docs/M01/實作規格.md
docs/M01/開發設計方針.md
docs/M01/測試指南.md
PROJECT_BRIEF.md
AGENTS.md
```

`PROJECT_BRIEF.md` M01-A～J全部 ✅，next step回 P2 planning。

---

## 5. API / UI 共通邊界

M01不是 API redesign Phase。

優先延伸既有 endpoints / DTO；必要新增欄位：

```text
source metadata
race variant selection
lineage / ancestral legacy
roleplay suggestions
Known Infusions / active infusion state
attunement state
advanced resources / movement modes
```

Frontend延續 P0/P1：

- searchable selector。
- keyboard accessible。
- disabled option顯示 server reason。
- source label為 secondary info。
- structural error path對應 Builder step。
- Server authoritative Review。

M01不新增：

```text
Content Library management page
Book enable/disable workflow
Marketplace
```

---

## 6. Data conversion固定流程

每份 supplied Markdown固定：

```text
讀 human reference
→ 列 mechanical records
→ 定 StableKeys
→ 建 parent/cross refs
→ 分 mechanical vs roleplay/flavor
→ 建 choices / prerequisites / level gates / resource metadata
→ schema validation
→ focused domain tests
→ Builder Review / Confirm integration test
→ human/E2E where applicable
```

不接受：

```text
Markdown → bulk JSON → json.loads成功 → 宣告完成
```

每筆 mechanical content至少核對：

- source / identity。
- parent relationship。
- ability bonuses。
- proficiencies。
- choice count / distinctness。
- movement modes。
- level gates。
- spell source identity。
- resource capacity / recharge metadata。
- equipment quantity。
- Build vs Current State ownership。

---

## 7. M01 Anti-patterns

以下一律視為設計錯誤：

1. 非 SRD data寫入 `data/srd5.1/`。
2. 新 data偽裝 `source=srd5.1`。
3. 用 display name當 foreign key。
4. 同名後載內容覆蓋先載內容。
5. Frontend hardcode Variant Human / Dhampir / Artificer legality。
6. 每個 Builder module各自解析 `/api/2014/...`。
7. `content_sources`同時被當 allowlist與actual provenance，語意混用。
8. 掃描 `data/`所有子資料夾並自動 enable pack，導致fixture/未完成pack污染 production registry。
9. Variant Human另造完整平行 race而失去 Human base identity，或沒有 `race_variant_ref`可追溯 variant。
10. Half-Elf replacement只 hide UI、不移除 final Build grants。
11. Dhampir old race + lineage完整疊加。
12. Artificer用 scattered `if artificer: ceil`而 rules config仍只寫 `half`。
13. Known Infusions與Active Infusions都放 Build。
14. Active Infusion在 Inventory與State mapping各存一份。
15. Attunement直接任意 PATCH boolean繞過 server rules。
16. TCE item為了自動化完整效果建立 generic script DSL。
17. 建 schema-incomplete fake spells / dangling refs只為讓 Artificer spell list看起來完整。
18. Flavor table變 mandatory Builder choice。
19. M01-J才發明 A～I本來就該完成的 major schema。
20. 為 M02 / future books先設計沒有 supplied content驗證的機制。
