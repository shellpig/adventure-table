# P3 — 測試指南

> Phase：**P3 — Exploration + Roll + AI**  
> 本文件定義 P3-A～P3-F 的自動／人工驗收與 closeout evidence。產品完成條件以 `實作規格.md` 為準；具體架構、schema、API、MCP與transaction contract以 `開發設計方針.md` 為準。

最後更新：2026-09-08

---

## 1. P3 Subphase 順序

三份 P3 文件固定使用：

```text
P3-A — Session Table Runtime & Event Stream
P3-B — Exploration, Chat & Actions
P3-C — Roll, Check & PendingAction
P3-D — AI Controller, Scoped Token & Handoff
P3-E — AI Tool Surface & Event Delivery
P3-F — Full P3 Integration & Closeout
```

每個 Subphase 的 code與對應tests同一小階段完成；先做focused local驗證與static review，review完成後才依 `AGENTS.md` gate使用GitHub Actions。不要把permission / secret filtering / restart / concurrency全部留到P3-F第一次驗。

---

## 2. P3 測試核心風險

P3最危險的regression包括：

- Player / AI Player收到DM-only或secret DC。
- Whisper DM被其他Seat讀到。
- **Seat private event被寫成連current DM都讀不到的孤兒event**，或反過來讓非DM第三方讀到。
- **Human玩家交接給AI後，接手的AI讀得到交接前的Whisper DM與Seat private歷史。**
- **private visibility floor做過頭，讓接手者連public event / Stage / pending state都拿不到，桌面直接不能玩。**
- 猜event sequence / UUID繞過audience filter。
- reconnect後pending roll消失或被重roll。
- retry造成同一formal RollRequest兩個結果。
- Character State已改但event沒commit，或反過來。
- P2 Human-only caller contract被複製到P3新service，AI永遠無法走正式授權。
- AI為通過P2函式被偽造成Human `RoomAccessContext` / Room authority。
- DM proxy被錯做成「切換Player Controller」，或audit只剩subject Player看不出真正acting DM。
- DM proxy走DM Direct Edit bypass而不是Player rules，導致可繞過資源／roll規則。
- `/search` 自動roll或自選Skill，違反DM裁定；`/check` 讓Player自行建立secret DC / formal result。
- Player Human → AI → Human後舊token還能操作。
- **Take Back Control只看「同Room member」或display name，讓第三方搶走別人的AI-controlled Seat。**
- **原 handoff access session遺失後，系統錯把新裝置的新 access session當同一個Human，自動放行Take Back。**
- Temporary Handoff Instruction被寫進Character/Campaign永久資料，或Take Back後仍被下一個AI看到。
- Session End / Abandon後session-scoped AI token仍可read/wait/write。
- AI DM pre-session token未原子綁定Session，可被拿去操作其他Session。
- **未綁Session的pre-session AI DM token沒有TTL／revoke lifecycle，變成永久有效DM Seat bearer。**
- **pre-session AI DM token在Seat controller改變、Seat/Campaign失效後仍可Start。**
- P2 controller binding CHECK沒有DB invariant綁grant/generation。
- **只檢查 `grant.status='active'`、不比對Seat current grant id與controller epoch，讓stale grant繼續授權。**
- event同步只靠process-local queue，server restart就斷歷史。
- `wait_for_event` 用sync long-poll佔滿worker或等待期間持有DB connection。
- Session頁為了即時更新高頻重抓P2 Resume，放大N+1。
- MCP adapter直接繞過Human UI共用service。
- MCP SDK仍用 2025-era `initialize` / `Mcp-Session-Id` path，卻被誤認為已驗 `2026-07-28`。
- MCP request 的 `MCP-Protocol-Version` / `_meta` / `Mcp-Method` / `Mcp-Name` 不一致，gateway與body看到不同operation。
- external MCP bearer token經 plain HTTP 離開 loopback，或選定client實際無法注入 static Authorization header，導致 external gate到最後才發現不能跑。
- P3 PostgreSQL job只設 `P3_POSTGRES_URL`，使既有 P2/M03 PostgreSQL tests因 env 缺失靜默 skip，形成假綠。
- workflow YAML被改壞但沒有 contract test釘住 workflow name / branch / env / explicit regression files。
- `app.mcp.*` 不在既有 M03 forbidden regex，Standalone protected import graph意外 reach MCP package卻仍綠。
- P3誤做半套 `resolve_action()`，提前形成P6/P7不完整contract。
- standalone因P3新module/table/MCP route被污染。
- P3 event log被誤當成P7完整Timeline，形成難以演進的contract。

P3 minimum gate：

```text
scope isolation
+ audience secrecy
+ durable ordered events
+ non-blocking event wait
+ acting-vs-subject audit
+ DM proxy permission/rules parity
+ slash command behavior
+ atomic gameplay writes
+ roll idempotency
+ typed Human/AI actor authorization
+ Seat-authoritative controller epoch + stale-grant rejection
+ controller session token scope / revocation
+ finite-TTL restricted pre-session AI DM grants
+ Take Back origin-Human authorization + admin recovery path
+ Temporary Handoff Instruction lifecycle
+ controller binding DB invariants
+ real PostgreSQL concurrency
+ no-silent-skip PostgreSQL regressions
+ workflow contract tests
+ real browser UX
+ modern MCP 2026-07-28 wire compliance
+ HTTPS/TLS real external MCP client
+ restart recovery
+ standalone independence including app.mcp
```

---

## 3. 執行環境與指令原則

### Backend

Windows / PowerShell 5.1 不使用 `&&`。需要 `alembic.ini` / relative path的Backend Python指令，cwd在 `apps/server`，interpreter使用repo root `.venv`。

```powershell
Set-Location .\apps\server
..\..\.venv\Scripts\python.exe -m pytest
```

Focused：

```powershell
Set-Location .\apps\server
..\..\.venv\Scripts\python.exe -m pytest tests\test_p3a_table_events.py -q
```

### PostgreSQL focused tests

P3建立自己的環境變數：

```text
P3_POSTGRES_URL
```

但 P3 CI **不能只設這一個名字**。既有 P2 PostgreSQL suites直接讀 `P2_POSTGRES_URL`，M03 migration suite讀 `M03C_POSTGRES_URL`（或 fallback P2）；若缺 env 它們會 skip。P3 `postgres-migrations` job必須把同一 dedicated test DB 同時提供為：

```text
DATABASE_URL=<dedicated test db>
P3_POSTGRES_URL=<same dedicated test db>
P2_POSTGRES_URL=<same dedicated test db>
M03C_POSTGRES_URL=<same dedicated test db>
```

本機要跑完整 P3 + legacy PostgreSQL regression時也比照設定：

```powershell
docker compose up -d db
docker compose exec -T db psql -U adventure -d postgres -c "DROP DATABASE IF EXISTS adventure_table_p3_test;"
docker compose exec -T db psql -U adventure -d postgres -c "CREATE DATABASE adventure_table_p3_test;"
Set-Location .\apps\server
$env:P3_POSTGRES_URL = "postgresql+psycopg://adventure:adventure@localhost:5432/adventure_table_p3_test"
$env:P2_POSTGRES_URL = $env:P3_POSTGRES_URL
$env:M03C_POSTGRES_URL = $env:P3_POSTGRES_URL
$env:DATABASE_URL = $env:P3_POSTGRES_URL
..\..\.venv\Scripts\python.exe -m pytest <P3 PostgreSQL focused tests + required P2/M03 PostgreSQL regressions> -q
```

不要drop開發中的真實資料庫。migration/concurrency證據不能由SQLite取代。Closeout必須能證明 required legacy PostgreSQL tests **實際執行而非 skip**。

### Frontend unit / build

```powershell
Set-Location .\apps\web
npm test -- --run
npm run build
```

### E2E

依既有 `KI-ENV-001`，完整Playwright走Docker/Linux web service。

P2-F已知限制：`test:e2e:docker`只rebuild web。P3在第一次新增backend-dependent E2E時必須把這個operational trap收斂掉：

- 優先修改script讓它一起build server；或
- 建立P3 wrapper command，明確build server + web後再跑。

不得讓P3 closeout evidence可能靜默測到舊backend image。

P3-F具名 workflow 固定為：

```text
.github/workflows/p3-e2e.yml
Display name: P3 Full-Stack E2E
```

至少支援 `workflow_dispatch`，在同一 run 中 clean full-stack、**rebuild server + web**、等 health/readiness、跑完整 P3 Playwright、failure印 logs、always upload Playwright report/test-results、always teardown。可以沿用 P2 workflow structure，但名稱與 artifact必須能區分 P3 evidence。

### MCP client smoke

P3-E起分兩層：

1. protocol-level automated client：對真 `/mcp` 驗 `2026-07-28` modern wire contract，不把 legacy initialize handshake當通過。
2. closeout external-client smoke：至少一個實際外部AI host/client經 **HTTPS/TLS** MCP URL連線 Adventure Table，且已驗證能設定 P3 static Bearer credential。

repo docker-compose 的 loopback plain HTTP可以作 automated protocol substrate，但不能算 external bearer closeout evidence。只call Python handler function或 custom mock client也不算正式MCP evidence。

---

## 4. P3 Actions workflow

### P3 Non-E2E

P3-A建立或正式接手：

```text
.github/workflows/p3-non-e2e.yml
Display name: P3 Non-E2E
```

最低jobs：

```text
backend
frontend
postgres-migrations
windows-standalone
```

`postgres-migrations` job至少：

- PostgreSQL 17 service / project-supported equivalent。
- 同時設定 `DATABASE_URL`、`P3_POSTGRES_URL`、`P2_POSTGRES_URL`、`M03C_POSTGRES_URL` 指向同一 dedicated test DB。
- 明確執行 P3 migration/concurrency tests。
- 明確重跑既有 P2 PostgreSQL suites，例如 `test_p2a_postgres_migration.py`、`test_p2b_postgres_workspace.py`、`test_p2e_postgres_sessions.py`、`test_p2f_postgres_seat_selection.py`、`test_postgres_roster_lock_order.py`，以及 `test_m03c_migration.py`；若檔名後續合理重構，可更新 contract test，但不得用「pytest全跑所以大概有跑到」取代明列 legacy real-Postgres gate。

P3-E後可新增：

```text
mcp-protocol-smoke
```

若MCP automated smoke能安全併入backend job也可，但closeout文件必須能指出exact test / evidence，不能只寫「backend綠」。

### P3 E2E

P3-F建立／正式接手：

```text
.github/workflows/p3-e2e.yml
Display name: P3 Full-Stack E2E
```

`workflow_dispatch` 是最低要求；workflow必須 full rebuild backend/server + web，不得重用可能 stale 的 server image。

### Workflow contract test

P3-A建立 `p3-non-e2e.yml` 的同一小階段，必須比照既有 `test_p2a_workflow_contract.py` 增加 P3 workflow contract test（可叫 `test_p3_workflow_contract.py` 或依現有命名慣例）：

- assert `name: P3 Non-E2E`。
- assert允許的 branch / `workflow_dispatch` policy；不得意外加入 pre-review `pull_request` auto-run，除非專案流程日後明確改變。
- assert PostgreSQL service與四個 URL env都存在。
- assert required P3 + P2 + M03 real-Postgres test files被明列。
- assert Windows standalone gate / build / smoke仍存在。

P3-F加入 `p3-e2e.yml` 時，同一 contract test（或獨立 P3 E2E contract test）再 assert：

- `name: P3 Full-Stack E2E`。
- `workflow_dispatch`。
- clean stack + full `docker compose ... --build`／等價 server+web rebuild。
- Playwright command、logs-on-failure、artifact upload、teardown。

Actions執行順序遵守使用者既定開發流程：該小階段code + tests完成 → static review完成 → 才跑non-E2E Actions。

每個closeout記錄：

- branch / exact code SHA。
- local commands與pass/fail/skip。
- Actions workflow + run id + exact SHA。
- PostgreSQL-specific tests實際在哪個job跑，並證明 legacy suites不是 skip。
- P3 E2E workflow + run id + artifact。
- external MCP client名稱、版本、auth配置方式、HTTPS/TLS入口形態與測試日期。
- intentional skip對應已知問題。

---

## 5. Test layers

### Layer A — Domain / pure policy

驗：

- event audience projection，含SEAT_PRIVATE的current DM可讀性。
- private visibility floor policy：epoch前進設新floor、self-service Take Back還原舊floor、只作用於private面。
- `TableActorContext` Human / AI typed identity與role permission。
- `TableActionContext` acting actor / subject Seat / execution mode。
- current-DM policy可接受合法Human或AI actor，但Owner escape hatch不等於DM actor。
- current DM proxy任意合法Player Seat；非current DM不可proxy。
- DM proxy不改controller。
- PendingAction transition。
- RollRequest status / visibility。
- advantage / disadvantage / flat adjustment計算。
- Seat `controller_epoch` 單調演進、grant generation immutable snapshot與current binding validation。
- controller session scope / revocation / pre-session expiry validation。
- Take Back origin Human access-session policy與 administrative reassignment分離。
- Temporary Handoff Instruction active/inactive lifecycle。

### Layer B — Persistence / migration

驗：

- P3 web-branch Alembic revisions。
- fresh PostgreSQL `upgrade heads`。
- P2 final DB → P3 heads upgrade。
- P3-D不修改歷史 `0013_p2d_campaign_seats.py` / `0014_p2e_sessions.py`。
- `ck_campaign_seats_controller_binding` / `ck_sessions_dm_controller_binding` / `ck_session_participants_controller_binding` 新revision正確drop + add。
- Seat `controller_epoch` 對Human/AI/None都NOT NULL；既有P2 Seat migration後有合法初始值。
- Seat `private_floor_seq` NOT NULL、既有P2 Seat migration後預設0（不遮蔽既有內容）；epoch前進與floor更新在同一transaction，不會只commit其中一半。
- Human Seat binding：access session有值、AI grant為NULL。
- AI Seat binding：access session為NULL、AI grant有值；current grant.generation必須等於Seat controller_epoch才算授權有效。
- None Seat binding：Human access session與AI grant皆NULL，但controller_epoch仍保留單調值。
- Session fixed AI DM snapshot / Participant AI join snapshot要求grant id + generation；Human/None snapshot不混AI欄位。
- Session fixed DM AI grant/generation與Participant join snapshot restart後仍可還原，但**不作為current Seat authority來源**。
- grant `generation` mint後不可改；rotate/reassign用新epoch+新grant。
- session-scoped grant有明確 `session_id` / 等價不可跨Session綁定。
- pre-session AI DM grant必有有限 `pre_session_expires_at`；不得NULL表示永久有效。
- Player handoff grant必存 `handoff_return_access_session_id`，且指向原 Let AI Control Human access session。
- pre-session AI DM grant可在Start transaction綁定一次，不能重綁另一Session。
- Temporary Instruction持久於同一grant generation但不進Character/Campaign永久資料。
- standalone `character@head`完全無P3 tables。
- event seq unique / monotonic。
- message+event / roll+event atomicity。
- restart persistence。
- Room Hard Delete清P3 Room-scoped rows/assets/grants或使token不可解析。

### Layer C — API integration

驗：

- Session / Seat / Room path scope。
- audience filtering，含SEAT_PRIVATE對current DM可讀。
- private visibility floor在events / wait / context三個讀取面一致生效。
- Stage upload/read auth。
- Chat / Action / Whisper。
- DM proxy Dialogue / Action / Roll / Current State。
- `/action /check /search /whisper /ooc` parser/route行為。
- Request Check / Group Check / Secret Check。
- formal / physical / quick roll。
- handoff / token revoke。
- Take Back origin identity與admin reassignment path。
- Temporary Instruction讀取與失效。
- pre-session AI DM restricted surface / expiry / lifecycle revocation。
- AI DM Start policy。
- End / Abandon session token revoke。
- Human / AI caller都只透過共同actor resolver進gameplay service。
- Owner Abandon escape hatch仍成立，但Owner不是current DM時不能End / Late Join /改Stage / proxy。
- stable error code。

### Layer D — Concurrency / resource

真 PostgreSQL至少驗：

- concurrent event append不重seq。
- duplicate formal roll completion。
- roll resolve vs cancel。
- PendingAction resolve vs cancel。
- Player write vs DM proxy同一subject的競態走同一UoW/invariant。
- Human→AI與Take Back同時競態；最終 `controller_epoch` 與 `private_floor_seq` 必須一致，不得出現controller是AI但floor已還原的組合。
- two token rotations / two Let AI Control。
- Take Back vs Owner/DM administrative reassignment。
- pre-session DM grant rotate vs Start。
- DM Seat controller change/archive vs pre-session Start。
- Campaign transition / active_campaign switch vs pre-session Start。
- old token write racing with revoke。
- stale grant request racing with Seat controller_epoch increment。
- Session End / Abandon vs AI write/wait。
- Start Session AI DM grant bind vs duplicate Start/rebind。
- End/Abandon token revoke任一步故障時整個lifecycle transaction rollback，不留下ended+valid token。
- multiple idle event waiters不長期佔用DB connections；一般DB-backed request仍可完成。
- wait cancellation / timeout釋放registration與資源。

SQLite sequential test不能取代這些證據。

### Layer E — Frontend unit / projection

驗：

- capability route split。
- existing `ai_actor` flag沿用；不得出現第二個同義 AI capability。
- P3期間 `combat=false`、`timeline=false`；若新增 `table_runtime` / `roll`，backend/frontend type與guards同commit更新。
- Session initial Resume + incremental cursor reducer。
- stale event / duplicate event reducer idempotency。
- Stage modes。
- Seat / Character input selector。
- DM proxy UI清楚標示acting as target，送出後controller視圖不變。
- slash parser五個commands。
- `/search`只轉Action intent；`/check`不直接roll。
- secret field永遠不出現在Player DTO type / rendered DOM。
- floor之前的private event不進client store；前端不得先收到再自行截斷。
- token只顯示一次與copy UX。
- pre-session AI DM token顯示有限 expiry / expired狀態，不暗示永久有效。
- Temporary Instruction輸入optional，Take Back後UI清除active instruction。
- Take Back只在目前browser持有原 handoff access session時可self-service；否則顯示需要Owner/DM reassignment的明確UX，不用display name自動認人。
- bilingual copy parity / raw key guard。

### Layer F — Real-backend Playwright

驗真正browser：

- multi-browser Human DM + Players。
- Stage / Chat / Action。
- slash commands。
- DM proxy。
- roll request與result。
- secret roll network/DOM不洩漏。
- reload / reconnect。
- handoff UI + Temporary Instruction + origin-only Take Back。
- administrative Player Seat reassignment recovery。
- late join。
- End/Abandon token revoke可由API/MCP evidence配合。

### Layer G — MCP protocol / external AI

Modern `2026-07-28` automated wire gate至少驗：

- `/mcp`只在web channel存在。
- request沒有 legacy `initialize` prerequisite；不使用 `Mcp-Session-Id`。
- `MCP-Protocol-Version: 2026-07-28` 與 body `_meta.io.modelcontextprotocol/protocolVersion`一致。
- 每個 request 有符合 body method 的 `Mcp-Method`；named operations有正確 `Mcp-Name`；缺失或 header/body mismatch被拒絕。
- `server/discover` 可成功，但後續合法 request **不依賴先 discover**；Server保持stateless protocol authorization。
- `server/discover`、`tools/list` 與其他規格定義的 cacheable result在 raw wire有 `ttlMs` / `cacheScope`。
- unbound pre-session AI DM grant只可protocol metadata/list + minimal own pre-session context + Start；`get_session_context` / events / gameplay tools全部拒絕。
- expired / invalid / revoked / wrong-seat / wrong-session / stale-epoch token被拒絕。
- tool schema與structured error。
- get context audience。
- wait cursor。
- Player AI context可得有效Temporary Instruction、失效後不可得。
- action / roll共用canonical state。
- `resolve_action` **不在 P3 tool catalog**。
- real external client經 HTTPS/TLS完成journey，且該client版本已實測可設定 static Bearer/header auth。

### Layer H — Standalone regression

驗：

- frozen build成功。
- SQLite無P3 tables。
- import graph無P3 modules。
- `app.mcp.*` 被 `test_m03_import_boundary.py` 明確視為 forbidden multiplayer dependency；新增 negative fixture要能證明 protected Character/Standalone import reach `app.mcp.tools`／等價 module 時測試會失敗。
- `/mcp` / P3 routes不存在或capability disabled。
- `ai_actor=false`；P3不得打開 `combat` / `timeline`。
- Character Create / Import / Sheet / Level Up / Export仍正常。

---

## P3-A — Session Table Runtime & Event Stream

### Automated gate

最低案例：

1. event seq由1起單調增加，同Session無duplicate。
2. 兩個transaction並發append，真PostgreSQL得到不同且連續／可排序seq。
3. public event所有合法participant可見。
4. DM_ONLY只current DM可見；Owner但不是current DM不可見。
5. ACTOR_AND_DM只有相關actor/subject controller + current DM可見。
6. SEAT_PRIVATE只recipient Seat的current controller + current DM可見。**current DM可讀SEAT_PRIVATE是定案行為**，必須有正面斷言，不得以「依contract」留白。
7. private visibility floor：把某Player Seat的controller epoch往前推一格並設新floor後，新controller的event feed與context**完全不含**floor之前的SEAT_PRIVATE / 該Seat ACTOR_AND_DM event；直接指定舊event id / seq也取不到。
8. 同一情境下，新controller仍取得完整public event、Stage、pending action / roll與Character Current State；floor不得誤傷public面。
9. 還原語意：把floor還原成舊值後（模擬self-service Take Back），該controller重新讀得到floor之前的自己Seat private event。
10. current DM在整段epoch變更前後都讀得到該Seat的private event，不受floor影響。
11. Room A caller拿Room B Session id → not found/scope reject，不洩漏existence。
12. `after_seq`只回後續；limit/cursor正確。
13. duplicate idempotency key不產生第二event。
14. wait HTTP endpoint是 `async def`／等價非同步路徑；不得以sync endpoint + `sleep`/blocking loop佔住AnyIO worker。
15. wait timeout正常回empty，不報500。
16. wait進入idle狀態後沒有長期checked-out DB connection / active transaction；可用pool instrumentation、test hook或等價證據驗證。
17. 在受限DB pool / worker資源下同時建立16～32個或明顯高於pool容量的idle waiter，另一個一般DB-backed Session/event request必須在wait timeout前完成。
18. wait期間新增event會wake；即使故意miss process-local notify，DB cursor recheck仍能補到event。
19. client cancellation / timeout後waiter registration與資源回到baseline。
20. process restart後last seq / runtime revision保持。
21. Ended Session不接受新gameplay event write，但既有event可依history policy讀取。
22. initial Resume後增量event不用再call完整Resume；前端mock/spy或API contract證明。
23. P3-A修正／避開P2 Resume N+1後有query-count或repository batch test，避免heartbeat級別放大。
24. `recipient_seat_ids` 若在 SQL 直接做 JSONB/ARRAY membership，migration有對應 membership index且有 `EXPLAIN`／focused query-plan evidence；若採 bounded `(session_id, seq)` window後 application projection，測試必須證明 window有硬limit / query bound，而不是無界seq scan。
25. standalone schema / import boundary新增P3命名後仍綠。

### Static review checklist

- 是否有任何audience filter只在frontend。
- 是否用 `max(seq)+1` 無鎖配置seq。
- 是否process-local queue成為唯一truth。
- 是否P3 event payload複製整份Character snapshot。
- 是否event endpoint可以傳flag要求DM資料。
- private recipient query是否採無界JSON/array scan；若不用membership index，是否真的有 bounded seq window證據。
- SEAT_PRIVATE的DM可讀性是否還寫成「依contract」「依產品行為」等待定敘述；三份文件應已定案為current DM可讀。
- private visibility floor是否只在frontend或context builder套用，而不是在同一層Server projection。
- floor是否誤套到public event / Stage / pending / Character State，讓接手者拿不到桌面現況。
- wait endpoint是否仍是sync `def`。
- await notifier期間是否還持有DB dependency / ORM Session / transaction。
- 是否把整個30–60秒wait offload到sync worker thread。
- disconnect/cancellation是否真的cleanup。

### Subphase closeout evidence

- focused backend。
- PostgreSQL concurrent event test。
- private-recipient query/index strategy evidence。
- event wait async/no-DB-hold/starvation resource test。
- frontend cursor reducer tests。
- `P3 Non-E2E` exact SHA綠。
- P3 workflow contract test綠。
- standalone boundary綠。

---

## P3-B — Exploration, Chat & Actions

### Automated gate

1. current DM設定Text only Stage。
2. Image only / Image+Text / Clear round-trip。
3. Player不能改Stage。
4. DM Key holder但不是current DM不能改active Stage。
5. PNG/JPEG/WebP allowlist成功；SVG/HTML/超size/偽MIME被拒。
6. Room A asset URL不能由Room B access讀取。
7. Character Dialogue綁正確Seat/Character。
8. 一個Human控制兩Seat時可選不同角色，各自actor identity正確。
9. Action / OOC / Narration kind正確。
10. Whisper DM sender與DM看得到，其他Human/AI Player API response裡完全沒有。
11. browser reload後Stage/message仍在。
12. server restart後Stage/message仍在。
13. Session End保留已commit message，不改Character State。
14. `/action` → 同一typed Action service。
15. `/search` → 同一typed Action/search intent；**不建立RollRequest、不呼Server RNG、不自行選Skill/DC**。
16. `/whisper` → 同一Whisper service。
17. `/ooc` → 同一OOC service。
18. `/check` parser可被辨識但正式flow由P3-C gate完成。
19. current DM可proxy Human-controlled Player Seat Dialogue / Action；Seat Controller前後不變。
20. current DM可proxy AI-controlled/offline Player Seat Dialogue / Action；AI grant仍是原controller，不被revoke/replace。
21. noncurrent DM authority holder不能proxy。
22. proxy event/message保存 acting DM + subject Player Seat/Character + `dm_proxy`／等價audit truth。
23. proxy Action仍跑Player action validation，不可用DM Direct Edit bypass。
24. `zh-TW` / `en` parity。
25. P3 Session page進hardcoded copy scan。

### Real-backend journey B1

```text
DM browser + Player browser
→ Start Session
→ DM set image + text Stage
→ Player Character Dialogue
→ Player /action check the door
→ Player /search old chest
→ no automatic roll
→ Player /whisper DM
→ DM receives whisper
→ second Player never receives whisper
→ reload both browsers
→ Stage / public stream / own private stream still correct
```

### Real-backend journey B2 — DM proxy

```text
Player Seat Mira Controller=Human (offline/unresponsive)
→ DM chooses Act as Mira
→ DM posts Mira action
→ event says acting DM + subject Mira
→ Seat Controller remains Human
→ repeat with another Seat Controller=AI Offline
→ DM action succeeds through same Player rules
→ AI Controller remains AI
```

### Static review checklist

- Stage是否被做成Scene/Adventure entity偷跑P6。
- asset是否public static path繞過Room auth。
- Whisper是否先回全量再frontend filter。
- slash command是否另外打另一套business endpoint。
- `/search`是否偷自動roll。
- DM proxy是否偷改controller或只記subject Player不記acting DM。
- P3新TableActionService是否把 `RoomAccessContext.access_session_id` 當永久caller contract。

---

## P3-C — Roll, Check & PendingAction

### Domain / backend cases

#### Normal formal roll

- DM request Investigation。
- target controller或current DM proxy roll。
- Server保存raw d20、modifier、total。
- request `pending -> resolved`。

#### Advantage / Disadvantage

- raw two d20皆保存。
- kept die由Server計。
- modifier來自canonical subject Character rules。

#### Group Check

- 一個RollGroup多requests。
- 每個Seat各自完成或由current DM proxy完成。
- group projection顯示Waiting/Rolled。
- 不自動宣稱fiction success/failure。

#### Secret Check

- Player能看到「需要擲」所需資訊。
- secret DC不在Player JSON。
- dm-only result不在Player JSON / event stream。
- DM projection完整。

#### Physical formal dice

- submit raw die成功。
- Server重算total。
- 只傳total被schema拒絕。

#### Quick Dice

- 可roll / log。
- pending formal request狀態完全不變。

#### Duplicate completion

同一RollRequest兩個並發completion：

- 一個結果commit。
- DB只有一個formal result。
- retry取得same result或stable conflict。
- 不得重新RNG。

### `/check` cases

1. Player `/check search the sigil`只建立Check intent /呈現給DM，不直接建立帶secret DC的formal result。
2. Player不能藉command自行指定「DC 1 therefore success」或把Quick Dice配對pending request。
3. current DM `/check` 走同一 `RequestCheckService` 可合法建立RollRequest。
4. `/check` 與Toolbar / Player Action上的Request Check最後進同一domain service。

### DM proxy roll cases

```text
RollRequest target=Mira
Mira controller offline/unresponsive
→ current DM proxy Roll
→ RNG/modifier使用Mira canonical Character rules
→ result acting=DM, subject=Mira
→ Mira controller unchanged
```

另驗：

- DM proxy physical roll提交raw die仍由Server計modifier。
- 非current DM不能proxy完成request。
- current DM不能用自己的Ability/Proficiency替代subject Character。

### PendingAction cases

- legal transition全覆蓋。
- illegal reverse transition拒絕。
- waiting_for_roll綁request。
- roll resolve後action可繼續resolve。
- cancel與resolve concurrency有真PostgreSQL證據。

### Character state atomicity / actor boundary

至少：

```text
Human Player self action
→ legal Character Current State change
→ table event

current DM proxy Player action
→ legal subject Character Current State change
→ event retains acting DM + subject
```

故障注入／transaction rollback證明不能只成功一半；proxy不改Controller。

### Real-backend journey C1

```text
Player Action: check the door
→ DM Request Check
→ Player sees pending request
→ current controller or DM proxy rolls
→ DM sees raw/result/DC
→ Player sees allowed result only
→ reload
→ request remains resolved, no second roll
```

### Static review checklist

- client能不能自填proficiency / total。
- AI/tool能不能直接提交「natural 20」當Server roll。
- formal roll有沒有unique result constraint。
- secret DC是否只是TypeScript optional但backend其實全回。
- DM proxy是否使用subject Character規則，而不是DM角色規則。
- Character write是否又直接呼叫只能吃Human `RoomAccessContext` 的P2入口。

---

## P3-D — AI Controller, Scoped Token & Handoff

### Migration / backward compatibility gate

1. P2 final Web DB → P3-D heads migration成功；既有Human Seat / Session / Participant rows仍合法。
2. 新revision對三條具名binding CHECK做drop + add，不修改 `0013` / `0014` 歷史migration。
3. Seat `controller_epoch` 對既有與新Human/AI/None row都NOT NULL，P2 legacy row upgrade後有合法初值。
4. Seat AI current binding必須有grant id；grant generation是mint時Seat epoch snapshot，current authorization還要比對 `seat.controller_epoch == grant.generation`。
5. Session AI DM fixed snapshot必須有grant id + generation；Participant AI join snapshot同理。
6. Human row若混入AI grant、AI row若混入Human access session、None row若帶controller identity都被DB拒絕；epoch不因Human/None而清NULL。
7. restart / reload後fixed AI DM identity與participant snapshot不靠in-memory map還原。
8. session-bound grant不可重綁到另一Session。
9. grant generation immutable；controller改變只能前進Seat epoch並建立／綁新的identity，不能改舊grant generation。
10. Player handoff grant持久保存原 `handoff_return_access_session_id`；pre-session DM grant持久保存有限 `pre_session_expires_at`。

### Existing P2 caller regression

改 `TableActorContext` / current-DM policy後，至少重跑並更新：

- `test_p2e_live_character_scope.py`。
- `test_p2e_session_lifecycle.py`。
- `test_p2e_late_join.py`。
- `test_p2e_session_api.py`。
- `test_p2e_persistence_contract.py` / controller persistence相關tests。
- P2-D Seat controller policy / persistence regressions。

Human DM Start / Late Join / End / Abandon、Human Player live Character write既有行為不得退化。

### Token security / current binding cases

1. plaintext token只create/rotate response出現一次。
2. DB row / log不含plaintext。
3. random wrong secret拒絕。
4. Room/Campaign/Seat mismatch拒絕。
5. revoked grant拒絕。
6. wrong `session_id` / ended Session scope拒絕。
7. token不能取得Owner endpoint。
8. AI Player token不能取得DM tool/action。
9. AI token resolve後產生typed `TableActorContext`；不得產生假的Human access session / Room authority。
10. `grant.status=active` 但 Seat current grant id已指向另一grant → reject。
11. `grant.status=active`、grant id仍在資料庫，但 `grant.generation != seat.controller_epoch` → reject。
12. controller epoch每次 controller identity變更單調增加；舊generation不能復活。
13. Session/Participant generation snapshot與current Seat epoch不同時，只能當歷史資料，不能拿snapshot放行current request。

### Pre-session AI DM grant lifecycle cases

1. Owner建立AI DM pre-session grant時 `session_id=NULL`、finite `pre_session_expires_at`、generation等於DM Seat current epoch。
2. `pre_session_expires_at=NULL`／已過期的unbound DM grant不能通過production contract。
3. 未過期unbound grant可做protocol metadata/list、讀自己DM Seat + Campaign + Start readiness的最小context、呼Start。
4. 同一unbound grant呼 `get_session_context`、event read/wait、Narration、Stage、Request Check、Roll、Character State、Late Join、End、Abandon → reject。
5. TTL一到，即使DB `status` 尚未lazy更新為revoked，authorization也立即回expired/revoked類stable error。
6. DM Seat controller AI→Human / AI→None → old unbound grant在同一mutation boundary失效。
7. AI DM rotate/regrant → old grant revoke、Seat epoch+1、新grant generation=new epoch；old token reject。
8. DM Seat archive/delete → old unbound grant失效。
9. Campaign從active轉 `completed` / `archived` / 其他非Start-eligible status → 所有unbound DM grants失效。
10. Room active Campaign切離該Campaign → 該Campaign未綁Session DM grants revoke；重新選回需重新mint。
11. Room/Campaign hard delete後token無法解析／授權。
12. Owner explicit revoke立即生效。
13. Start transaction在bind前重新驗 current DM Seat grant id、epoch/generation、Campaign仍Start-eligible與TTL；任一不符不建立Session。
14. 成功Start後grant只可bind該Session一次；pre-session expiry不再被拿來讓fixed DM中途自動失效，之後走Session revoke lifecycle。

### Take Back Control authorization cases

1. `Let AI Control` 把當下 Human `access_session_id` 存成 `handoff_return_access_session_id`。
2. **exact origin access session仍active** → self-service Take Back成功。
3. 同 Room另一個member access session → reject。
4. 新access session使用相同 `display_name` → reject。
5. 知道Room password後新建member access session → reject。
6. origin access session已revoked → self-service Take Back reject。
7. origin browser/localStorage遺失、玩家重新進Room取得新access session → self-service Take Back reject；UI/API明確指向admin recovery，不猜同一人。
8. Owner/DM可依既有Player Seat management policy執行 **administrative reassignment** 給新的合法Human access session。
9. administrative reassignment必須atomically revoke AI grant、Seat epoch+1、清AI binding、設Human binding、保留participant/Active Character snapshot並寫audit。
10. 普通member不能呼admin reassignment；DM/Owner acting identity與new Human identity都可追蹤。
11. Take Back成功後舊grant即使row仍active（故障fixture）也會因Seat binding/epoch mismatch被拒絕。

### Private visibility floor cases

1. `Let AI Control` 在同一 transaction 把 `seat.private_floor_seq` 設為本次 controller-changed event 的 `seq`，並把舊值存進 grant 的 `handoff_return_private_floor_seq`。
2. handoff 前該 Seat 送出的 Whisper DM / SEAT_PRIVATE event，在接手 AI 的 event feed、`get_session_context` 與直接指定 seq/id 的查詢中**全部取不到**。
3. 同一 AI 仍取得完整 public event、Stage、pending action / roll 與自己 Character Current State；floor 不得誤傷 public 面。
4. handoff 之後產生的 Seat private event，接手 AI 讀得到。
5. self-service Take Back **還原**舊 floor：原 H1 拿回控制權後重新讀得到 handoff 前自己的 whisper。
6. administrative reassignment **不還原** floor：接手的 H2 讀不到交接前的 Seat private 歷史，但仍取得完整桌面現況。
7. AI rotate / regrant 同樣設新 floor；rotate 後的新 generation 讀不到 rotate 前的 Seat private event。
8. current DM 在上述所有 epoch 變更前後都讀得到該 Seat 的 private event。
9. floor 與 epoch 在 server restart 後保持，不重置為 0、不倒退。
10. concurrency：`Let AI Control` 與 Take Back 競態下，最終 floor 與最終 controller identity 必須一致，不得出現「controller 是 AI 但 floor 已還原成 handoff 前」的組合。

### Temporary Handoff Instruction cases

1. `Let AI Control` instruction省略成功（nullable）。
2. 有instruction時AI第一次context可取得原文。
3. server restart / AI reconnect後同一有效generation仍可取得。
4. instruction不出現在public event、其他Player context、Character JSON、Character Biography/Roleplay Guidance、Campaign permanent facts。
5. Take Back後舊AI token被拒絕，Human UI active instruction清除。
6. 下一次 Let AI Control不自動沿用舊instruction。
7. Session End / Abandon後instruction不再可由任何舊grant context取得。
8. administrative reassignment離開AI後instruction同樣失效。

### Player handoff journey D1

```text
Human access session H1 controls Mira
→ Let AI Control with "Protect the wizard; save last 2nd-level slot"
→ grant stores handoff_return_access_session_id=H1
→ Seat controller_epoch advances; grant generation matches
→ token generated once
→ AI context contains instruction
→ AI cannot read Mira's pre-handoff whispers, but sees full public stage/pending state
→ AI acts as Mira
→ AI disconnect/reconnect
→ instruction still available
→ H1 Take Back Control
→ Seat epoch advances again
→ Human can act again
→ H1 can read its own pre-handoff whispers again (floor restored)
→ old token rejected everywhere by revoke/current-binding/epoch checks
→ instruction inactive
→ Mira id / state / active character unchanged except committed actions
```

### Player recovery journey D1b — lost origin access session

```text
H1 controls Mira
→ Let AI Control
→ H1 browser storage is lost / H1 revoked
→ same person re-enters Room and receives H2
→ H2 self-service Take Back rejected
→ another Room member also rejected
→ Owner/DM administratively reassigns Mira Seat to H2
→ AI grant revoked atomically
→ Seat epoch advances
→ controller=Human(H2)
→ private floor set to this transition (NOT restored): H2 cannot read pre-handoff Seat private history
→ H2 still receives full public stage / pending state
→ Session participant + Active Character remain Mira
→ audit records admin actor + subject Seat + H2
```

### AI DM Start / lifecycle journey D2

```text
Owner creates active Campaign / DM Seat
→ Owner sets DM Seat AI
→ Seat controller_epoch advances
→ mint finite-TTL pre-session AI DM token (session_id=NULL, generation=epoch)
→ AI auth/join
→ AI can read only own minimal pre-session/start context
→ AI calls Session Start before expiry
→ Start transaction rechecks grant current binding + epoch + TTL
→ binds grant to new Session
→ fixed DM controller = that AI grant generation
→ AI authorizes Late Join
→ AI can End as fixed current DM
→ End atomically revokes all Session AI grants
→ same token cannot read/wait/write ended Session
→ same token cannot operate next Session
```

另驗：

- expired pre-session DM token不能Start。
- DM Seat controller change / rotate / archive、Campaign terminal transition或active-Campaign switch讓未綁grant失效。
- active Session時rotate DM grant被拒絕，避免暗中handoff。
- fixed AI DM可Abandon是「current DM actor」權限，不是Owner權限。
- Owner可Abandon不會讓Owner通過current-DM-only Stage / Request Check / End / Late Join / proxy path。
- AI DM disconnect/offline時Session stays active，已綁grant不因presence timeout或舊pre-session TTL被自動revoke。

### Owner Abandon journey D3

```text
AI DM starts Session
→ one AI Player Seat also active
→ Owner Abandon via Human Owner escape hatch
→ Session abandoned
→ DM + Player session-scoped AI grants all revoked
→ old tokens read/wait/write rejected
→ Character Current State remains as committed
→ leases released
```

### End/revoke atomicity

故障注入至少驗：

- revoke step失敗 → Session不能只留下ended/abandoned狀態。
- final event失敗 → lifecycle transaction rollback。
- lease release / pending close失敗 → 不留下「token仍有效但Session半關閉」或相反狀態。

具體transaction順序可不同，但observable result必須all-or-nothing。

### Controller / grant concurrency

真PostgreSQL：

- two Let AI Control同時。
- Let AI Control vs Take Back。
- Take Back vs administrative reassignment。
- two administrative reassignments。
- pre-session rotate vs Start。
- DM Seat controller mutation/archive vs Start。
- Campaign transition / active-Campaign switch vs Start。
- old token write transaction vs revoke。
- stale-grant request vs Seat epoch increment。
- repeated Take Back idempotency / stable conflict。
- End/Abandon vs AI action/wait。
- duplicate AI DM Start / pre-session grant bind。

不得留下兩個current grants、epoch倒退／重用、或grant跨Session重綁。

### Static review checklist

- 是否token plaintext入DB。
- pre-session DM grant是否允許NULL expiry／永久有效。
- unbound pre-session token是否能碰Session private/gameplay surface。
- Seat/controller/Campaign lifecycle是否會遺留仍可Start的unbound grant。
- 是否只驗token hash/status、不驗Seat current grant id + controller_epoch + session/pre-session scope。
- Seat epoch與grant generation誰是authority是否又被混淆；grant generation是否被update而非immutable snapshot。
- 是否AI DM因role=dm順便拿Room Owner service。
- 是否Human/AI controller id混用同一無type欄位。
- 是否為了相容P2而替AI偽造 `RoomAccessContext` / `RoomAccessAuthority`。
- `start_session` / `_is_current_dm` / `late_join` / `end_session` / current-DM `abandon` / live-character authorization是否真的全部進actor-aware policy。
- persistence `start_from_lobby` / late-join current-DM comparison是否仍硬性要求Human access session。
- Take Back是否只看display name／同Room member，而沒有exact `handoff_return_access_session_id`。
- origin access session遺失時是否錯誤auto-claim，而不是Owner/DM admin reassignment。
- administrative reassignment是否atomically revoke AI grant + increment epoch + audit。
- pre-session AI DM grant是否只bind一次。
- End/Abandon是否只best-effort revoke而非同一UoW。
- Temporary Instruction是否污染Character/Campaign permanent state。

---

## P3-E — AI Tool Surface & Event Delivery

### Protocol automated tests

以真HTTP MCP endpoint，**pin `2026-07-28` modern era**；測項不是舊版 handshake negotiation：

1. Server接受合法 `2026-07-28` request，不要求先送 `initialize` / `notifications/initialized`；response/request flow不建立 `Mcp-Session-Id` correctness依賴。
2. `MCP-Protocol-Version: 2026-07-28` 與 request body `_meta.io.modelcontextprotocol/protocolVersion` 一致；缺失／不一致得到 protocol-level reject，不能silent fallback到legacy。
3. `Mcp-Method` 必須與 JSON-RPC method一致；`tools/call`、`resources/read`、`prompts/get` 等 named operation 的 `Mcp-Name` 必須與body name一致；缺失/mismatch reject。
4. `server/discover` 成功並提供 modern capability/cache metadata；但 client不先discover直接送合法 `tools/list` / `tools/call` 仍可工作，證明 discover不是handshake state。
5. `server/discover`、`tools/list`、`prompts/list`、`resources/list` / `resources/read` 中本專案實際支援的 cacheable response在 raw wire有規格要求的 `ttlMs` / `cacheScope`，值符合server policy；不能只看SDK已normalize後的object。
6. tools list穩定且只含P3正式工具。
7. valid unbound pre-session AI DM token只能取得minimal pre-session context + Start；Session/gameplay tools得到stable scope error。
8. expired pre-session token拒絕，即使還未被background/lazy job更新status。
9. no token / invalid / revoked / wrong-session / stale-current-binding token拒絕。
10. Player token呼DM-only tool得到structured permission error。
11. structured result schema可被client SDK解析。
12. `get_session_context` Player projection無secret，含有效Temporary Instruction（若有）。
13. AI Player的 `get_session_context` / `get_pending_events` / `wait_for_event` 都套用 private visibility floor：讀不到交接前的Seat private歷史，但仍取得完整public Stage / pending state。
14. Take Back / admin reassignment / End後同token context call被拒絕，不可再讀instruction。
15. `post_action`後Human browser/event API看到同一canonical action。
16. Human DM Request Check後AI Player `get_pending_events`看到pending。
17. AI `roll_pending`完成後Human UI看到同一result。
18. `wait_for_event` timeout成功empty。
19. MCP wait使用P3-A同一async/no-DB-hold service；不得另包sync blocking wait。
20. wait中server無process-local history依賴；disconnect後cursor補回。
21. revoked / expired / stale epoch token對所有適用tool由共同auth resolver拒絕。
22. standalone endpoint不可用。
23. **tool catalog不得包含 `resolve_action`**；若出現視為P3越界。

如果使用官方/第三方 SDK，另加 negative test或wire capture證明 SDK 已明確 opt in 到 `2026-07-28`，不是 SDK default legacy mode碰巧可call成功。

### Shared-service proof

至少用spy / dependency injection / architecture test證明：

- Web action route與MCP action tool呼同一application service。
- Web roll route與MCP roll tool呼同一Roll service。
- Human route與AI MCP最後都resolve成typed `TableActorContext`進同一permission layer。
- pre-session AI DM grant resolve成restricted pre-session context，不能偽造成active Session actor。
- MCP module不得import persistence repository作direct mutation。
- AI DM細粒度 Narration / Request Check / Current State tool不得偷包成同名half `resolve_action()`。

### External MCP human gate E1

P3-E closeout必做一次真實external client，而且 remote URL必須是 **HTTPS/TLS**：

```text
0. Record external client name/version and prove it can configure static Bearer/header auth.
1. Expose Adventure Table /mcp through TLS tunnel / reverse proxy / HTTPS ingress.
2. Human Player Let AI Control + Temporary Instruction.
3. External MCP client connects to the HTTPS URL with the scoped AI Join Token.
4. get_session_context sees instruction.
5. Human DM在browser送Narration或Request Check。
6. AI wait/get pending event收到。
7. AI post_action。
8. AI完成formal roll。
9. Human browser看到同一action/result。
10. origin Human Take Back或End Session。
11. external client下一個call被拒絕。
```

Closeout記錄：client名稱、版本、static Bearer/header設定方式、HTTPS入口（tunnel/reverse proxy/正式ingress）、測試日期。**不得**用 plain HTTP docker-compose URL當 external gate；不得使用只為測試寫的自製 client來取代「真 external AI host/client」。若預定client無法安全設定 static token，P3-E在此 gate前就是 blocker，先調整client或auth design。

### AI DM gate E2

- pre-session token先驗 limited context/Start-only surface與finite TTL。
- DM context只有Start綁Session成功後才可見DM資料，Player secret不外洩。
- AI DM可Request Check / Narration。
- AI DM可用一般DM proxy path代理Player Seat時，acting/subject audit正確（若MCP surface暴露對應操作）。
- AI DM不能Room Hard Delete / rotate Owner credentials。
- AI DM不能修改Player Character Build。
- AI DM沒有 `resolve_action()`；P3 closeout不得把多個細粒度tool包裝後宣稱P6能力已完成。

---

## P3-F — Full P3 Integration & Closeout

### Journey 1 — Human Exploration + slash commands

```text
Create/Enter Room
→ Campaign/Roster/Lobby
→ Human DM Start
→ Stage text/image
→ /action
→ /search (no automatic roll)
→ /whisper
→ /ooc
→ /check intent
→ DM Request Check
→ Roll
→ Character state change
→ reload
→ continue
→ End
```

驗所有資料持久、End不reset Character State。

### Journey 2 — Secret / Group Roll

```text
DM group check for 3 Player Seats
→ one Human controls 2 Seats
→ third Player separate browser
→ secret DC
→ each rolls / current DM may proxy an unresponsive Seat
→ DM sees all + DC
→ each Player only sees allowed results
→ reload
```

### Journey 3 — Late Join

```text
Session already has Exploration events
→ Late Join Player
→ gets current Stage + allowed current context
→ does not receive old DM-only/private events
→ can receive new public / own events
→ can complete new Request Check
```

### Journey 4 — DM proxies Player Seats

```text
Seat A Controller=Human Offline
Seat B Controller=AI Offline
→ DM proxy Action for A
→ DM proxy formal Roll for A
→ DM proxy Current State action for B
→ A/B controller bindings remain unchanged
→ each event stores acting DM + proper subject Seat/Character
→ reconnect original controller and continue normally
```

### Journey 5 — Human → AI → Human + origin identity

```text
Human access H1 controls Mira
→ Let AI Control + Temporary Instruction
→ grant stores return identity H1 and generation=current Seat epoch
→ real MCP client reconnect/action/wait/roll
→ H1 Take Back succeeds
→ Seat epoch advances
→ old token rejected
→ instruction inactive
→ separate member Hx could not Take Back at any point
```

### Journey 5b — Lost origin credential / admin recovery

```text
H1 Let AI Control Mira
→ lose/revoke H1
→ re-enter Room as H2 with same display name
→ H2 Take Back rejected
→ Owner/DM administratively reassigns Mira Seat to H2
→ AI grant revoked + Seat epoch advances in same transaction
→ controller=Human H2
→ private floor set (not restored); H2 sees no pre-handoff Seat private history but full public context
→ participant/Active Character unchanged
→ audit identifies admin actor, subject Seat, H2
```

### Journey 6 — AI DM pre-session lifecycle + Session End token revoke

```text
Owner assigns AI DM before Start
→ mint finite-TTL pre-session token, generation=current DM Seat epoch
→ before Start, token can only see own minimal start context
→ verify gameplay/event tools reject
→ AI MCP Start rechecks TTL + Seat current grant + epoch and binds token to Session
→ Human/AI Players enter Exploration
→ AI DM Narration / Request Check
→ Human roll
→ AI receives result
→ AI disconnect/reconnect
→ fixed DM identity unchanged
→ End by same AI DM
→ every Session AI token revoked
→ all old clients read/wait/write rejected
→ Character State preserved
```

另有獨立 pre-session negative path：expired token、Seat controller變更/rotate/archive、Campaign離開active或Room active Campaign切換，都讓舊unbound token不能Start。

### Journey 7 — Owner Abandon token revoke

```text
Active AI DM + AI Player
→ Owner Abandon escape path
→ Session abandoned
→ all session-scoped AI grants revoked
→ old clients read/wait/write rejected
→ leases released
→ committed Character State preserved
```

### Journey 8 — Restart recovery

完整dataset至少：

- active Session。
- Stage image+text。
- public / whisper / dm-only events。
- one resolved + one pending RollRequest。
- one PendingAction waiting_for_roll。
- Human controller Seat with controller_epoch與non-zero private_floor_seq。
- AI controller Seat with active token generation + Temporary Instruction + handoff_return_access_session_id。
- unbound pre-session AI DM grant with finite TTL（可另dataset）。
- AI DM或AI Player的grant/generation/session binding與participant snapshot。
- ended historical Session有messages/rolls。

重建server process / engine後：

- cursor接續不重置。
- Stage存在。
- visibility仍正確。
- pending仍pending。
- resolved不會重roll。
- active AI token verifier / generation / session scope仍有效。
- current authorization仍從Seat current grant id + controller_epoch重建，不靠process-local map。
- Temporary Instruction只在原有效generation/context中存在。
- handoff return identity仍可驗 exact access session。
- 各Seat `private_floor_seq` 與 grant `handoff_return_private_floor_seq` 保持persisted值，restart後private可見範圍不放寬也不倒退。
- unbound pre-session expiry仍按persisted timestamp執行，restart不重置TTL。

真PostgreSQL migration/restart至少有一條整合證據；若另有SQLite fast integration，只能補充，不能取代Web DB證據。

### Journey 9 — Cross-scope / controller attack matrix

| Attack | Expected |
|---|---|
| Room A token + Room B Session UUID | reject |
| Active token for Session A + Session B | reject |
| Ended/Abandoned Session token read event | reject |
| Ended/Abandoned Session token wait | reject |
| Ended/Abandoned Session token post action | reject |
| expired unbound AI DM token Start | reject |
| unbound AI DM token read/wait/narrate/roll | reject |
| unbound grant after DM Seat controller change/rotate/archive | reject |
| unbound grant after Campaign no longer Start-eligible | reject |
| `grant.status=active` but Seat current grant id changed | reject |
| `grant.status=active` but generation != Seat controller_epoch | reject |
| same-display-name H2 Take Back H1 handoff | reject |
| unrelated Room member Take Back another Player Seat | reject |
| revoked origin H1 Take Back | reject |
| Player A event cursor猜Player B private event | absent/reject |
| 接手AI以舊seq讀handoff前的Seat private event | absent/reject |
| administrative reassignment後H2讀交接前Seat private event | absent/reject |
| rotate後新AI generation讀rotate前Seat private event | absent/reject |
| floor誤擋public event / Stage / pending / Character State | must NOT be blocked |
| Player token Request Check as DM | reject |
| Owner不是current DM改Stage | reject |
| Owner非current DM proxy Player | reject |
| noncurrent DM Key holder proxy Player | reject |
| `/search` causes automatic formal roll | impossible |
| Player `/check` supplies own secret DC/result | reject/ignored by contract |
| revoked AI token post action | reject |
| stale AI generation roll pending | reject |
| AI DM token呼Owner Hard Delete | reject |
| ended Session新gameplay write | reject |
| quick roll id冒充formal result | impossible/reject |
| MCP legacy initialize-only client presented as 2026-07-28 evidence | reject evidence |
| MCP header/body method mismatch | protocol reject |
| external bearer gate over plain HTTP | reject evidence |
| MCP `resolve_action` in P3 | tool absent |

### Journey 10 — Event waiter resource safety

在production-like ASGI app +真Web DB stack：

```text
start many idle /events/wait requests
→ verify DB pool not pinned by waiters
→ issue normal DB-backed Session/event request
→ request completes while waiters remain pending
→ publish event and verify relevant waiters wake
→ cancel/timeout remaining waiters
→ resource counters return to baseline
```

測試配置應把pool / worker容量刻意縮小，讓錯誤的sync long-poll / hold DB session實作可靠失敗。

### Journey 11 — Standalone

Frozen standalone：

- launch。
- Create/Import/Sheet/Level Up/Export。
- no Room/Session/Stage/Roll/MCP UI。
- no P3 DB tables。
- `test_m03_import_boundary.py` 明確禁止 `app.mcp.*` reachability。
- direct P3 path capability-disabled / not mounted。

---

## 6. Static review gate before final Actions

P3-F跑最終Actions前，static review至少逐項回答：

1. Human / MCP是否存在兩套permission/rules。
2. Human / AI是否真的以typed actor identity進同一gameplay authorization，而非AI偽造Human access context。
3. acting actor與subject Seat是否被混成一個identity；DM proxy audit是否完整。
4. DM proxy是否誤改Seat Controller或繞過Player rules成Direct Edit。
5. 五個slash command是否都有owner；`/search`是否偷自動roll、`/check`是否繞過DM。
6. P2 Session lifecycle / live Character scope有哪些簽名被actor abstraction改動，對應P2 regression是否同步更新。
7. P2三條controller binding CHECK是否由新migration正確演進，歷史0013/0014是否保持不變。
8. Seat `controller_epoch` 是否current generation唯一SSOT；grant generation是否immutable snapshot；每個AI auth是否驗 current grant id + epoch/generation，而非只看grant status。
9. AI grant綁Session的時機是否明確；pre-session DM grant是否只能bind一次。
10. pre-session DM grant是否finite TTL、Start-only/minimal-context scope；Seat/controller/Campaign/active-Campaign lifecycle是否能留下永久unbound bearer。
11. Take Back是否只允許exact origin `handoff_return_access_session_id`；相同display name/同Room member是否被錯放行。
12. origin access session遺失時是否明確走Owner/DM administrative reassignment，且reassignment atomically revoke AI + increment epoch + audit。
13. End/Abandon是否原子revoke全部session-scoped AI grants，而非 best effort cleanup。
14. Temporary Instruction是否只存在handoff lifecycle、失效後不可讀、不污染Character/Campaign資料。
15. DM-only / private資料是否任何endpoint先全量回傳。
16. SEAT_PRIVATE的DM可讀性是否已定案為current DM可讀，三份文件是否還殘留「依contract」「依產品行為」等待定敘述。
17. private visibility floor是否在Server projection與audience filter同一層套用；是否只擋private面而沒有誤傷public event / Stage / pending / Character State。
18. epoch前進的三條path（Let AI Control、rotate/regrant、administrative reassignment）是否都在同一transaction設新floor；self-service Take Back是否正確**還原**舊floor而不是設新值。
19. token secret是否可能進log、DB、exception。
20. formal roll能否duplicate commit。
21. Character State + event是否atomic。
22. event seq是否DB-safe。
23. private recipient query是否有matching membership index或真正 bounded `(session_id,seq)` projection；有沒有無界scan。
24. event wait是否async；等待期間是否零長期DB checkout / sync worker hold；並發waiter是否會starve一般request。
25. Session Resume是否被高頻poll。
26. MCP SDK是否真的 pin `2026-07-28`，還是legacy initialize path；header/_meta/method/name wire tests是否存在。
27. external bearer evidence是否走HTTPS/TLS，且closeout client真的支援 static header auth。
28. MCP tool catalog是否誤包含 `resolve_action`；P3是否偷做P6/P7。
29. `CapabilityFlags` 是否沿用既有 `ai_actor`；`combat` / `timeline` 是否仍false；新增flag是否backend/frontend同步。
30. P3 `postgres-migrations` 是否同時設 P3/P2/M03 URL env，required legacy PostgreSQL tests是否實際run而非skip。
31. `p3-non-e2e.yml` / `p3-e2e.yml` 是否有 workflow contract test釘住名稱、env、test files、full-stack rebuild與artifact。
32. `app.mcp.*` 是否被 M03 standalone import-boundary gate明確擋住，而非以為現有regex會自動match。
33. P3 modules/tables是否被standalone forbidden gate涵蓋。
34. capability backend/frontend是否同步。
35. UI copy是否雙語與hardcoded scan完整。

發現blocker先修code + test並commit，再重新review；review完成後才跑final non-E2E Actions。

---

## 7. P3 closeout required evidence block

P3-F closeout文件至少包含：

```text
Branch / final code SHA
Alembic heads
Backend pytest counts
Frontend unit counts
TypeScript/build result
P3 Non-E2E workflow name + run id + exact SHA
P3 workflow contract test result
PostgreSQL migration/concurrency tests
Explicit P2/M03 PostgreSQL regression execution (not skipped)
P2 caller/controller regression after TableActorContext migration
Seat controller_epoch / grant-generation current-binding evidence
Finite-TTL pre-session AI DM lifecycle + restricted-surface evidence
Take Back origin-Human authorization + admin-reassignment recovery evidence
DM proxy acting/subject evidence
Five slash command evidence
Temporary Handoff Instruction lifecycle evidence
Session End/Abandon AI token revocation + atomicity evidence
Private-recipient event query/index strategy evidence
SEAT_PRIVATE DM-readability positive assertion
Private visibility floor evidence (handoff / rotate / admin reassignment set; self-service Take Back restores; public面不受影響)
Event wait async/no-DB-hold/starvation evidence
P3 Full-Stack E2E workflow name + run id + Playwright artifact
MCP 2026-07-28 raw wire compliance (no legacy handshake dependency; headers/_meta/cache hints)
P3 tool list (resolve_action absent)
External MCP client human gate over HTTPS/TLS
External client name/version/static-auth configuration/test date
Windows standalone frozen build/smoke
app.mcp standalone import-boundary negative fixture
Restart persistence evidence
Known skips / limitations, including no persistent cross-device Human identity for self-service Take Back
```

不得用：

- 「CI綠」代替列出它實際跑了什麼。
- required PostgreSQL suite顯示 skipped 還算作 regression green。
- `grant.status=active` 單獨代替 current Seat grant-id + epoch/generation authorization proof。
- same display name / same Room member代替 origin handoff Human identity。
- 只驗「接手AI讀不到舊private」而沒有同時驗「接手AI仍拿得到完整public桌面現況」；floor誤傷public面與洩漏同樣是regression。
- 手動rotate才能清掉的永久 pre-session AI DM token代替 finite-TTL lifecycle。
- mock MCP handler代替真HTTP / external client。
- legacy `initialize` client成功代替 `2026-07-28` wire evidence。
- SDK high-level discover/list object代替 raw header / `_meta` / `ttlMs` / `cacheScope` contract evidence。
- plain HTTP loopback external URL代替 HTTPS/TLS bearer external gate。
- SQLite concurrency代替PostgreSQL。
- frontend hidden DOM代替Server secret filtering。
- human smoke代替可自動化的authorization regression。
- 單一timeout-return-empty測試代替event wait resource/starvation證據。
- 「token已revoke」單元測試代替 End/Abandon lifecycle整合證據。
- 多個細粒度AI tool成功代替宣稱 `resolve_action()` 已完成。

---

## 8. P3 closeout後不得誤宣稱

P3關門只代表 **Exploration + Roll + AI** 可用。Closeout明確不得宣稱：

- Combat完成。
- Adventure / world runtime完成。
- full AI DM campaign memory完成。
- high-level AI DM `resolve_action()`完成（P6）。
- Timeline / Snapshot / Restore完成（P7）。
- Site Tools所有transport都完成。
- 已有帳號級、跨裝置持久Human identity；P3的Player Take Back self-service仍以原Room access session為可信identity，遺失後走Owner/DM administrative reassignment。

下一個正常產品Phase仍是 **P4 — Quick Combat**。
