# M05-B — Cross-Session Chat History Paging 實作紀錄

## 接手摘要

- **更新日期**：2026-09-20
- **目標與邊界**：新 Session 聊天串向上翻越過 Session 邊界；舊場讀取走專用 `HistoricalSessionReadScope`（Seat 為單位、DM 層跟 `dm_seat_id`），**不改** `resolve_human_actor`／`actor_binding_is_current`／任何 gameplay route；舊事件只進聊天串。契約：`docs/M05/實作規格.md` M05-B（B.1～B.6）、`docs/M05/開發設計方針.md` §4、`docs/M05/測試指南.md` B.1～B.8。
- **Branch**：`feat/m05b-cross-session-history`（自 `main@7b913d3d` 開出）
- **最近已驗證 commit**：`7b913d3d`（M05-A merge；尚無 M05-B 程式）
- **Worker**：agy 做 backend 與純前端邏輯（B1a、B1b、B2a）；UI 接線、copy、E2E、關門由指揮者做
- **下一步**：無；M05-B 關門並合併 `main`，M05 全 Phase 關門，下一步 P6-A
- **阻礙／未審**：無
- **跨步依賴**：B1b 依賴 B1a 的 `resolve_history_scope`；B2a 依賴 B1b 的 `SessionHistoryLink` DTO；B2b 依賴 B2a；B3 依賴全部

## 步驟進度

| Step | 標題 | 狀態 | 依賴 | 紀錄 |
|---|---|---|---|---|
| B1a | backend：history read scope（persistence＋domain）、`/events/history` 非 active 分流、`test_m05b_history_reader_scope.py`（B.2、B.3） | 完成 | — | [B1a](M05-B_steps/B1a.md) |
| B1b | backend：`previous_for_campaign`、`SessionHistoryLink`、`GET /sessions/{id}/previous`、`test_m05b_session_history_link.py`（B.1、B.5 backend） | 完成 | B1a | [B1b](M05-B_steps/B1b.md) |
| B2a | web 純邏輯：`sessionEventStream.ts` chain 型別與函式、`api/sessions.ts` `getPreviousSession`、Vitest（B.4 邏輯） | 完成 | B1b | [B2a](M05-B_steps/B2a.md) |
| B2b | web 接線：`RoomSessionPage` loadOlder 串接、`SessionTableSurface` 分隔線／已到最前、copy 兩 locale、CSS、Vitest（B.4 UI、B.5、B.6） | 完成 | B2a | [B2b](M05-B_steps/B2b.md) |
| B3 | E2E B 段、關門 gate、closeout、合併 `main` | 完成 | B1a～B2b | [B3](M05-B_steps/B3.md) |
