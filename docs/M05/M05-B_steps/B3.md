# B3 — E2E B 段、關門 gate、closeout、合併

## Scope

- `apps/web/e2e/m05-session-history.spec.ts` 補 B 段（`測試指南.md` B.7）：真人 DM 第二場送 narration → 往上翻到分隔線 → 看到第一場 AI narration → 再翻到「已到最前」；Player context 看不到第一場 `dm_only`。
- 關門 gate：全套 backend pytest、`npm test -- --run`、`npm run build`、`docker compose config`、§1.1 M05-B E2E；合併 `main` 前全套 E2E。
- `M05-B_CLOSEOUT.md`、PROJECT_BRIEF（M05 全 Phase 關門、下一步 P6-A）、ROADMAP_HISTORY 移表。

## 前置

- B1a～B2b 已 commit。

## 紀錄

- **起始**：2026-09-20，指揮者。
- **交付**：`m05-session-history.spec.ts` B 段（同一 journey 延伸：DM narration → 翻頁到分隔線／AI narration／DM 層 prompt → 已到最前；Kael／Mira 兩個 member context 驗 Seat scope）；`M05-B_CLOSEOUT.md`；PROJECT_BRIEF／ROADMAP_HISTORY 更新。
- **修正回合**：第一輪 `request_check` 失敗——目標 Seat 需 active Character，改為 import fixture 角色並選給 Player Seat。
- **測試**：見 closeout「關門 gate」。
- **未解問題／下一步**：全套 E2E 的 Builder flake 讓 docker script 在 parallel 失敗後中止，其餘 project 需手動補跑；建議 U01 後續讓 script 跑完所有 project 再彙總。M05 全 Phase 關門，下一步 P6-A。
