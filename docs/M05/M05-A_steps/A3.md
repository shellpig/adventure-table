# A3 — E2E A 段、關門 gate 與 closeout

## Scope

- 新 `apps/web/e2e/m05-session-history.spec.ts` A 段（`測試指南.md` A.5 E2E 流程）：Owner 發 AI DM grant → MCP `start_session` → AI narration → Owner 瀏覽器 End → Lobby 無 active Session → assign 自己為 DM → Start → `dm_controller_kind` human。
- Subphase 關門 gate（AGENTS 工程守則第 4 條）：全套 backend pytest、`npm test -- --run`、`npm run build`、`docker compose config`、`測試指南.md` §1.1 M05-A 對應 E2E。
- `docs/M05/M05-A_CLOSEOUT.md`、PROJECT_BRIEF M05-A 狀態、合併回 `main`。

## 前置

- A1、A2 已 commit。

## 紀錄

- **起始**：2026-09-20，指揮者自己實作。
- **交付**：`apps/web/e2e/m05-session-history.spec.ts`（A 段：API 建 AI DM grant → MCP `start_session`／`set_stage_text`／`post_narration` → Owner 瀏覽器見 AI DM 提示、End 確認文案、End → 舊 token 401／403 → Lobby 無 active → 指派 DM Seat → Start → human DM）；`M05-A_CLOSEOUT.md`。
- **修正回合**：(1) 第一輪缺 `ADVENTURE_TABLE_E2E_ALLOW_DESTRUCTIVE_RESET=1`；(2) 第二輪拿掉「Owner 看得到 AI narration」斷言——Owner 無 Seat 讀不到事件串是既有行為；p2f 兩個失敗是既有順序依賴，第三輪加 `m03c-character-import` 前置後全綠。
- **測試**：見 closeout「關門 gate」。
- **未解問題／下一步**：兩項觀察登記在 closeout；下一步 M05-B。
