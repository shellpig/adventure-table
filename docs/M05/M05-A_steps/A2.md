# A2 — web：Session 頁 Owner End 顯示條件與 copy

## Scope

- `apps/web/src/features/rooms/RoomSessionPage.tsx`：`canOwnerEnd = status === 'active' && isOwner && dm_controller_kind === 'ai' && !isCurrentDm`；End 按鈕顯示 `isCurrentDm || canOwnerEnd`；`canOwnerEnd` 時 confirm 用 `ownerEndAiDmConfirm`；提示依 AI／Human DM 分別顯示 `ownerEndAiDmHint`／`ownerAbandonHint`。
- `apps/web/src/features/rooms/sessionCopy.ts`：新 key `ownerEndAiDmHint`、`ownerEndAiDmConfirm`；改 `ownerAbandonHint`；zh-TW／en 文案見 `開發設計方針.md` §3.4。
- Vitest：`RoomSessionPage.test.ts` 三個情境＋copy parity。
- 契約：`實作規格.md` A.5；`開發設計方針.md` §3.3、§3.4；`測試指南.md` A.5。

## 前置

- A1 已 commit（Owner 對 AI DM 場 `POST /end` 200）。

## 驗收

- `npm test -- --run`、`npm run build`（cwd `apps/web`）。

## 紀錄

（待補）
