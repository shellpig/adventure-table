# A1 — backend：`end_session` Owner 分支

## Scope

- `apps/server/app/domain/rooms/sessions.py`：`SessionService.end_session` 在 `_require_active` 之後、`_resolve_human_actor` 之前加 Owner 分支——`context.authority is RoomAccessAuthority.OWNER and stored.dm_controller_kind == "ai"` → `_finalize_with_event(status=ENDED, actor=None, owner_access_session_id=context.access_session_id)`。其餘路徑（Human DM 場、非 Owner、`end_session_actor`）零改動。
- `apps/server/tests/test_m05a_owner_end_ai_dm.py`：A.1～A.4 的 domain／API 測試（含零副作用斷言）。
- 契約：`實作規格.md` A.1～A.4；`開發設計方針.md` §3.1、§3.2；`測試指南.md` A.1～A.4、A.6。

## 前置

- 無程式依賴。fixture 沿用 `tests/test_p3d_owner_abandon.py`（`_seed_ai_dm_lobby`、`start_session_as_ai_dm`）。

## 驗收

- `pytest tests/test_m05a_owner_end_ai_dm.py tests/test_p3d_*.py tests/test_p2e_session_*.py tests/test_p3a_*.py tests/test_code_quality_gate.py`（cwd `apps/server`）全綠。

## 紀錄

- **起始**：2026-09-20，worker agy（`Gemini 3.8 Flash (High)`），1 回合 3.5 分鐘，prompt `C:\_work\AI_Work\Toolsgy-runsgy-m05a-A1.prompt.txt`，conversation `8b9557b7-571c-46e2-a111-88087b2073fa`
- **交付**：`SessionService.end_session` Owner 分支（12 行，與 `開發設計方針.md` §3.1 逐字一致）；`tests/test_m05a_owner_end_ai_dm.py` 9 個測試（A.1×2、A.2×2、A.3×3、A.4×2），含 before／after row-count 與 epoch 快照的零副作用斷言、`character_states` 不變斷言。
- **指揮者審核修正**：
  - 刪掉 `test_ai_dm_token_rejected_after_owner_end`——它只是呼叫另一個測試的空殼；token 拒絕斷言已在 `test_ai_dm_binding_rejected_after_owner_end`。
  - 兩個 fixture builder 各自貼了 ~50 行 room／campaign／access-session 種子，抽成 `_seed_room_and_campaign` 與 `_insert_access_session`。
  - production diff 零修正。
- **測試**：`pytest tests/test_m05a_owner_end_ai_dm.py tests/test_p3d_*.py tests/test_p2e_session_*.py tests/test_p3a_*.py tests/test_code_quality_gate.py`（cwd `apps/server`）exit 0；focused 9 passed。
- **未解問題／下一步**：無；A2（web）。
