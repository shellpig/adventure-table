# Adventure Table — 單機創角版本討論

> 本文件只用來討論**單機（可下載、離線執行）創角版本**的定位、可行性、必要界線與交付順序。
>
> 這裡**不寫實作規格**。資料模型、API、打包腳本、JSON schema 欄位等細節，等 M03 開工時寫進 `docs/M03/` 三份正式文件。
>
> 若本文件與 `規格企劃.md` 衝突，以產品規格為準。

最後更新：2026-09-02

---

## 一、要做的是什麼

一個**下載、解壓、執行**就能用的離線角色建立工具。

- 使用者不需要安裝 Python、Node、Docker 或資料庫。
- 執行後自動開啟瀏覽器，看到的就是現在的 Character Workshop / Builder / Character Sheet。
- 角色存在執行檔旁邊的本機檔案。
- 角色可以匯出成 JSON，也可以匯入別人的 JSON。

範圍上限就是**目前已完成的角色相關功能**：Character Workshop、Builder、Level Up、Version History、Character Sheet、Multi-Source Content Packs、`zh-TW` / `en` 雙語。

不包含、也永遠不會包含本質上就是多人的東西：Room、Session、Seat、Combat、Timeline、AI Actor、DM 工具。這不是缺陷，是定義。

---

## 二、產品規格已經允許

`規格企劃.md` 第五章〈Character Workshop〉：

> 未加入 Room 前也能創角色。沒有帳號時可 browser-local + JSON；進 Room / Campaign 後 Server persistent。

「不需要 Room 也能創角色」是既有產品保證，不是為了單機版新開的例外。單機版是把這條保證包裝出貨，不是分支產品。

---

## 三、可行性

2026-09-02 檢查現有 codebase 的結果：

| 原本的疑慮 | 實際狀況 |
|---|---|
| 需要 PostgreSQL | **SQLite 已可用且已被測試覆蓋**。整套後端測試就跑在 `sqlite+pysqlite` 上；`json_payload_type = JSON().with_variant(JSONB(), "postgresql")` 本來就是 dialect-agnostic；migration `0005` 已有 sqlite 分支 |
| 前端需要獨立 server | SPA 打的是相對路徑 `/api/...`。FastAPI 掛靜態檔即同源，不需要 CORS 或額外設定 |
| 需要連外部服務 | 沒有。產品守則「網站不接 LLM API」成立，規則內容全部是磁碟上的 JSON |
| 需要登入 / 多使用者 | 目前沒有 |

以現有範圍來說，這是一個被拆成三個容器在跑的單機程式。

### 已知的既有障礙

`app/content/registry.py` 的 `REPOSITORY_ROOT = Path(__file__).resolve().parents[4]` 寫死了 repo 目錄結構。凍結打包後會找不到 `data/`，必須改成可被環境變數或 bundle 路徑覆寫。

這是既有脆弱點，與單機版無關，可以獨立先修。

---

## 四、打包方式

| 方案 | 使用者要安裝 | 概估大小 | 結論 |
|---|---|---|---|
| **PyInstaller one-folder + 靜態 SPA + SQLite** | 無 | 60–100 MB | **採用** |
| 現有 Docker Compose | Docker Desktop | — | 已可用，但不是「解壓執行」 |
| 內嵌 Python + `.bat` | 無 | 150 MB+ | 打包簡單，但目錄雜亂、啟動慢 |

採用 PyInstaller one-folder。流程：雙擊執行檔 → 選一個空閒的 localhost port 起 uvicorn → 自動開瀏覽器 → 關閉時收掉 process。

已知風險：Windows SmartScreen / 防毒對 PyInstaller 產物誤判是常態，朋友間傳檔要有心理準備。

---

## 五、同一份 codebase，兩個 entry point

不做第二份 repo，也不用 feature flag 散落在各處。

```text
單機版 entry → FastAPI 只掛 content / character / builder 相關 router
             → 前端只 build Workshop / Builder / Sheet 路由
             → SQLite 檔放在執行檔旁邊

線上版 entry → 全部 router，含 P2 之後的 Room / Session / Seat
```

同一份 domain code，差別只在掛哪些 router、build 哪些前端路由。單機版包裡不會出現半殘的多人 UI。

---

## 六、JSON 交換（已拍板）

單機版第一版就要**有出有進**：

- 可匯出角色 JSON。
- 可匯入角色 JSON。
- 匯入來源包含：網頁版匯出的角色、其他人單機版匯出的角色。
- 網頁版同樣要能匯入單機版匯出的角色。

也就是說，JSON 是網頁版與單機版之間、以及不同人的單機版之間的通用交換格式，不是單機版專用的備份檔。

### 版本相容性（已拍板）

開發期間允許舊版 JSON 失效。角色資料模型仍在變動，不為了向後相容而綁住 M01 / P2 的設計。

到某個版本再正式鎖定 JSON schema，之後才承擔相容責任。**鎖定在哪個版本尚未決定**，等 M03 或之後再定。

### 與 P7 的關係

Character JSON Import / Export 原本排在 P7。單機版把它提前，但**只提前角色本身的交換**，不含 P7 其餘的資料生命週期工作（Campaign export、全站備份等）。

---

## 七、P2 必須守住的界線

會殺死單機版的不是 P2 有多少功能，而是 P2 把 Room 的概念滲進角色層。

| # | 界線 | 破壞後果 |
|---|---|---|
| 1 | `characters` 表不得長出 `room_id` / `campaign_id` / `session_id` 欄位；Campaign 歸屬走 Party Roster join table | 角色無法脫離 Room 存在，單機版死亡 |
| 2 | Character Campaign Status（`active` / `inactive` / `retired` / `dead`）住在 roster row，不是 character row | 同上；同一角色跨 Campaign 也會打架 |
| 3 | `app/content`、`app/domain/character*`、`app/persistence/characters` 與 `builder_drafts` 不得 import 任何 room / session / seat 模組，依賴只能單向 | import 一牽就得整包帶走多人機制 |
| 4 | 權限檢查以 route 外層的 wrapper / dependency 加入，不寫進 character domain 內部 | Builder 邏輯開始需要 Seat 才能執行 |

第 1、2 條是 P2 的 schema 決策，開工前就要定案。第 3、4 條靠測試與 CI 保證，不靠自律。

### 機械化保證

1. **Import boundary test**：斷言上述模組的 import graph 不碰 room 層。
2. **單機版打包進 CI**：只要有人讓角色層依賴到 Room，單機版 build 直接失敗。

這兩道是單機版能在 P2 之後繼續存在的真正原因。文件會被違反，失敗的 build 不會。

---

## 八、交付順序（已拍板）

```text
M01 收尾  →  M03 單機創角版本  →  P2
```

理由：

- **現在整個 codebase 只有角色這一層，沒有 Room 可以滲進來。** 界線在此刻自動成立，是最便宜的時機。
- 單機版先進 CI，P2 開工時就已經有一個會失敗的 build 當守門員；反過來做則只有一份文件寫著「不要這樣」。
- P2 是大 Phase，開發期間朋友本來就只能建角色，單機版提早出就有實際用途。
- P2 的 schema 決策在真實約束下做，比對著文件猜可靠。

### 承擔的代價

- 多一個永久維護的 entry point 與打包流程，每次 release 多一份要驗的產物。
- 朋友手上會有本機資料庫檔，之後每次 schema 變動都要靠 alembic 平順升級，不能砍掉重建。這條不論何時出貨都躲不掉，只是提早開始。
- 界線 1、2 偶爾會逼出稍多的程式碼（join table 比加一個欄位麻煩）。

---

## 九、M03 開工時要決定的事

本文件刻意不決定的，留給 `docs/M03/`：

- Character JSON schema 的實際欄位與版本標記方式。
- 匯入時的驗證與衝突處理（重複角色、缺少 content pack、規則版本不符）。
- 單機版本機資料庫的位置、初始化與 migration 執行時機。
- PyInstaller spec 的資料檔打包方式（`data/`、alembic 腳本）。
- launcher 的 port 選擇、瀏覽器開啟與關閉行為。
- 單機版的 CI build 與驗收證據要求。
- Import boundary test 的實際斷言範圍。

---

## 十、待決事項

| 項目 | 狀態 |
|---|---|
| JSON schema 要鎖在哪個版本 | 未定，M03 或之後再決定 |
| 單機版是否要支援 Windows 以外的平台 | 未定 |
| `REPOSITORY_ROOT` 路徑修正是否獨立於 M03 先做 | 未定 |
